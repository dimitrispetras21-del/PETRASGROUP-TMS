# Σημειώσεις Παντελή — έλεγχος στη βάση & πλάνο εκτέλεσης

**Ημερομηνία:** 25/8/2026 · **Πηγή:** «Σημειώσεις για TMS» (9 σημεία)
**Κατάσταση:** ανάλυση ολοκληρωμένη, καμία αλλαγή δεν έγινε — εκκρεμούν
αποφάσεις owner.

Κάθε σημείο ελέγχθηκε στη Supabase και στον χάρτη του Worker, όχι στην οθόνη.

---

## Η μεγάλη εικόνα: 5 από τα 9 είναι το ΙΔΙΟ πρόβλημα

Τα σημεία 1, 2, 3, 5 (και εν μέρει το 4) δεν είναι πέντε αιτήματα. Είναι πέντε
συμπτώματα του ότι το `locations` έχει **μία** ελεύθερη στήλη `type` που κάνει
τη δουλειά τεσσάρων πεδίων που δεν υπάρχουν.

Μέτρηση στις 1.185 ενεργές τοποθεσίες:

| Εύρημα | Νούμερο |
|---|---|
| Διακριτές τιμές στο `type` | **~200** |
| Κενό `type` | 612 (52%) |
| Μορφή `CLIENT : <όνομα>` | η συντριπτική πλειοψηφία των γεμάτων |
| `opening_hours` γεμάτο | **0 / 1.185** |
| `delivery_days` γεμάτο | **0 / 1.185** |

Το `type` χρησιμοποιείται ήδη αυθόρμητα ως **notes** και ως **tag πελάτη** —
επειδή δεν υπάρχει αλλού να μπουν. Πραγματικές τιμές παραγωγής:

- `NOTES : ΓΡΑΦΕΙΟ` · `NOTES : ΠΕΝΟΓΛΟΥ ΦΡΕΝΑΣ` · `NOTES : OGL`
- `ΡΟΚΑΚΗΣ : 210 558 4237`  ← **τηλέφωνο μέσα στο type**
- `ΖΥΓΙΣΗ ΓΙΑ ΜΠΑΡΜΠΑ ΣΤΑΘΗ` · `Ηλεκτρολόγος φορτηγών, Νεοχωρούδα`
- `TERESPOLSKA 1 NOWY KONIK PL 05074`  ← **διεύθυνση μέσα στο type**
- `CLIENT : GLOBAL, KMG (ΓΚΟΠΗΣ)`  ← **πολλοί πελάτες σε μία τιμή**

Και οι παραλλαγές του ίδιου πελάτη είναι ασύμβατες μεταξύ τους:
`CLIENT : KMG` / `CLIENT : KMG ` (κενό στο τέλος) / `CLIENT: KMG` /
`CLIENT : KMG (ΓΚΟΠΗΣ)` / `CLIENT : ΓΚΟΠΗΣ (KMG)` / `CLIENT : ΓΚΟΠΗΣ` —
**έξι γραφές, ένας πελάτης.** Αναζήτηση ανά πελάτη (αίτημα #3) σήμερα είναι
αδύνατη· δεν είναι θέμα UI.

**Συνέπεια για το #1** (pop-up με στοιχεία προορισμού): το pop-up είναι εύκολο,
αλλά **δεν έχει τι να δείξει** — ωράριο και ημέρες παράδοσης είναι 0/1.185.
Η φόρμα τα έχει (`locations.js:411-416`), ο Worker τα χαρτογραφεί
(«Wave 3, owner-approved 8/8»), κανείς δεν τα συμπλήρωσε ποτέ.

---

## Ανά σημείο

| # | Αίτημα | Τι βρέθηκε | Τι θέλει |
|---|---|---|---|
| 1 | Locations: pop-up στοιχείων | Στήλες υπάρχουν, **δεδομένα 0** | front end + **συμπλήρωση δεδομένων** |
| 2 | Locations: πεδίο NOTES | Δεν υπάρχει στήλη· γίνεται ήδη μέσα στο `type` | migration |
| 3 | Locations: tags + αναζήτηση ανά πελάτη | Γίνεται ήδη ως `CLIENT : X`, με 6 γραφές/πελάτη | migration + **καθαρισμός** |
| 4 | Locations: διαχωρισμός φόρτωση/εισαγωγή/εξαγωγή/παράδοση + εσωτ./διεθν./εθν. | Δεν υπάρχει· το `type` δεν το κάνει | **σχεδίαση** — βλ. ερώτημα |
| 5 | Locations: tags προϊόντων ανά σημείο φόρτωσης | Δεν υπάρχει· ίχνη στο `type` (`ΚΡΕΑΤΑ`, `ΑΥΓΑ`, `ΜΑΝΙΤΑΡΙΑ`, `ΠΑΤΑΤΕΣ`) | migration (ίδιο με #3) |
| 6 | Daily Ops: Arrival time στα LIDL | **Ήδη σχεδιασμένο** ως «Loading/Delivery Appointment» (owner 10/8, `weekly_natl.js:1183-1200`) | migration + UI |
| 7 | Daily Ops: κουμπί «Αναβολή» | **ΣΦΑΛΜΑ ΕΠΙΒΕΒΑΙΩΜΕΝΟ** — βλ. κάτω | front end μόνο |
| 8 | Μοναδικός κωδικός παραγγελίας | 37/87 έχουν `reference`, **35 διακριτά → 2 διπλά ήδη** | απόφαση + migration |
| 9 | Driver: πολλά τηλέφωνα, προσωπικό/εταιρικό | Μία στήλη `phone` | migration |

---

## #6 — δεν είναι «να προστεθεί πεδίο»· η ώρα ΔΕΝ ΥΠΑΡΧΕΙ πουθενά

Ελέγχθηκαν και οι τρεις πίνακες που θα μπορούσαν να την κρατούν:

| Πίνακας / στήλη | Τύπος | Πραγματικά δεδομένα |
|---|---|---|
| `orders.loading_datetime` / `delivery_datetime` | **`date`** | καμία ώρα — δεν χωράει |
| `order_stops.datetime` | **`date`** | καμία ώρα — δεν χωράει |
| `national_loads.loading_datetime` / `delivery_datetime` | `timestamptz` | **18/18 ακριβώς 12:00** |

Το 12:00 δεν είναι ραντεβού: είναι σταθερό placeholder, γραμμένο από το
`orders_intl.js:1065` (`natLoadDt + 'T12:00:00.000Z'`).

Άρα: **σε ολόκληρο το TMS δεν αποθηκεύεται σήμερα ούτε μία πραγματική ώρα
ραντεβού.** Οι ώρες ζουν μόνο μέσα σε ελεύθερο κείμενο, όπως καταγράφει και το
σχόλιο στο `weekly_natl.js:1183`: «LIDL ΑΤΤ (ΠΑΡΑΔΟΣΗ ΣΤΙΣ 10.00)».

Το πεδίο «Loading/Delivery Appointment» είχε σχεδιαστεί (owner 10/8) αλλά **δεν
δημιουργήθηκε ποτέ**. Το αίτημα του Παντελή είναι ακριβώς αυτό.

---

## #7 — το μόνο καθαρό bug: η αναβολή ΠΑΓΩΝΕΙ την παραγγελία

**Διόρθωση προηγούμενης εκτίμησης.** Αρχικά έγραψα ότι η «Αναβολή» καταστρέφει
την ώρα. **Λάθος.** Το `orders.loading_datetime` είναι τύπου **`date`** στη
βάση — δεν έχει ώρα καθόλου, παρά το όνομα «DateTime». Το ότι γράφεται
ημερομηνία χωρίς ώρα είναι σωστό, όχι σφάλμα. Ο υπολογισμός +1 ημέρας είναι
επίσης σωστός.

Το πραγματικό σφάλμα είναι στην εμφάνιση, `modules/daily_ops.js:434` και `:459`:

```js
const isPostponed = !!f['Postponed To'] && !isDone && !isInTransit;
// ...
const actionCol = statusBadge ? `<td>${statusBadge}</td>`
                              : `<td>${loadBtn} ${postBtn}</td>`;
```

Μόλις γραφτεί το `Postponed To`, η γραμμή δείχνει **μόνο** το σήμα
«ΑΝΑΒΛΗΘΗΚΕ». Χάνει **και** το «Σε μεταφορά» **και** την «Αναβολή».

Συνέπεια στην πράξη: την επόμενη μέρα, που η παραγγελία κανονικά φορτώνει,
ο dispatcher **δεν έχει κανένα κουμπί να την προχωρήσει**. Δεν μπορεί ούτε να
την ξανα-αναβάλει. Το `Postponed To` δεν καθαρίζεται ποτέ, οπότε η κατάσταση
είναι μόνιμη.

**Μετρημένο στην παραγωγή σήμερα: 3 παραγγελίες** σε αυτή την κατάσταση
(status `Assigned`, αναβολή έως 25/8, παλαιότερη φόρτωση 22/8).

Η διόρθωση είναι στο front end μόνο: το «ΑΝΑΒΛΗΘΗΚΕ» να είναι σήμα **δίπλα**
στα κουμπιά, όχι αντί για αυτά.

---

## #8 — δεν είναι μόνο «να προστεθεί κωδικός»

Σήμερα το `reference` είναι προαιρετικό ελεύθερο κείμενο:
**87 παραγγελίες, 37 με κωδικό, 35 διακριτοί** → δύο ήδη διπλοί.
Στα `national_loads`: 18 φορτία, 8 με κωδικό.

Δηλαδή 57% των διεθνών παραγγελιών δεν έχουν κανέναν κωδικό, και η μοναδικότητα
δεν επιβάλλεται πουθενά. Θέλει απόφαση owner πριν γραφτεί κώδικας.

---

## Τι ΔΕΝ έγινε

Καμία αλλαγή σε βάση, Worker ή front end. Καμία migration δεν εκτελέστηκε.

---

# Συμπλήρωμα 25/8 (owner): «το ωράριο υπάρχει αλλά δεν εμφανίζεται» + κάρτα τοποθεσίας

## Γιατί δεν εμφανίζεται — read-side drop

Η αιτία δεν είναι η φόρμα ούτε ο Worker. Είναι η **ανάγνωση**.

`modules/locations.js:535-536` — η λίστα ζητά μόνο:

```js
fields: ['Name','Country','City','Address','Type','Latitude','Longitude']
```

Το `Opening Hours` και το `Delivery Days` **δεν ζητούνται ποτέ**. Και το
`_locOpenEdit` (`:375`) δεν ξαναδιαβάζει την εγγραφή — παίρνει το
**cached record της λίστας**:

```js
const rec = LOC.records.find(r => r.id === id);
openModal('Edit Location', _locFormHTML(rec.fields), ...)
```

Άρα `f['Opening Hours']` είναι πάντα `undefined` → το input πάντα κενό, **ακόμη
κι αν η βάση έχει τιμή**. Και επειδή το `_locSave` (`:450`) στέλνει το πεδίο
μόνο αν δεν είναι κενό, μια τιμή που κάποτε γράφτηκε γίνεται **αόρατη για
πάντα** και δεν ξαναγράφεται ποτέ.

Είναι το ίδιο μοτίβο που επισήμανε η ValueDriven στις 10/8 — αλλά στο **άλλο
μισό**: όχι «γράφω και δεν αποθηκεύεται», αλλά «αποθηκεύεται και δεν διαβάζεται».
Το αποτέλεσμα για τον χρήστη είναι ίδιο.

**Διόρθωση:** τα δύο ονόματα στη λίστα πεδίων. Δύο λέξεις.

## Η κάρτα τοποθεσίας — τι μπορεί να δείξει ΣΗΜΕΡΑ, χωρίς migration

Σήμερα δεν υπάρχει καθόλου κλικ σε γραμμή· μόνο ✏️ και 🗑️.

Μετρήσεις που δείχνουν ότι η κάρτα έχει πραγματικό περιεχόμενο από μέρα ένα:

| Πηγή | Νούμερο |
|---|---|
| `order_stops` με `location_id` | **267 / 267 (100%)** |
| Διακριτές τοποθεσίες με ιστορικό | **111** |
| Στάσεις με `goods` (εμπόρευμα) | 178 · σε **77** τοποθεσίες |
| Στάσεις με `stop_type` | **267 / 267** |

Και η κατανομή `stop_type`:

| Τύπος | Στάσεις | Τοποθεσίες |
|---|---|---|
| Loading | 126 | 61 |
| Unloading | 123 | 55 |
| Cross-dock | 18 | 1 (Βέροια) |

### Συνέπεια για το #4 και το #5 του Παντελή

- **#4** (διαχωρισμός ανά φόρτωση / παράδοση): **δεν χρειάζεται χειροκίνητη
  ταξινόμηση** για τις 111 τοποθεσίες με ιστορικό — προκύπτει από το
  `stop_type`. Ο ρόλος γράφεται μόνος του: «Φόρτωση 12× · Παράδοση 3×».
- **#5** (tags προϊόντων ανά σημείο φόρτωσης): το σύστημα **ήδη ξέρει** τι
  φορτώθηκε — `order_stops.goods`, 178 εγγραφές σε 77 τοποθεσίες. Αντί για
  χειροκίνητα tags, η κάρτα δείχνει τα πραγματικά εμπορεύματα του σημείου·
  τα χειροκίνητα tags μένουν ως προαιρετικό δεύτερο στρώμα.

**Το 90% της αξίας που ζητά ο Παντελής στα #1/#4/#5 υπάρχει ήδη στα δεδομένα
και απλώς δεν εμφανίζεται πουθενά.**

Επιφύλαξη που πρέπει να ειπωθεί: **111 από 1.185** τοποθεσίες έχουν ιστορικό.
Οι υπόλοιπες θα δείχνουν άδεια καρτέλα ιστορικού — και πρέπει να το λέει
καθαρά, όχι να μοιάζει με σφάλμα (**αρχή 1**).
