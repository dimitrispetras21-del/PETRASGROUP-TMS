# Redesign — ημερολόγιο παρατηρήσεων

Κρατιέται **κατά** την εκτέλεση, όχι μετά. Από αυτό γράφεται στο τέλος το
skill `graph-task` — ώστε να κωδικοποιεί τι όντως έσπασε, όχι τι υποθέσαμε.

Απόφαση owner 28/8/2026: το skill γράφεται **τελευταίο**.

## Τι καταγράφεται

| Κατηγορία | Παράδειγμα |
|---|---|
| **Ψευδής συναγερμός** | κριτής κοκκίνισε χωρίς πραγματικό πρόβλημα |
| **Διαφυγή** | κάτι έσπασε και **κανένας** κριτής δεν το έπιασε — το πιο πολύτιμο |
| **Νεκρός κόμβος** | agent δεν γύρισε· τι τον σκότωσε |
| **Απόκλιση χρόνου** | πύλη που πήρε πολύ περισσότερο απ' ό,τι εκτιμήθηκε |
| **Κρυφή ακμή** | δύο κόμβοι που πάτησαν ο ένας τον άλλο |

## Καταγραφές

### 2026-08-28 — εκκίνηση

- Σημείο μηδέν: 528 hex μέσα σε modules, 22.795 γραμμές, 11 μονάδες.
- Ο κριτής #5 («άγνωστο ≠ μηδέν») κοκκίνισε από την πρώτη στιγμή σε πραγματικά
  ευρήματα. Δεν διορθώθηκαν εδώ — ανήκουν στο redesign κάθε μονάδας.

### 2026-08-28 — Διαφυγές κατά την κατασκευή της σουίτας

1. **Διαφυγή** — Το πλάνο δήλωνε «@playwright/test ήδη εγκατεστημένο» επειδή
   ήταν στο `package.json`. Δεν υπήρχε `node_modules`· το `record-har.js`
   έσκαγε με MODULE_NOT_FOUND πριν ανοίξει browser. Μάθημα: «δηλωμένο» ≠
   «εγκατεστημένο».
2. **Διαφυγή** — Το πλάνο όριζε ψεύτικο χρήστη `critic`. Ο tamper guard του
   `core/auth.js` πετάει όποιον λείπει από τη λίστα `USERS` του `index.html` →
   ατέρμονη ανακατεύθυνση. Λύση: `demo_<role>`. Το `CLAUDE.md` το
   προειδοποιούσε ρητά.
3. **Διαφυγή (η χειρότερη)** — Το πλάνο πλοηγούσε με `app.html#/route`. Η
   εφαρμογή δεν διαβάζει καθόλου `location.hash`. Ο σωστός τρόπος ήταν ήδη
   γραμμένος στο `tests/e2e/smoke.spec.js:71`, στο αρχείο που το ίδιο το
   πλάνο ανέφερε ως πρότυπο.
4. **Διαφυγή** — Ο εξαγωγέας συμβολαίου υπέθεσε ότι κάθε οθόνη είναι πίνακας
   (`th`/`label`). Τα `weekly_intl`/`weekly_natl` είναι Command Center/kanban
   και δεν έχουν κανένα. Μένουν χωρίς συμβόλαιο μέχρι το κύμα 4.

### Κατάσταση 28/8/2026

- 528 hex μέσα σε modules στην εκκίνηση.
- 9/11 συμβόλαια (`docs/redesign/contracts/`) — λείπουν `weekly_intl`,
  `weekly_natl` (βλ. Διαφυγή #4).
- 23 κελιά «άγνωστο ως μηδέν»: `metrics_audit` 5, `workshops` 4,
  `maint_svc` 14. ⚠️ **ΚΑΤΩ ΟΡΙΟ, όχι το σύνολο.** Ο κριτής #5 ταιριάζει μόνο
  ακριβές κείμενο `€0`/`0%`/`0 €` **μέσα σε `<td>`**. Χάνει το ίδιο σφάλμα σε
  κάρτες KPI (επαληθευμένο στο `core/entity.js`) και σε άλλες μορφοποιήσεις
  (`0.0%`, `0,0%`, `€0.00`). Μη διαβάσεις το 23 ως τον λογαριασμό.
- 0 διαρροές P&L προς τον `dispatcher`.

### 2026-08-28 — έλεγχος της ίδιας της σουίτας (9 ευρήματα, όλα διορθώθηκαν)

Ο κριτής χρειάστηκε κριτή. Τα τρία σοβαρότερα ήταν **σιωπηλές** — η σουίτα
έβγαινε πράσινη ενώ δεν είχε ελέγξει τίποτα:

1. **Νεκρός κόμβος χωρίς φρουρό.** Ο fan-in guard είχε μπει μόνο στο στατικό
   μισό του `run.js`· το `report.errors` του Playwright δεν διαβαζόταν ποτέ.
   Όταν το playwright δεν έβρισκε κανένα test, τυπωνόταν
   `0/0 ζωντανοί έλεγχοι πέρασαν` **χωρίς exit code** — δηλαδή `npm run critics`
   έβγαινε 0 έχοντας τρέξει μηδέν ζωντανούς ελέγχους.
2. **Ο φρουρός που αυτοκτονούσε.** Το `repair-har.js` είχε
   `if (removed !== 5) throw`, και έτρεχε σε **module load** του `auth.js`. Μια
   νέα ηχογράφηση με 4 ή 6 χαλασμένες εγγραφές — που ο ανασχεδιασμός εγγυάται —
   δεν χαλούσε ένα test· εμπόδιζε τα δύο spec να **φορτώσουν**. Μαζί με το #1,
   ολόκληρη η ζωντανή σουίτα εξαφανιζόταν αναφέροντας επιτυχία.
3. **Διαφυγή — το `assets/style.css` δεν το μετρούσε κανείς.** 427 hex και 27
   κανόνες κοπής, εκτός κάθε μονάδας, ενώ οι στατικοί κριτές διάβαζαν μόνο
   `.js`. Ο ανασχεδιασμός που κάνει το **σωστό** (χρώμα και κοπή έξω από τη JS,
   μέσα στο CSS) θα κατέβαζε κάθε αριθμό της καστάνιας — θα διαβαζόταν ως
   πρόοδος — ενώ θα έκοβε επωνυμίες πελατών εκεί που δεν κοιτούσε κανείς
   (DESIGN.md #6). Έγινε μονάδα `styles` με δικό της όριο.

Τα υπόλοιπα έξι: τα `actions` καταγράφονταν και δεν συγκρίνονταν ποτέ (κάθε
κουμπί κάθε οθόνης μπορούσε να σβηστεί)· οι σημασιολογικοί κριτές περνούσαν σε
**λευκή σελίδα** (μηδέν διαρροές, μηδέν μηδενικά — επιβεβαιώθηκε: 2 passed σε
κενό έγγραφο)· το `critics:capture` ξαναέγραφε κάθε baseline πιο **χαλαρό**
χωρίς να το πει· σε CAPTURE το συμβόλαιο γραφόταν **πριν** τον έλεγχο «φόρτωσε
η οθόνη;»· το `units.test.js` έλεγχε βαθμίδες με πλήθος (το `entity` μπορούσε
να υποβιβαστεί σιωπηλά, αφοπλίζοντας έξι οθόνες)· και το `isKnownGap()`
ταξινομούσε από την **ύπαρξη αρχείου**, οπότε οποιαδήποτε άλλη αποτυχία στα
`weekly_intl`/`weekly_natl` βαφόταν «γνωστό κενό».

**Μάθημα (το ίδιο και για τις τρεις σιωπηλές):** ένας φρουρός που κανείς δεν
τον είδε να χτυπά δεν είναι φρουρός. Καθένας από τους τρεις δοκιμάστηκε με
σκόπιμη αποτυχία και μετά επανήλθε — βλ. `.superpowers/sdd/fix-final-report.md`.

### Κατάσταση μετά τη διόρθωση

- **12** μονάδες (11 κώδικα + `styles`). Ζωντανοί έλεγχοι: **33** = 11 μονάδες
  με routes × 3· ο ενορχηστρωτής ματαιώνει (**exit 2**) αν τρέξουν λιγότεροι.
- Νέο όριο `styles`: hex **427**, κοπή **27**.
- Χαλάρωση καστάνιας μόνο με ρητό `CRITICS_WIDEN=1`, και τυπώνεται τι χαλάρωσε.
- Παραμένει το γνωστό κενό: `weekly_intl`/`weekly_natl` χωρίς συμβόλαιο →
  `npm run critics` βγαίνει **1**, όπως και πριν.

### 2026-08-29 — Κύμα 1, σχεδίαση

**Διαφυγή #5 — σχεδίασα πριν διαβάσω τη σημερινή σελίδα.** Πρόσθεσα στήλη
«ΕΓΓΡΑΦΑ» που ο owner είχε ήδη πει ότι δεν θέλει, ένωσα Μάρκα/Μοντέλο σε μία
στήλη ενώ είναι δύο, και παρέλειψα εντελώς τα τρία φίλτρα που υπάρχουν. Όλα
φάνηκαν μόλις διάβασα το `ENTITY_CONFIG`. Ίδια αιτία με τα λάθη αντίθεσης της
28/8: **υπέθεσα αντί να κοιτάξω.**

**Διαφυγή #6 — «44px» διαβάστηκε ως στόχος αντί για οροφή.** Ο κανόνας #5 έχει
δύο μισά: γραμμή ≤44px **και** ≥20 γραμμές ορατές. Στα 44 χωρούσαν 18. Το ένα
μισό του κανόνα ακυρώνει το άλλο αν διαβαστεί μεμονωμένα.

**Ελάττωμα του ίδιου του κριτή:** ο εξαγωγέας συμβολαίων μαζεύει το κέλυφος της
εφαρμογής ως «ενέργειες οθόνης». Στο `entity.json`, 9 από τις 10 ενέργειες είναι
sidebar/topbar (`Briefing`, `Sign out`, `Today`, `◀`). Άρα ο κριτής #1 φυλάει
πράγματα που δεν ανήκουν στην οθόνη — και θα κοκκινίσει αν αλλάξει το μενού.

**Ξεπερασμένο έγγραφο:** το `CLAUDE.md` έλεγε ότι το `/costs/*` γυρίζει 404.
Μετρήθηκε: **401**. Είναι ζωντανό. Διορθώθηκε.

**Τρία πεδία που δεν γράφονται ποτέ** (βλ. spec κύματος 1 §4): `contact_person`
σε πελάτες και συνεργάτες, `payment_terms_days` σε πελάτες, μισθός σε οδηγούς.
Η φόρμα τα δέχεται, ο Worker δεν τα έχει, το toast λέει επιτυχία.

### 2026-08-29 — Ο κριτής έπεσε από το ίδιο μας το deploy (ΕΚΛΕΙΣΕ αυθημερόν)

Μετά το push του accent και των tokens, οι ζωντανοί έλεγχοι πήγαν από **31/33 σε
24/33**: εννέα συμβόλαια απέτυχαν ταυτόχρονα. Δεν ήταν υποχώρηση οθόνης — και τα
εννέα με το ίδιο μήνυμα, `net::ERR_FAILED` στον κριτή #6.

**Πρώτη αιτία, επιβεβαιωμένη:** το `routeFromHAR` κάλυπτε `**/*`, δηλαδή **και τα
στατικά αρχεία**. Το `bump-versions.sh` αλλάζει το `?v=` σε 41 URL· το HAR έχει
τις παλιές εκδόσεις, το `notFound:'abort'` τα κόβει, καμία σελίδα δεν φορτώνει.
Μετρήθηκε: το HAR έχει 27 εκδόσεις, καμία η τρέχουσα `1787950284`.

**Ο οραματιστής πέθαινε ακριβώς μετά από κάθε release** — τότε που χρειάζεται.

**Διόρθωση που έγινε:** το HAR καλύπτει πλέον **μόνο** τις κλήσεις προς τον
Worker· τα στατικά φορτώνουν ζωντανά. Σωστό ούτως ή άλλως: έτσι ελέγχεται ο
πραγματικά deployed κώδικας, όχι μια επανάληψη.

Μόνο του **δεν έλυσε το πρόβλημα** — παρέμεναν 9 αποτυχίες με `net::ERR_FAILED`.

**Δεύτερη αιτία, επιβεβαιωμένη με probe (`page.on('requestfailed')`):** το
αίτημα που ακυρωνόταν ήταν του `core/ai-chat.js:1319`, που βάζει το
`localToday()` μέσα στο URL: `IS_AFTER({Delivery DateTime},'2026-08-29')`. Το
HAR ηχογραφήθηκε 28/8 με `'2026-08-28'`. Το `routeFromHAR` ταιριάζει URL **κατά
λέξη**, οπότε κάθε αίτημα που κουβαλά «σήμερα» σταματά να ταιριάζει **την
επόμενη μέρα** — και τα εβδομαδιαία παράθυρα την επόμενη εβδομάδα. Ωρολογιακή
βόμβα ημερολογίου, όχι υποχώρηση οθόνης. Χειρότερο: ο κριτής #6 αποτυγχάνει
**πριν** τη σύγκριση πεδίων, άρα όσο κρατούσε ο θόρυβος η σκληρή πύλη tier-1
δεν εκτελούνταν καθόλου.

**Τρίτη αιτία — βρέθηκε ακολουθώντας την οδηγία του ίδιου του πλάνου** («τρέξε
με `PW_BASE_URL` σε τοπικό server για να δεις τη δική σου αλλαγή»): οι
ηχογραφημένες αποκρίσεις κουβαλούν `Access-Control-Allow-Origin:
https://dimitrispetras21-del.github.io`. Αναπαραγόμενες αυτούσιες σε σελίδα από
`127.0.0.1`, ο browser μπλόκαρε **κάθε** backend απόκριση — η τοπική επαλήθευση
δεν είχε δουλέψει ποτέ· η οδηγία του πλάνου ήταν άγραφη υπόσχεση.

**Διόρθωση (και για τις δύο):** το `routeFromHAR` αντικαταστάθηκε στο
`tests/critics/auth.js` από δικό μας replay για το host του Worker: ακριβές
ταίριασμα URL → ταίριασμα με τα `YYYY-MM-DD` κανονικοποιημένα → θορυβώδης
ματαίωση (η παλιά σημασιολογία `notFound:'abort'` διατηρείται). Το
`Access-Control-Allow-Origin` απηχεί το origin της σελίδας που ρωτά, οπότε το
ίδιο rig δουλεύει και στο deployed και σε τοπικό δέντρο.

**Μέτρηση μετά τη διόρθωση:** 12/12 στατικά, **31/33 ζωντανά · 2 γνωστό κενό**
(weekly_* ENOENT) — η βάση του πλάνου αποκαταστάθηκε, χωρίς καμία χαλάρωση
baseline.

**Παρ' ολίγον διαφυγή:** στην πρώτη έκδοση της διόρθωσης οι συναρτήσεις
γράφτηκαν αλλά η κλήση τους ΔΕΝ μπήκε ποτέ στο `preparePage` — νεκρός κώδικας
που «έμοιαζε» λύση. Το έπιασε μόνο η επανάληψη της μέτρησης (24/33 ξανά), όχι η
ανάγνωση του diff. Αρχή 2: η απόδειξη είναι ο πίνακας, ποτέ η οθόνη.

**Μάθημα:** ο κριτής εξαρτιόταν από παγωμένο αντίγραφο ενός συστήματος που
αλλάζει σε κάθε deploy **και** σε κάθε γύρισμα ημερολογίου. Ένας φρουρός που
σπάει όταν αλλάζει αυτό που φυλάει, δεν φυλάει.

**Δεύτερο μάθημα — τυφλή υπογραφή:** και οι ΤΡΕΙΣ μηχανισμοί παρήγαγαν το
ταυτόσημο «Failed to load resource: net::ERR_FAILED». Ο κριτής #6 δεν ξεχωρίζει
cache-bust από ημερομηνία από CORS — και μια ανεκτή γενική υπογραφή στο
baseline **κρύβει** νέα αποτυχία σε άλλο endpoint: έτσι «πέρασαν» pallets/audit
ενώ είχαν το ίδιο πρόβλημα με τις άλλες εννέα. Ο τελικός έλεγχος της 28/8 το
είχε σημάνει ως Important· σήμερα συνέβη, και έστειλε την πρώτη διάγνωση σε
λάθος δρόμο. Κατεύθυνση διόρθωσης: υπογραφή από το `page.on('requestfailed')`
με το URL (κανονικοποιημένο), όχι από το κείμενο κονσόλας. Θέλει και
επανακαταγραφή των error-baselines (νέα μορφή υπογραφής) — ξεχωριστή εργασία,
όχι μέσα στο κύμα 1.

### 2026-08-29 — Κύμα 1, βήμα 1: λίστα trucks (v2)

Υλοποίηση κατά το Figma `truck-overview` 103:290, πίσω από flag `v2: true`
**μόνο στα trucks** — οι άλλες πέντε οθόνες μένουν στο παλιό μονοπάτι μέχρι τη
δική τους σειρά (μία οθόνη τη φορά). Μετρήθηκε σε τοπικό δέντρο, 1920×1080:
**21 γραμμές ορατές** (όριο ≥20), ύψος γραμμής **40px**, **0 κελιά με
ellipsis**, κεφαλίδες στηλών ταυτόσημες με το συμβόλαιο, **0 σφάλματα
κονσόλας**, προ-επιλεγμένο «Κατάσταση: Ενεργός» όπως στο mock.

Συνειδητές αποκλίσεις από το mock (και γιατί):
1. Η μονάδα **kg** μένει δίπλα στο Απόβαρο — ρητή απόφαση owner 6-8-2026·
   υπερισχύει mock που δεν έχει περάσει ακόμα από ανασκόπηση owner.
2. Το placeholder της αναζήτησης κρατά το αναλυτικό hint («πινακίδα, VIN,
   μάρκα…») αντί του γενικού «Αναζήτηση…» — υπάρχουσα, πιο χρήσιμη συμπεριφορά.
3. Δεδομένα γραμμών σε 12px (`--text-sm`), όπως ρητά ορίζει το mock — το
   DESIGN.md ΜΕΡΟΣ Γ δίνει τον ρόλο «δεδομένα πίνακα» στα 13px. Και τα δύο
   είναι εντός κλίμακας· κρίθηκε από την πυκνότητα (ο κανόνας #5 μετρήθηκε στα
   12). Αν η ανασκόπηση owner προτιμήσει 13, είναι αλλαγή ενός token στο CSS.

### 2026-08-29 — Κύμα 1, βήμα 2: καρτέλα trucks

Πέντε ενότητες κατά το mock: κεφαλίδα (πινακίδα + badge + μάρκα/μοντέλο) ·
Χιλιόμετρα · Έγγραφα · Στοιχεία οχήματος · Round trips · Ζημιές & επισκευές.
Στιγμιαίο μισό από την εγγραφή· ασύγχρονο μισό (χλμ, RT, ζημιές) με ανεξάρτητη
φόρτωση ΑΝΑ ενότητα και ρητή κατάσταση αποτυχίας (κανόνας #7) — μετρήθηκαν και
το happy path (probe με fixtures) και οι αποτυχίες: καμία κενή καρτέλα, μηδέν
σφάλματα στο συμβόλαιο (τα fetch της καρτέλας τρέχουν μόνο σε κλικ, που ο
scraper δεν κάνει).

Πηγές, ελεγμένες μέχρι τον πίνακα πριν γραφτεί κώδικας:
- Χιλιόμετρα: τελευταίο `Odometer km` του maint_history (72/1091 εγγραφές
  έχουν ένδειξη — η απουσία λέγεται ρητά, κανόνας #3).
- Round trips: `GET /costs/rt?truck_id=` μέσω `/costs/lookups` (ταίριασμα
  πινακίδας με normalizePlate). Χωρίς χρηματικά πεδία στην απόκριση — ασφαλές
  για κάθε ρόλο που διαβάζει. 403 (management/warehouse) = δική του κατάσταση
  «χωρίς πρόσβαση», όχι «σφάλμα».
- Ζημιές: maint_history με σύνδεσμο Truck ή πινακίδα (660 εγγραφές με φορτηγό,
  όλες με κόστος).
- «ΣΕ ΣΕΡΒΙΣ»: maint_req με Status «In Progress» στην ίδια πινακίδα.

Τι ΔΕΝ χτίστηκε από το mock, με μέτρηση:
1. **Μπάρα «επόμενο σέρβις»** — το `next_service_km` είναι **0/1091**: κανείς
   δεν το έχει καταχωρήσει ποτέ. Σχεδίαση χωρίς δεδομένα· θέλει απόφαση owner
   (να το γράφει η φόρμα σέρβις, ή να φύγει από την καρτέλα).
2. **Διαδρομή στα round trips** («Βέροια → Μιλάνο») — το `/costs/rt` γυρίζει
   ids σκελών, όχι ονόματα τοποθεσιών· θα χρειαζόταν joins που το endpoint δεν
   σερβίρει. Δείχνονται κωδικός/ημερομηνίες/χλμ/κατάσταση.

Πραγματικό εύρημα κατά την επαλήθευση: το IAB4166 εμφανίζει «ΚΕΚ — έληξε πριν
13 ημέρες» με σημερινά δεδομένα παραγωγής. Η καρτέλα κάνει ήδη τη δουλειά της.

### 2026-08-29 — Κύμα 1, βήμα 3: φόρμα trucks

Κατά το mock `truck-form` 119:345: τέσσερις ενότητες (Ταυτότητα · Τεχνικά ·
Έγγραφα · Σημειώσεις — ίδια 12 πεδία, μόνο ανα-ομαδοποίηση), inline validation
αντί για alert(), κουμπί Αποθήκευση σβηστό όσο υπάρχει σφάλμα με υποσημείωση
που εξηγεί το γιατί, ελληνικό λεξιλόγιο. Μόνο ο κανόνας που ορίζει το mock
(αρνητικό απόβαρο) — τίποτα εφευρεμένο· ο μηχανισμός `validators` ανά πεδίο
είναι έτοιμος για τους κανόνες των επόμενων φορμών. Μετρήθηκε με probe:
σφάλμα → μήνυμα + κόκκινο πλαίσιο + σβηστή Αποθήκευση· διόρθωση → όλα
καθαρίζουν· μηδέν σφάλματα κονσόλας.

**Διαφυγή (προϋπάρχουσα, σε παραγωγή): «undefined» επικεφαλίδες ενοτήτων σε
ΟΛΕΣ τις φόρμες.** Το buildEntityModal διάβαζε `sec.title` ενώ κάθε
ENTITY_CONFIG ορίζει `section:` — κάθε modal και των έξι οντοτήτων έδειχνε
«undefined» πάνω από κάθε ενότητα, από την πρώτη μέρα. Το έπιασε το probe της
φόρμας· κανείς δεν το είχε αναφέρει. Διορθώθηκε για όλες τις οντότητες
(`sec.section || sec.title`).

**Παρατήρηση (προϋπάρχουσα, ΔΕΝ διορθώθηκε εδώ):** το saveEntityRecord
παραλείπει τα κενά πεδία (`if (!val) continue`) — άρα ΑΔΕΙΑΣΜΑ τιμής δεν
αποθηκεύεται ποτέ: ό,τι γράφτηκε μία φορά δεν καθαρίζει από τη φόρμα. Ισχύει
και στις έξι οντότητες, θέλει απόφαση (PATCH με null στο facade) — εκτός
εμβέλειας του κύματος 1.

### 2026-08-29 — Κύμα 1, βήματα 4–6: trailers · drivers · workshops · clients · partners

Οι υπόλοιπες πέντε οντότητες πέρασαν στο v2 με το ίδιο engine — οι διαφορές
είναι config ανά οντότητα (ενότητες κάρτας υπό flags, badges, validators,
νεκρά πεδία). Μετρήθηκαν όλες με το probe σε 1080p: 40px γραμμή, ≥20 ορατές
(trailers/workshops ακριβώς 20), 0 κελιά με κοπή, κεφαλίδες συμβολαίου
ταυτόσημες, 0 σφάλματα κονσόλας στη διαδρομή του συμβολαίου.

Τα τρία νεκρά πεδία υλοποιήθηκαν όπως όριζε το πλάνο §2.1, και στις δύο
πλευρές της παγίδας: στη λίστα/κάρτα δείχνουν «—»/«Δεν έχει καταχωρηθεί», στη
φόρμα είναι απενεργοποιημένα με την αιτία γραμμένη («Δεν αποθηκεύεται —
εκκρεμεί στήλη στη βάση») ΚΑΙ παραλείπονται στο save — το facade δεν παίρνει
ποτέ την τιμή που θα πετούσε με «επιτυχία».

Ουσιαστικές αλλαγές πέρα από τη διάταξη:
1. **Το P&L έφυγε από μπροστά στον dispatcher στους πελάτες/συνεργάτες.** Τα
   παλιά stats strips έδειχναν «Τρέχων Μήνας €», Top-3 πελάτες/συνεργάτες με
   ποσά, και το παλιό panel συνεργάτη Avg Margin/Revenue — σε οθόνες όπου ο
   dispatcher έχει full πρόσβαση (clients perm). Οι v2 κάρτες δείχνουν ΜΟΝΟ
   όγκο/συχνότητα (παραγγελίες, τελευταία, ανά μήνα · αναθέσεις, ενεργές) —
   κατά το κλείδωμα 23/8 και τα mocks.
2. **Workshops:** το enrichment (Εργασίες/Δαπάνη/Τελευταία) αποσπάστηκε από
   το strip· συνεργείο χωρίς εργασίες μένει «—», ποτέ 0/€0 — τα 4
   unknown-as-zero κελιά της μέτρησης 28/8 πεθαίνουν εδώ.
3. **Ζωντανά ευρήματα στην πρώτη κιόλας δοκιμή:** η πρώτη ρυμούλκα της λίστας
   δείχνει «ΚΤΕΟ λήγει σε 29 ημέρες» και «ATP/FRC — δεν έχει καταχωρηθεί» —
   η κατηγορία του περιστατικού ATP, τώρα ορατή στην καρτέλα.

Συνειδητές αποκλίσεις από τα mocks (τεκμηρίωση για την ανασκόπηση owner):
- **Χώρες ως έχουν** («GR», «GREECE», «ΕΛΛΑΔΑ») — το mock δείχνει «Ελλάδα»/
  «Αυστρία»· χάρτης εμφάνισης χωρών δεν υπάρχει και δεν εφευρέθηκε. Τα δεδομένα
  έχουν 3+ γραφές ανά χώρα (CL-1) — θέλει δική του απόφαση/κανονικοποίηση.
- **Χωρίς route text στα round trips** (το /costs/rt γυρίζει ids σκελών) και
  **χωρίς «% εμπρόθεσμες» στον συνεργάτη** (τα PARTNER ASSIGNMENTS δεν έχουν
  πεδίο επίδοσης) — δείχνονται κωδικός/ημερομηνίες/χλμ/πινακίδα και
  σύνολο/τελευταία/ενεργές αντίστοιχα.
- **Ο δείκτης «τον μήνα»** του πελάτη ορίστηκε ως σύνολο ÷ μήνες μεταξύ πρώτης
  και τελευταίας παραγγελίας (ελάχιστο 1 μήνας) — ο ορισμός γραμμένος εδώ για
  να μη μαντεύεται.
- **Το παλιό μονοπάτι (page-header/strips/panel/history) ΔΕΝ αφαιρέθηκε
  ακόμη**: κάθε οντότητα γυρίζει πίσω με αφαίρεση ενός `v2: true` — σκόπιμος
  μηχανισμός αναστροφής όσο εκκρεμεί η ανασκόπηση owner. Μετά την αποδοχή του
  κύματος, το νεκρό μονοπάτι φεύγει (κανόνας #8) — να μπει στα Εκκρεμή.

Κριτές κατά την ολοκλήρωση — τρία ευρήματα του rig, όλα κλεισμένα:
1. **Σκόπιμη αλλαγή συμβολαίου entity:** τα `GET NAT_ORDERS`/`GET
   PARTNER_ASSIGN` έφυγαν από το load (τα έκαναν τα strips που διέρρεαν P&L·
   πλέον φορτώνουν στο κλικ της καρτέλας). Έγινε αποδεκτή με στοχευμένο
   CAPTURE μόνο του entity — τα 24 πεδία αμετάβλητα.
2. **Το CAPTURE φωτογράφισε θόρυβο κελύφους** («Open»/«Snooze» του briefing
   widget + το NL fetch του), που δεν αποδίδεται ντετερμινιστικά — στον
   επόμενο έλεγχο διαβάστηκε «ΧΑΘΗΚΑΝ ενέργειες». Το προδιαγεγραμμένο ελάττωμα
   του exporter (φυλάει το κέλυφος) δάγκωσε στην πράξη· αφαιρέθηκαν από το
   συμβόλαιο με το χέρι, τεκμηριωμένα.
3. **Διπλότυπα entries στο HAR με άδειο σώμα:** το local_moves έχει 404 και
   με JSON σώμα και άδεια. Το άδειο δίδυμο έκρυβε τα `[TMS ERROR] Table not
   available…` που η ΖΩΝΤΑΝΗ εφαρμογή παράγει σε κάθε άνοιγμα του Weekly
   National — το replay πλέον προτιμά entry με σώμα, και οι δύο προϋπάρχουσες
   υπογραφές μπήκαν στο error-baseline (διεύρυνση προς την αλήθεια της
   παραγωγής, όχι κάλυψη νέας υποχώρησης).

Τελική μέτρηση, τοπικό δέντρο με όλα τα v2: **12/12 στατικά (entity hex 0) ·
31/33 ζωντανά · 2 γνωστό κενό** — ίδια βάση με την εκκίνηση του κύματος, με
τη σκληρή πύλη tier-1 να περνά και στις έξι οθόνες.

## 2026-09-02 · Δύο αναποδογυρίσματα σε τρία λεπτά — απόδοση του «Export CSV»

**Τι έγινε.** Μετά το re-capture, το `maintenance.json` πήρε νέα ενέργεια
«Export CSV». Την απέδωσα στις οθόνες Ιστορικού (σωστά), αλλά **έδεσα την
απόδειξη με το commit στην ίδια εντολή** — το μήνυμα γράφτηκε πριν διαβαστεί
το grep. Το grep, περιορισμένο με `awk` στο σώμα των `renderTrucksHistory`/
`renderTrailersHistory`, γύρισε κενό. Έκανα amend δηλώνοντας τον ισχυρισμό
«αβάσιμο». Ένα πλήρες grep έδειξε ότι το κουμπί ζει στην `_historyPaint`, δύο
κλήσεις βαθύτερα (`renderTrucksHistory → _renderHistory → _historyPaint`,
`modules/maintenance.js:1203`). Ο αρχικός ισχυρισμός ήταν σωστός· η
«διόρθωση» ήταν το λάθος. Commit `74b756e` κρατά το λανθασμένο amend· αυτή η
εγγραφή είναι η διόρθωση — δεν ξαναγράφεται ιστορικό δεύτερη φορά.

**Δύο κανόνες που βγαίνουν.**
1. **Η απόδειξη διαβάζεται πριν γραφτεί ο ισχυρισμός** — ποτέ στην ίδια
   εντολή με το commit. Κόστος: ένα επιπλέον round-trip. Κόστος του
   αντίθετου: ψεύτικο γεγονός σε μόνιμο αρχείο.
2. **Ένα grep περιορισμένο σε συνάρτηση αποδεικνύει μόνο απουσία *εκεί*.**
   Το «δεν βρέθηκε στο Χ» δεν είναι «δεν υπάρχει». Πριν δηλωθεί απουσία,
   ψάξε ολόκληρο το αρχείο και τις κλήσεις.

**Το ουσιαστικό συμπέρασμα παραμένει:** οι στήλες των οθονών Ιστορικού είναι
εκτός σκληρής πύλης επειδή ο πίνακας εμφανίζεται μόνο μετά από επιλογή
οχήματος — ο εξαγωγέας πρέπει να επιλέγει όχημα σε αυτές τις διαδρομές.

## 2026-09-03 · Το συμβόλαιο βαθμίδας 1 παγώνει ΚΕΙΜΕΝΑ, όχι ΙΚΑΝΟΤΗΤΕΣ

**Τι βρέθηκε** (συνεδρία `locations`, πριν γράψει κώδικα — σωστή στιγμή).
Το `contracts/locations.json` κρατά strings: «Name ↕», «+ New Location», τα
κουμπιά-μπάρες της παλιάς Επισκόπησης. Το εγκεκριμένο Figma είναι ελληνικό
και list-first. Πιστή υλοποίηση = ο κριτής #1 μετρά ~5 «χαμένα πεδία» και
~25 «χαμένες ενέργειες» **κατά κείμενο**, μηδέν **κατά ικανότητα**.

Είναι το ίδιο πρόβλημα με το κύμα 1 (29/8), τώρα ονομασμένο: **ο εξαγωγέας
συγκρίνει ό,τι μπορεί να διαβάσει (κείμενο), όχι ό,τι θέλουμε να φυλάξουμε
(ικανότητα).** Κάθε εξελληνισμός tier-1 μονάδας θα κοκκινίζει τον #1 εκ
κατασκευής — ψευδής συναγερμός που, αν επαναληφθεί, θα αγνοηθεί.

**Απόφαση ολοκληρωτή (ισχύει για όλη την 1η παρτίδα και μετά):**
1. Πιστή υλοποίηση του Figma. **Ποτέ** διατήρηση αγγλικών κειμένων για να
   μείνει ο κριτής πράσινος — αυτό είναι «κρατάω το λάθος για να μη σκάσει
   το μέτρο».
2. Κάθε ικανότητα του συμβολαίου διατηρείται **και είναι προσβάσιμη** κάπου
   στην οθόνη (π.χ. το 🗑️ που λείπει από την καρτέλα ζει στη γραμμή).
3. Ο agent παραδίδει **πίνακα πριν→μετά** ανά γραμμή συμβολαίου:
   παλιό κείμενο → νέο → «ίδια ικανότητα» | «ΧΑΘΗΚΕ». Γραμμή ΧΑΘΗΚΕ = δεν
   παραδίδει.
4. Ο #1 είναι κόκκινος στο worktree — **αναμενόμενο και δηλωμένο**. Οι
   υπόλοιποι πέντε + πλάτος πράσινοι.
5. Το re-capture το κάνει **μόνο ο ολοκληρωτής**, στο merge, αφού ελέγξει
   τον πίνακα γραμμή-γραμμή. Έτσι είναι απόφαση με απόδειξη, όχι χαλάρωση.

**Τι πρέπει να αλλάξει στο εργαλείο (μετά την παρτίδα, όχι τώρα):** το
συμβόλαιο να φυλάει **σταθερά κλειδιά ικανότητας** (π.χ. `data-field`,
`data-action`, ονόματα endpoints) και όχι το ορατό κείμενο. Ο εξαγωγέας ήδη
διαβάζει `[data-field]`/`[data-action]` — απλώς τα modules δεν τα έχουν.
Όταν τα αποκτήσουν, ο #1 παύει να είναι ευαίσθητος στη γλώσσα.

## 2026-09-03 · Οι κριτές δεν είχαν δει ΠΟΤΕ το audit_trail — και έλεγαν «πράσινο»

**Μετρημένο** (όχι υποθετικό — προηγήθηκαν τρεις λάθος υποθέσεις, βλ. τέλος):
```
πριν   audit_trail   th=0  rows=0   text=207B  «No session token. Sign in again.»
μετά   audit_trail   th=6  rows=200 text=20740B
```
Η ψεύτικη συνεδρία του harness έσπερνε `tms_user` (για τον φρουρό ρόλων) αλλά
**όχι `tms_jwt`**. Το `modules/audit_trail.js:59-60` ελέγχει την ύπαρξη token
πριν καλέσει το `/audit` και σταματά στο μήνυμα. Το HAR είχε την απάντηση
(200, 385 KB, 200 entries) και **δεν ρωτήθηκε ποτέ**.

Τρία κενά, το ένα πάνω στο άλλο:
1. Το harness δεν έδινε token → η οθόνη δεν απέδιδε.
2. Η «απόδειξη απόδοσης» (`assertRendered`) δεχόταν τα 207 bytes του μηνύματος
   σφάλματος ως περιεχόμενο → «απέδωσε».
3. Το fan-in του contract.spec (ένα Set πεδίων για όλες τις διαδρομές της
   μονάδας) → το `audit.json` γέμισε με τα 8 πεδία του `metrics_audit` και
   κανείς δεν είδε ότι το `audit_trail` δεν συνεισέφερε τίποτα.
**Μια οθόνη βαθμίδας 1 ήταν αόρατη στη σουίτα από την πρώτη μέρα, με τη σουίτα
πράσινη.** Βρέθηκε επειδή η συνεδρία `audit` διάβασε το συμβόλαιό της ΠΡΙΝ
γράψει κώδικα και είδε ότι δεν περιείχε καμία δική της στήλη.

**Διορθώσεις (ελάχιστες):** `auth.js` σπέρνει `tms_jwt` με εμφανώς ψεύτικη
τιμή· `assertRendered` απορρίπτει τις δύο υπογραφές auth που ο κώδικας όντως
γράφει. Παρενέργεια: το `metrics_audit` έδειξε 3 ακόμη δομικά μηδενικά
(`0/87`, `0/28 busy`, `€0 All-time` — αληθινά, με παρονομαστή) που πριν δεν
έφταναν να υπολογιστούν. Καταγράφηκαν ως γνωστό χρέος με ρητό
`CRITICS_WIDEN=1` (5→8), όχι ως υποχώρηση.

**Τρεις λάθος υποθέσεις πριν τη μέτρηση, όλες δικές μου, ίδια μέρα:**
(1) «το HAR γύρισε 403/κενό» — όχι, 200/200 entries· (2) «δεν υπάρχουν `<th>`»
— `grep -c` μετρά ΓΡΑΜΜΕΣ, τα 6 `<th>` ήταν σε μία· (3) στάλθηκε ως
«μετρημένο» πριν μετρηθεί. Κανόνας που βγαίνει, τρίτη φορά σήμερα: **η
απόδειξη διαβάζεται ΠΡΙΝ γραφτεί ο ισχυρισμός, και ένα grep λέει μόνο ό,τι
ρωτήθηκε.** Ο ανιχνευτής 20 γραμμών που έτρεξε τον ίδιο εξαγωγέα κόστισε 30
δευτερόλεπτα και έδωσε τη σωστή απάντηση με την πρώτη.

## 2026-09-03 · Η σουίτα «επαλήθευσε» την παραγωγή — δεύτερη φορά

**Τι έγινε.** Στο σημείο push Α έτρεξα `npm run critics` χωρίς `PW_BASE_URL`.
Το `playwright.config.js` πέφτει τότε στο deployed GitHub Pages. Δύο
συμβόλαια (locations, pallets) βγήκαν «κόκκινα — χάθηκαν πεδία»: ο κριτής
κοίταζε τον ΠΑΛΙΟ κώδικα («+ New Location», «Export CSV») ενώ ο τοπικός
server σέρβιρε τον νέο. Το ίδιο είχε συμβεί 29/8 (σχέδιο §3, βήμα 4).

**Πώς βρέθηκε.** Όχι από τη σουίτα — από τα screenshots της αποτυχίας, που
έδειχναν οθόνη που δεν υπάρχει πλέον στο repo. Το `curl` προς το 8788 και
ένα probe με το ίδιο harness έδειχναν τον νέο κώδικα· άρα η διαφορά ήταν ο
στόχος, όχι ο κώδικας. Δύο λάθος υποθέσεις πριν (ανταγωνισμός CPU, χρόνος
απόδοσης) — αρχή «η απόδειξη είναι η μέτρηση, όχι η υπόθεση».

**Τι άλλαξε.** Το `run.js` αρνείται πλέον να τρέξει χωρίς `PW_BASE_URL`
(exit 2) εκτός αν ζητηθεί ρητά `CRITICS_TARGET=deployed`, και τυπώνει τον
στόχο στην πρώτη γραμμή. Κανόνας: **κάθε αναφορά σουίτας γράφει τον στόχο
της**. Αναφορές του παρελθόντος χωρίς στόχο δεν αποδεικνύουν τίποτα για τον
τοπικό κώδικα.

## 2026-09-03 · Ο κριτής kanban μετρούσε τόνο, όχι ημέρα — λάθος ισχυρισμός για τον παλιό weekly_natl

**Τι ισχυρίστηκα.** Ότι ο παλιός weekly_natl «δεν δείχνει τις 7 μέρες» (κριτής
kanban, σημείο 2). Γράφτηκε και σε commit message (fd8230d).

**Τι ίσχυε.** Η JS κρατά τον τόνο στο `toUpperCase()` («Κυριακή» →
«ΚΥΡΙΑΚΉ»), ενώ ο κριτής έψαχνε «ΚΥΡΙΑΚΗ». Οι μέρες ΥΠΗΡΧΑΝ. Το βρήκε ο agent
του weekly_natl δουλεύοντας ενάντια στον κριτή. Μετά από NFD + αφαίρεση
διακριτικών, ο παλιός natl περνά το σημείο 2 και πέφτει στο σημείο 3
(«3 κάθοδος · 0 άνοδος · 0% ολοκληρωμένο», «0%», «0 →0%») — αυτή είναι η
πραγματική του έλλειψη, ίδια με του intl.

**Μάθημα.** Κριτής που γεννιέται σήμερα και «αποδεικνύεται» στον παλιό
κώδικα πρέπει να αποδεικνύεται και στην ΑΝΤΙΘΕΤΗ κατεύθυνση: ένα ψευδώς
κόκκινο σε κάτι που ξέρουμε ότι υπάρχει. Έλεγξα μόνο ότι κοκκινίζει, όχι
ότι κοκκινίζει για τον σωστό λόγο.
