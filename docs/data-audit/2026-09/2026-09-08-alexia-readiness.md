# Έλεγχος ετοιμότητας — πρώτη μέρα accountant (8/9/2026)

Συντονισμός: Claude Fable 5.1 · μετρήσεις: 2 subagents (Haiku δικαιώματα, Sonnet χάρτης
πεδίων) · ημερομηνία ελέγχου: 7/9/2026 16:00–17:00 · βάση: SELECT μόνο.

Ερώτηση του owner: «οτιδήποτε θα ξεκινήσει να κάνει [η accountant] να λειτουργεί σωστά».
Η accountant έχει πλήρη δικαιώματα σε Τιμολόγηση, Ισοζύγιο Παλετών, Μισθοδοσία Οδηγών,
Πελάτες / Συνεργάτες / Οδηγοί. Όλα τα υπόλοιπα μόνο ανάγνωση ή κρυφά.

## 1. Τι ελέγχθηκε και πώς

| Έλεγχος | Μέθοδος | Αποτέλεσμα |
|---|---|---|
| Worker παραγωγής = repo για τον ρόλο | κατέβασμα bundle από CF API, ξετύλιγμα multipart, σύγκριση PERMISSIONS / COSTS_PERMS / PL_PERMS | ταυτόσημα |
| Φρουρός των τριών στο ζωντανό bundle | grep στο bundle | 3/3 (order_stops DELETE dispatcher, VS CD Date, VAT Number + Legal Name) |
| Λογαριασμός | SELECT users | `alexia` ρόλος accountant, active· `demo_accountant` active=false |
| Κάθε ενέργεια των σελίδων της → endpoint → δικαίωμα | 24 ενέργειες, front PERMS ↔ Worker | 22 ΟΚ · **2 θα γύριζαν 403** (βλ. §3) |
| Κάθε label που γράφει η οθόνη υπάρχει στον χάρτη TABLES | invoicing / payroll / pallet_ledger / entity ↔ Worker map | **0 σιωπηλές απορρίψεις** |
| Στήλες «γραμμένες στη βάση» | SELECT count | βλ. §2 |
| Ζωντανός έλεγχος ως accountant | claude-in-chrome | **ΔΕΝ ΕΓΙΝΕ** — δεν δόθηκε καρτέλα accountant / παραγγελία-δοκιμή, και ο classifier μπλόκαρε το browser tool (§5) |

## 2. Μετρήσεις (7/9/2026 16:30)

| Πεδίο | Στήλη | Γραμμένα / σύνολο |
|---|---|---|
| Παραγγελίες: τιμολογήθηκε | `orders.invoiced` | 2 / 134 (21/8, από owner) |
| Παραγγελίες παραδομένες χωρίς τιμολόγιο | `status='Delivered' and not invoiced` | **107** — η ουρά της πρώτης εβδομάδας |
| Εθνικές παραγγελίες | `national_orders` | 4 (όλες status κενό, invoiced=false) |
| Πελάτες: υπεύθυνος / ημέρες πίστωσης | `clients.contact_person` / `payment_terms_days` | 1 / 1.920 · 0 / 1.920 |
| Συνεργάτες: υπεύθυνος | `partners.contact_person` | 0 / 432 |
| Μισθοδοσία: γραμμές | `dl_entries` | 11.127 (excel 11.078 · auto από RT 44 · manual 5) |
| Μισθοδοσία: auto χωρίς αξία | `source='auto' and trip_value is null` | 13 / 44 — αυτές περιμένουν καταχώρηση |
| Παλέτες: κινήσεις | `pl_movements` | 96 (pending 61 · confirmed 34 · reversed 1)· όλες οι εκκρεμείς είναι πελατών |

Η εγγραφή `contact_person` / `payment_terms_days` είναι πλέον χαρτογραφημένη (30/8)· τα
μηδενικά είναι «δεν συμπληρώθηκε», όχι «απορρίφθηκε». Απόδειξη εγγραφής υπάρχει μόνο για
`clients.contact_person` (1 γραμμή). Το `payment_terms_days` (integer) μένει αδοκίμαστο.

## 3. Τι βρέθηκε και τι διορθώθηκε

### 3.1 Worker — δύο 403 την πρώτη μέρα (διορθωμένο στο working tree, ΘΕΛΕΙ COMMIT + DEPLOY)
1. **Τιμολόγηση εθνικής παραγγελίας.** Η οθόνη κάνει PATCH σε `national_orders`. Ο accountant
   δεν είχε γραμμή για αυτόν τον πίνακα → wildcard `*: GET` → 403. Προστέθηκε
   `national_orders: ["GET","PATCH"]`.
2. **«Διαγραφή εκκρεμούς» στο Ισοζύγιο Παλετών.** Το κουμπί εμφανίζεται σε κάθε εκκρεμή
   κίνηση (61 σήμερα) και στέλνει DELETE· ο accountant είχε GET/POST/PATCH. Προστέθηκε DELETE.
   Ασφαλές: ο handler αρνείται (409) ό,τι δεν είναι pending — οι οριστικές μόνο αντιστρέφονται.

Το diff είναι 2 γραμμές κώδικα + σχόλια στο `worker/src/index.js` (PERMISSIONS.accountant,
PL_PERMS.accountant). Ο classifier του agent μπλόκαρε το commit· ο owner κάνει commit και
deploy μετά τις 15:00 με φρουρό των τριών πριν/μετά.

### 3.2 Front — `Status='Invoiced'` (διορθωμένο, commit 815422e)
Η «Σήμανση ως τιμολογημένη» και η μαζική τιμολόγηση έγραφαν `Status='Invoiced'` μαζί με το
checkbox. Αντίθετο με την κλειδωμένη απόφαση 23/8 («Invoiced δεν είναι status») και με τη
migration 014 (CHECK στο λεξιλόγιο status, γραμμένη, **δεν έχει εκτελεστεί** — επιβεβαιώθηκε
7/9: κανένα CHECK στο `orders.status`). Την ημέρα που τρέξει η 014 κάθε τιμολόγιο θα
απέρριπτε. Τώρα γράφονται μόνο `Invoiced`, `Invoice Number`, `Invoice Date`· η αναγνώριση
τιμολογημένης (`_invIsInvoiced`) ήδη διάβαζε και το checkbox.

Παρενέργεια που ΔΕΝ αλλάχθηκε: το dropdown Status στη φόρμα διεθνούς παραγγελίας
(`orders_intl.js:483`) εξακολουθεί να προσφέρει «Τιμολογήθηκε». Απόφαση owner αν φεύγει.

### 3.3 Σχόλια που έλεγαν ψέματα (διορθωμένο, commit 920abca)
`core/entity.js` περιέγραφε τα Contact Person / Payment Terms Days ως «νεκρά, δεν
αποστέλλονται», ενώ από 30/8 γράφονται. Ενημερώθηκαν με τις μετρήσεις.

### 3.4 Βρέθηκε, δεν διορθώθηκε (μικρά)
- Τιμολόγηση: «Εβδομάδα» δείχνει «—» για κάθε εθνική παραγγελία (`Week Number` υπάρχει μόνο
  ως υπολογιζόμενο στα ORDERS). Χρειάζεται απόφαση αν οι εθνικές θέλουν εβδομάδα.
- Τιμολόγηση: νεκρά fallbacks σε ανύπαρκτα labels (`Client Name`, `Client Summary`,
  `Delivery Date`, `Total Price`, `Amount`, `Revenue`) — ποτέ δεν πυροδοτούν, θόρυβος.
- Μισθοδοσία: δεν καλεί `/costs/rt` (η σύνδεση RT↔ledger γίνεται από trigger). Το ledger
  endpoint απορρίπτει άγνωστο πεδίο με 400 — δεν έχει την παγίδα της σιωπηλής απόρριψης.

## 4. Τι δουλεύει επιβεβαιωμένα (στατικά) και τι όχι

**Δουλεύει (δικαίωμα + χάρτης πεδίων ΟΚ):** τιμολόγηση διεθνών (checkbox, αριθμός,
ημερομηνία) · δημιουργία / διόρθωση / επιβεβαίωση / αντιστροφή κίνησης παλετών · ανέβασμα
δελτίου · καταχώρηση αξίας / προκαταβολής / εξόδων / ακύρωσης στη μισθοδοσία · πλήρες CRUD
πελατών, συνεργατών, οδηγών.

**Δεν δουλεύει μέχρι το deploy του Worker:** τιμολόγηση εθνικής (4 παραγγελίες) · διαγραφή
εκκρεμούς κίνησης παλετών.

**Δεν αποδείχθηκε με εγγραφή:** καμία από τις παραπάνω — ο ζωντανός έλεγχος ως accountant
εκκρεμεί (§5). Ο κανόνας «η απόδειξη είναι ο πίνακας» δεν ικανοποιήθηκε ακόμη.

## 5. Τι χρειάζεται από τον owner

1. `git push origin main` — τα commits 815422e, 920abca είναι στο τοπικό main (ο classifier
   μπλόκαρε το push από τον agent).
2. Commit + deploy Worker (`worker/src/index.js`, 2 γραμμές δικαιωμάτων) μετά τις 15:00, φρουρός
   των τριών πριν/μετά, smoke: login + μία αποθήκευση.
3. Καρτέλα Chrome συνδεδεμένη ως accountant + μία παραγγελία-δοκιμή → ζωντανός έλεγχος
   (κονσόλα, κάθε φόρμα, μία εγγραφή ανά σελίδα, SELECT μετά).
4. Απόφαση: φεύγει η επιλογή «Τιμολογήθηκε» από το dropdown Status της διεθνούς παραγγελίας;
5. Απόφαση: θέλουν οι εθνικές «Εβδομάδα» στην Τιμολόγηση;

## 6. «Αν σπάσει, πώς θα το μάθω;»
- 403 σε ενέργεια της accountant → toast σφάλματος με το μήνυμα του Worker (όχι σιωπηλό) και
  εγγραφή στο Cloudflare observability (ενεργό από 7/9).
- Checkbox που «γράφτηκε» χωρίς να γραφτεί → ο επαναλαμβανόμενος έλεγχος του CLAUDE.md:
  `select count(*) filter (where invoiced) from orders` πρέπει να ανεβαίνει από το 2.
- Μισθοδοσία: `select count(*) from dl_entries where source='auto' and trip_value is null`
  πρέπει να πέφτει από το 13 όσο καταχωρεί.

## 7. Συμπλήρωμα (7/9 βράδυ → 8/9 00:30) — διόρθωση ρόλου, τυπική μέρα, νέα οθόνη

**Διόρθωση owner:** η Αλεξία **δεν** τιμολογεί. Δουλειά της: παλέτες + έξοδα (βήματα 1–9 της
τυπικής μέρας, επιβεβαιωμένα από τον owner). Οι §3.1(1)/§3.2 για την τιμολόγηση παραμένουν
σωστές ως διορθώσεις, απλώς δεν την αφορούν.

**Η τυπική της μέρα και τι δουλεύει:**
| # | Εργασία | Οθόνη | Κατάσταση |
|---|---|---|---|
| 1–4 | Εκκρεμείς κινήσεις παλετών, δελτία, επιβεβαίωση, αντιστροφή, υπόλοιπα | Ισοζύγιο Παλετών | ΟΚ (διαγραφή εκκρεμούς: μετά το deploy) |
| 5–7 | Αξία δρομολογίου / προκαταβολή / έξοδα ανά οδηγό, διόρθωση, υπόλοιπο | Μισθοδοσία Οδηγών | ΟΚ — πρώτη καταχώρηση του συστήματος |
| 8–9 | Καύσιμα, διόδια, DKV, AdBlue, φέρι, διαμονή, πρόστιμα, κόμιστρο συνεργάτη | **Έξοδα Δρομολογίων (ΝΕΑ, 7/9)** | POST ΟΚ τώρα· διόρθωση/διαγραφή/ανάθεση μετά το deploy |

**Παλέτες — μία πόρτα (commit cb741d0):** το «Δελτίο παλετών →» της καρτέλας παραγγελίας
αφαιρέθηκε: άνοιγε το OCR modal που γράφει σε νεκρό πίνακα (`pallet_ledger_*`, 0 γραμμές) και
σημαίες που είναι 0/138 στην παραγωγή. Η πύλη τιμολόγησης διαβάζει `pl_v_order_gate`
(επιβεβαιωμένες κινήσεις LOADING του Ισοζυγίου). Μέτρηση: `pl_movements.sheet_url` **0/96** —
κανένα δελτίο δεν έχει ανέβει ποτέ από το Ισοζύγιο· το πρώτο της είναι το πρώτο του συστήματος.

**Έξοδα Δρομολογίων (commit fd014eb, spec `docs/superpowers/specs/2026-09-07-trip-expenses-design.md`):**
`modules/expenses.js`, NAV «Οικονομικά» με perm `costs` (accountant/owner πλήρη, management
ανάγνωση, dispatcher κρυφό). Δεδομένα: `ct_round_trips` (48/50 δρομολόγια στο φίλτρο 60 ημερών),
`ct_cost_lines` (1 γραμμή σήμερα), 11 κατηγορίες ⊂ CHECK του πίνακα. Απόδειξη: 43 assertions
Playwright με captured bodies (POST καυσίμων με `liters/km_reading/station`, διοδίων χωρίς,
PATCH με `reason`, DELETE, αδιάθετη χωρίς `rt_id`, NAV ανά ρόλο, «300+» στο όριο του Worker,
χωρίς οριζόντιο scroll σε 1280/1440). Ελεγκτής read-only: κανένα key δεν πέφτει σιωπηλά
(POST/PATCH ⊂ `ctPick` ⊂ στήλες). Γνωστό όριο (απόφαση owner): ο Worker δεν επιβάλλει «δικές
της γραμμές μόνο» — κάθε accountant μπορεί να διορθώσει οποιαδήποτε γραμμή· η οθόνη μόνο κρύβει.

**Worker (commit cd137fb, ΘΕΛΕΙ DEPLOY):** accountant `national_orders GET/PATCH`, PL
`movements +DELETE`, COSTS `lines +PATCH/DELETE`· management `rt GET, lines GET`. Χωρίς
deploy: η Αλεξία καταχωρεί (POST) κανονικά αλλά κάθε Διόρθωση/Διαγραφή/Ανάθεση = 403 με
μήνυμα «Δεν έχεις δικαίωμα», και το management βλέπει κόκκινη κάρτα αντί για σελίδα.

**Δεν έγινε:** ζωντανός έλεγχος ως accountant (δεν δόθηκε καρτέλα/παραγγελία-δοκιμή· ο
classifier μπλόκαρε το browser tool). Πρώτη καταχώρηση + `select * from ct_cost_lines order by
id desc limit 1` = ο owner, μετά το deploy.
