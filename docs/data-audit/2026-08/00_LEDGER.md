# ΜΗΤΡΩΟ ΕΥΡΗΜΑΤΩΝ — Audit Αυγούστου 2026

**Κατάσταση 22/08/2026.** Όλα τα ευρήματα από τον έλεγχο δεδομένων (Claude) και
τον στατικό έλεγχο κώδικα (Cursor, ΕΡΓΑΣΙΕΣ 1, 2, 4, 7, 8, 9).

**Πώς διαβάζεται:** τα Α είναι αυτά που κάνουν ζημιά **σήμερα**. Τα Β θα
χτυπήσουν μόλις ανοίξει η αντίστοιχη λειτουργία. Τα Γ είναι οι ρίζες — μία
διόρθωση κλείνει πολλά Α και Β μαζί. Τα Δ θέλουν **δική σου απόφαση** πριν
γίνει οτιδήποτε. Τα Ε είναι κλειστά — μη τα ξανασυζητήσετε.

**Σύνολο:** 58 ανοιχτά · 9 κλειστά ή ψευδώς θετικά · 7 αποφάσεις owner.

---

## Α. ΕΝΕΡΓΗ ΖΗΜΙΑ — συμβαίνει τώρα

| # | Τι | Απόδειξη | Σοβ. | Αναστρέψιμο |
|---|---|---|---|---|
| **A1** | **Ληγμένο πιστοποιητικό ψυκτικού χωρίς καμία ειδοποίηση.** 6 σημεία διαβάζουν `ATP Expiry`, η στήλη λέγεται `FRC Expiry` | Ρυμούλκα **P47331**, έληξε **31/05/2026**, ενεργή. Επιβεβαιώθηκε ότι ΔΕΝ είναι από τις 2 σημασμένες ως εξαιρούμενες | P0 | Ναι — τα δεδομένα υπάρχουν |
| **A2** | **Ο λογιστής δεν μπορεί να σημάνει παραγγελία τιμολογημένη.** `accountant` έχει `"*": ["GET"]` και δεν έχει `orders` στη λίστα εγγραφής → 403 | `worker:417-431` · `invoicing.js:751-758` · **60 παραδομένα φορτία €137.682 σε αναμονή** | P0 | Ναι — αλλαγή δικαιωμάτων |
| **A3** | **Ο dispatcher μπορεί να κάνει cascade delete παραγγελίας.** Ο έλεγχος είναι σε `PATCH` αντί για `DELETE` | `worker:2349` · **8 πραγματικές εκτελέσεις από dispatcher**, τελευταία 21/08 | P0 | Ναι — αλλαγή ελέγχου |
| **A4** | **Τζίρος €0 σε CEO Dashboard και Performance.** 7 αναγνώσεις `Net Price` (ανύπαρκτο) + fallback σε `Revenue`/`Gross Revenue`, επίσης ανύπαρκτα | `ceo_dashboard.js:492,526,529,563,565,577,590` · πραγματικός τζίρος **€202.522** | P0 | Ναι |
| **A5** | **Τιμή, αμοιβή συνεργάτη και περιθώριο φτάνουν σε dispatcher και warehouse.** `atGet(ORDERS)` χωρίς `fields` τρέχει 3″ μετά το login και κάθε 5′, για κάθε ρόλο | `utils.js:1107,1457-1459` · `worker:1943-1945` | P0 | Απόφαση — βλ. Δ4 |
| **A6** | Το `Partner Rate` ζητείται **ονομαστικά** στο Weekly National, χωρίς κανένα `can()` gate στο module | `weekly_natl.js:104-110` | P0 | Απόφαση — βλ. Δ4 |
| **A7** | Το `partner_assignments_computed` σερβίρει `client_revenue`, `gross_profit`, `margin_percent` σε dispatcher | `worker:463-475,1509-1534` — το σχόλιο το παραδέχεται ως «R-04 TENSION, deliberate» | P0 | Απόφαση — βλ. Δ4 |
| **A8** | **Η αφαίρεση σημείου δεν φτάνει στη βάση.** `atDelete` → 403 → `console.error`, η αποθήκευση δείχνει ✓ | `stops-helpers.js:95-97` · **4 πραγματικά 403, 13/08** | P0 | Ναι |
| **A9** | **Το Daily Ops χάνει σιωπηλά το σκέλος VS.** Φίλτρο με `{VS CD Date}` → 422 → `catch` σε παλιό φίλτρο | `daily_ops.js:48,57-65` · **148 σφάλματα σε 10 μέρες**, συνεχίζεται | P1 | Ναι |
| **A10** | **Οι 30 εγγραφές ράμπας δεν συνδέονται με τίποτα.** `order_id`, `national_order_id`, `truck_id`, `driver_id` = NULL σε 30/30 | `daily_ramp.js:269-270` — το RAMP δεν έχει `links` block | P1 | **ΟΧΙ** για τις 30 |
| **A11** | **Καμία προστασία ταυτόχρονης επεξεργασίας.** Ο έλεγχος κρίνεται από `Last Modified`, που δεν υπάρχει → `if (tracked)` πάντα false | `atSafePatch` · **511 ενημερώσεις σε 10 μέρες από 3 dispatcher** | P1 | Ναι |
| **A12** | **Η τιμολόγηση δεν μπορεί να δείξει αριθμό παραγγελίας, ποτέ.** Δοκιμάζει `Order Number` και `National Order ID` — και τα δύο ανύπαρκτα | `invoicing.js:49` → γράφει πάντα «(χωρίς αριθμό)» | P1 | Απόφαση — βλ. Δ3 |
| **A13** | **Ο warehouse δεν μπορεί να ανοίξει καμία σελίδα.** `preloadReferenceData` κάνει `Promise.all` σε 6 πίνακες, ο warehouse δικαιούται 1 → reject → και το `_loading` μένει cached για πάντα | `api.js:729-770` · `worker:489-499` · 6 σελίδες σκάνε | P1 | Ναι |
| **A14** | Ο warehouse παίρνει **403 στο ίδιο του το Ramp Board** — ο πίνακας `ramp` δεν υπάρχει στα δικαιώματά του και δεν έχει `"*"` | `worker:489-507` · `daily_ramp.js:290-302` (και POST στο render) | P1 | Ναι |
| **A15** | **Management, accountant και warehouse παίρνουν 403 σε κάθε ενέργεια** σε Weekly National, Daily Ops, Daily Ramp — κανένα `can()` gate σε αυτά τα 3 modules | 34 σημεία για management, 51 accountant, 49 warehouse | P1 | Ναι |
| **A16** | Dispatcher και accountant παίρνουν **403 σε κάθε αποθήκευση στη Συντήρηση** — το `maintenance.js` δεν καλεί ποτέ `can()` | `maintenance.js:1041,2218,2320` · `worker:481-487` | P1 | Ναι |
| **A17** | Το management παίρνει **403 ακόμη και σε ανάγνωση** στο Trip PnL — `COSTS_PERMS.management = {}` | `worker:2380-2390` · `config.js:336` | P1 | Ναι |
| **A18** | Το management ανοίγει το **Pallet Ledger** και το ημερολόγιο κινήσεων γυρίζει 403 | `worker:2571-2585` · `router.js:46-48` | P1 | Ναι |
| **A19** | **Η σελίδα «My Performance» δεν φτάνεται ποτέ** από τους 3 ρόλους που τη δικαιούνται — το NAV item μπήκε σε group με `perm:'ceo_dashboard'` | `router.js:51-54` · το `can('performance')` δεν καλείται πουθενά | P1 | Ναι |
| **A20** | **Το `/v1/ai/messages` ελέγχει μόνο αν είσαι συνδεδεμένος, κανέναν ρόλο.** Κάθε χρήστης proxy-άρει στο Anthropic με το κλειδί της εταιρείας | `worker:595-597,3011-3013` | P1 | Ναι |
| **A21** | Ο warehouse παίρνει το **owner tool set** του AI βοηθού — `AIC_PROFILES[role] \|\| AIC_PROFILES.owner`, fail-open | `ai-chat.js:622-635,833-836` | P2 | Ναι |
| **A22** | **Ψευδής προειδοποίηση σε κάθε επιτυχή διαγραφή** παραγγελίας — ο `_delFail` μετρά και αποτυχίες ανάγνωσης | 5 εμφανίσεις, τελευταία 21/08. **Μηδέν πραγματική ζημιά** — ελέγχθηκε | P2 | Ναι |
| **A23** | **60 ψευδή σφάλματα «Missing fields in ORDERS: Status»** — ο facade παραλείπει τα NULL, ο validator το εκλαμβάνει ως εξαφανισμένο πεδίο | Πραγματικό υποκείμενο: **3 ενεργές παραγγελίες με `status = NULL`** | P2 | Ναι |
| **A24** | `Carrier Type`, `Invoice Status`, `Ops Notes`, `Ops Status` **εμφανίζονται στην οθόνη και κανείς δεν τα γράφει** | NULL σε 90/90. Ο χρήστης βλέπει «—» παντού | P2 | Ναι |
| **A25** | Το `emptyLegs` στο Metrics Audit μετρά **πάντα 0** — τα Summary πεδία δεν έρχονται | R7-9 | P2 | Ναι |
| **A26** | Οι εξαιρέσεις **NO-FRC δεν διαβάζονται ποτέ** — το `Notes` δεν ζητείται στις ρυμούλκες. 2 οχήματα ρητά εξαιρεμένα από τον owner | R9-4 · λανθάνον πίσω από το A1 | P2 | Ναι |
| **A27** | **Ανενεργοί πελάτες μπαίνουν στο scan** — το `Active` λείπει από το preload των clients | `_REF_FIELDS.clients = ['Company Name']` | P2 | Ναι |
| **A28** | **Ο λογιστής μπορεί να διαγράψει φορτηγό, ρυμούλκα, οδηγό, πελάτη.** Το σχόλιο το παραδέχεται ως «CURRENT behaviour, not an endorsement» | `worker:369-375,417-431` | P2 | Απόφαση |
| **A29** | Ο dispatcher μπορεί να **δημιουργεί και να αλλάζει round trips κόστους** — ο μόνος φραγμός είναι έλεγχος `ROLE` από `localStorage` | `worker:2383` · `costs.js:54-61` | P2 | Ναι |
| **A30** | **Οι σελίδες με gate μόνο στην όψη** (`weekly_pickups`, `metrics_audit`, `costs`) κρίνονται από `localStorage`. Το Metrics Audit αποδίδει **πραγματικό τζίρο** σε ρόλο που δεν τον δικαιούται | `router.js:281,380` · `metrics_audit.js:303,308` | P2 | Απόφαση |
| **A31** | Η `alexia` **δεν υπάρχει στη βάση** — 10 μέρες, δεν μπορεί να συνδεθεί | Επισημάνθηκε 12/08, ισχύει 22/08 | P1 | Ναι |
| **A32** | **5 λογαριασμοί `stg_*` ενεργοί** στη βάση, χωρίς αντιστοιχία στο `config.js` | Συνδέονται και πετάγονται έξω από τον tamper guard | P2 | Ναι |
| **A33** | **23 από 89 παραγγελίες χωρίς τιμή** (26%) | Κενό καταχώρησης, όχι κώδικα | P2 | Ναι |
| **A34** | **9 ληγμένα ΚΤΕΟ, 3 ληγμένες ασφάλειες** — εδώ οι ειδοποιήσεις **χτυπούν** και αγνοήθηκαν | Διαφορετικό πρόβλημα από το A1: ο κώδικας μιλά, κανείς δεν ακούει | P2 | Ναι |

---

## Β. ΛΑΝΘΑΝΟΝΤΑ — θα χτυπήσουν μόλις ανοίξει η λειτουργία

| # | Τι | Πότε ενεργοποιείται |
|---|---|---|
| **B1** | `{Groupage Lines}` σε **8 διαδρομές διαγραφής** → 422 → **το CONSOLIDATED LOAD δεν σβήνει ποτέ**. Ο Worker το τεκμηριώνει: ο σύνδεσμος ζει στην πλευρά του GL | Με το πρώτο groupage. Σήμερα `consolidated_loads` = 0 |
| **B2** | `Source Record` με id εθνικής παραγγελίας → **απορρίπτεται ολόκληρη η εγγραφή με 400**, ο χρήστης βλέπει «Order updated ✓» | Με την πρώτη εθνική παραγγελία. Σήμερα = 0 |
| **B3** | `Nat Load` → PARTNER ASSIGNMENTS. Το `national_load_id` είναι NULL σε **21/21**. Ο έλεγχος «υπάρχει ήδη ανάθεση;» θα γυρίζει πάντα κενός | Με τις πρώτες αναθέσεις συνεργατών στο εθνικό σκέλος |
| **B4** | `Pallets` αντί `Total Pallets` στο partner feeder → κινήσεις με **μηδέν παλέτες**, το `/confirm` τις απορρίπτει με 400 | Σήμερα: **0 κινήσεις συνεργάτη** από 29. **28/29 σε `pending`, καμία confirmed** |
| **B5** | `F.LOADING_DATE` / `F.DELIVERY_DATE` δηλώνονται για NAT_LOADS, **υπάρχουν μόνο στα GROUPAGE LINES** | Με το πρώτο groupage |
| **B6** | `F.CL_KATHODOS` / `CL_ANODOS` λένε `'North→South'` με σχόλιο «unified». **Δεν έγινε** — ο Worker λέει ελληνικά | Αν κάποιος τις χρησιμοποιήσει. Σήμερα αχρησιμοποίητες |
| **B7** | `Matched Export ID` δεν υπάρχει — το κλείδωμα «ήδη ταιριασμένο» δεν πυροδοτείται ποτέ | **31 από 89** παραγγελίες έχουν ήδη ταίριασμα |
| **B8** | Το `Opening Hours` / `Delivery Days` δεν ζητούνται → **κενή φόρμα τοποθεσιών** | Ήδη ενεργό, χαμηλή ζημιά |

---

## Γ. ΣΧΕΔΙΑΣΤΙΚΑ — οι ρίζες. Μία διόρθωση κλείνει πολλά

| # | Η ρίζα | Τι εξηγεί |
|---|---|---|
| **Γ1** | **Ο Worker καταπίνει σιωπηλά κάθε άγνωστο πεδίο.** `if (column) row[column] = value;` — δεν υπάρχει `else`, ούτε log. Αν έστω ένα πεδίο αναγνωριστεί, το αίτημα γυρίζει `200` | A4, A9, A11, A12, B1, B2, B3, B4, B7 — **και όσα δεν βρήκαμε ακόμη** |
| **Γ2** | **Ο facade παραλείπει κάθε πεδίο με τιμή NULL.** Άρα `'X' in fields` είναι false ακόμη κι όταν το πεδίο ζητήθηκε σωστά | A23, R9-3, και κάθε έλεγχος ύπαρξης πεδίου |
| **Γ3** | **Το `config.js F` δεν είναι πηγή αλήθειας.** 158 σταθερές, χρησιμοποιούνται 29. **20 φαντάσματα + 16 λάθος στόχοι** — συμφωνία 74% με τον πραγματικό χάρτη. Τα λάθη επέζησαν επειδή σχεδόν κανείς δεν το διαβάζει | Το `core/constants.js` έχει **έναν** καταναλωτή σε όλο το repo |
| **Γ4** | **13 σταθερές `RE_*` δείχνουν στο RAMP_EVENTS**, πίνακα εκτός χάρτη (404), με τιμές (`Status`, `Truck`, `Pallets`) που υπάρχουν σε άλλους πίνακες — **περνούν κάθε επιφανειακό έλεγχο** | Η μόνη κατηγορία που δεν θα βρίσκαμε ποτέ από τα δεδομένα |
| **Γ5** | **Το wildcard `"*"` ΑΝΤΙΚΑΘΙΣΤΑ, δεν συγχωνεύεται.** Ο dispatcher και ο warehouse δεν το έχουν → κάθε πίνακας εκτός ονομαστικής λίστας είναι 403 ακόμη και για ανάγνωση | A13, A14. **Προειδοποίηση:** μια ειδική εγγραφή που ξεχνά το `GET` αφαιρεί σιωπηλά την ανάγνωση |
| **Γ6** | **Πέντε ανεξάρτητοι πίνακες δικαιωμάτων** που κανείς δεν συγκρίνει: `PERMS` (UI), router gating, `PERMISSIONS`, `COSTS_PERMS`, `PL_PERMS` | A15–A19 |
| **Γ7** | **Ο Worker γράφει σφάλματα μόνο στην κονσόλα του.** Δεν υπάρχει logpush ούτε tail consumer — κανείς δεν τα διαβάζει ποτέ | Γι' αυτό δεν ξέρουμε ακόμη ποιο ακριβώς query παράγει τα 148 × 422 |
| **Γ8** | **Επτά κλειδιά του `TABLES` δείχνουν σε ανύπαρκτους πίνακες** (404): TRIP_COSTS, METRICS_SNAPSHOTS, SCAN_TRAINING (διπλό κλειδί `config.js:79/85`), TRIPS, NAT_TRIPS, DRIVER_LEDGER, RAMP_EVENTS | Το CEO Dashboard καταρρέει μετά το 404 |
| **Γ9** | **Το `CLAUDE.md:136` περιγράφει την παλιά αλυσίδα v1** και αντιφάσκει με τον κώδικα και το `ARCHITECTURE.md`. Το `ai-chat.js:652,657` διδάσκει το ίδιο λάθος **στους χρήστες** | Παραλίγο να κατατεθεί ψευδές P0 βασισμένο σε αυτό |

---

## Δ. ΑΠΟΦΑΣΕΙΣ ΠΟΥ ΘΕΛΟΥΝ ΕΣΕΝΑ — πριν γίνει οτιδήποτε

| # | Ερώτημα | Τα γεγονότα |
|---|---|---|
| **Δ1** | **Cross-dock Date ή VS CD Date;** | Το `Cross-dock Date` το ξέρει ο Worker, το γράφει η φόρμα, έχει **19/19 δεδομένα**. Το `VS CD Date` έχει μηδέν. **Αλλά:** η αποθηκευμένη τιμή είναι *υπολογισμένη εκτίμηση* (παράδοση −1). Αν γράψεις εκεί και τις χειροκίνητες διορθώσεις, **χάνεται η διάκριση εκτίμησης/πραγματικού** — αντίθετο στον δικό σου κανόνα «το παραδόθηκε υπολογίζεται, δεν γράφεται» |
| **Δ2** | **Cascade delete: owner-only ή dispatcher;** | Κάθε άλλο DELETE ελέγχει `DELETE`· μόνο ο πιο καταστροφικός ελέγχει `PATCH`. Έγινε 8 φορές από dispatcher, **μηδέν ορφανά** — στάθηκαν τυχεροί |
| **Δ3** | **Τι νούμερο θέλει η Ειρήνη στο τιμολόγιο;** | Το `Reference` υπάρχει σε **37/89 (42%)**, τιμές τύπου `6100112483` — είναι **ο αριθμός του πελάτη**. Η άλλη διαδρομή (view, όπως το `Week Number`) είναι δοκιμασμένη. **Ρώτα την τι πληκτρολογεί σήμερα στο ERP** |
| **Δ4** | **Φιλτράρισμα πεδίων ανά ρόλο;** | Δεν υπάρχει καθόλου στον `toAirtableRecord`. Σήμερα **3 dispatcher βλέπουν τιμή, αμοιβή συνεργάτη και περιθώριο** σε κάθε φόρτωση. Ο ίδιος ο κώδικας προτείνει τη λύση και δεν υλοποιήθηκε |
| **Δ5** | **Αφαίρεση stop: DELETE ή soft-flag;** | **Και τα δύο μοτίβα υπάρχουν ήδη** στον ίδιο πίνακα: 16 stops με `deleted_at`, 23 φυσικά εξαφανισμένα |
| **Δ6** | **Net Price** | Αναβλήθηκε από εσένα μέχρι το PnL. **Μέχρι τότε το CEO Dashboard δείχνει €0** — θέλει είτε υλοποίηση του επιμερισμού 650/2.350 είτε ανάγνωση από `Price` |
| **Δ7** | **Ληγμένα έγγραφα** | Ένα FRC ληγμένο 3 μήνες, 9 ΚΤΕΟ, 3 ασφάλειες. Ποιος ευθύνεται να τα κλείσει και με τι ρυθμό ελέγχου; |

---

## Ε. ΚΛΕΙΣΤΑ ή ΨΕΥΔΩΣ ΘΕΤΙΚΑ — μη τα ξανασυζητήσετε

| Τι | Γιατί έκλεισε |
|---|---|
| «Το `audit_log` σταμάτησε 28/7» (`AT-1`) | **Λάθος.** 2.791 εγγραφές, 1.303 στο 10ήμερο, τελευταία σήμερα |
| «Διαγραφές σε GROUPAGE LINES» (`AT-2`) | Κενό ερώτημα — ο πίνακας έχει 0 εγγραφές |
| «Οι `demo_*` συνεχίζουν διαγραφές» (`AT-5`) | Καμία δραστηριότητα demo στο 10ήμερο |
| «Το `national_orders` είναι άδειο = σφάλμα» | **Σωστά άδειο.** Απόφαση owner: VS → NAT_LOADS κατευθείαν |
| «Οι ημερομηνίες cross-dock χάθηκαν» | **Λάθος δικό μου.** 19/19 υπάρχουν στο `cross_dock_date` |
| «Διπλότυπες αναθέσεις συνεργατών» | 21 αναθέσεις, 21 παραγγελίες, **0 διπλότυπα** |
| «Το `local_moves` επιστρέφει 404» | Έκλεισε με deploy 14/08 11:26 |
| «Το SQL της `delete_order_cascade` λείπει» | Υπάρχει: `db/migrations/2026-08-12_wipe_orders.sql` |
| «Το `Price` λείπει από τον χάρτη» | **Λάθος δικό μου** — `worker:990`. Υπάρχει |
| «1.090 test records» | Δεν μεταφέρθηκαν από το Airtable. Βάση καθαρή |

---

## ΣΤ. ΤΟ ΠΡΩΤΟ ΠΡΑΓΜΑ, ΠΡΙΝ ΑΠΟ ΟΛΑ

**Δεν αποδεικνύεται ότι ο deployed Worker είναι αυτός του repo.**

Στις 13/08 05:50–05:54 ο `kelesmitos` πήρε **403 τέσσερις φορές** διαγράφοντας
stop. Στις 07:51 την ίδια μέρα, **η ίδια ενέργεια πέτυχε**. Το bundle του repo
λέει ότι ο dispatcher δεν έχει `DELETE` στο `order_stops`.

Ή το RBAC άλλαξε στο ενδιάμεσο, ή **ο deployed Worker διαφέρει από το repo**.

Αν διαφέρει, **μέρος αυτού του μητρώου αφορά κώδικα που δεν τρέχει.**
Λύνεται σε δέκα λεπτά με `wrangler tail` ή σύγκριση hash. **Πριν κάτσετε με
τη ValueDriven.**

---

# ΠΡΟΣΘΗΚΗ — ΕΡΓΑΣΙΑ 3 (σιωπηλές αποτυχίες), 22/08

24 ευρήματα, **14 P0**. Επαληθεύτηκαν στον κώδικα τα S-2, S-3, S-7, S-14, S-21.

## ⚠️ ΔΙΟΡΘΩΣΗ ΠΡΟΗΓΟΥΜΕΝΗΣ ΑΝΑΛΥΣΗΣ

Το **S-14** αλλάζει την αξιοπιστία όσων είπα για τα σφάλματα. Το
`reportError()` (`utils.js:85-94`) στέλνει το detail **μόνο** στο `_tmsLog`,
που γράφει μόνο αν `window.TMS_DEBUG` ή `localStorage.tms_debug === '1'` —
**κανένα δεν τίθεται πουθενά στον κώδικα**.

Άρα **44 σημεία** εμφανίζουν toast στον χρήστη και **δεν αφήνουν κανένα ίχνος**:
ούτε `app_errors`, ούτε console, ούτε stack.

**Συνέπεια:** η ανάλυση των 1.186 `app_errors` ήταν **ελλιπής κατά άγνωστο
ποσό**. Ό,τι είπα για «τι σπάει» ισχύει· το «τι ΔΕΝ σπάει» δεν αποδεικνύεται.

## Νέα Α — ενεργή ζημιά

| # | Τι | Απόδειξη | Σοβ. |
|---|---|---|---|
| **A35** | **44 σφάλματα προς τον χρήστη χωρίς κανένα ίχνος** | `utils.js:85-94` → `_tmsLog` πίσω από debug flag που δεν τίθεται ποτέ | P0 |
| **A36** | **13 σημεία κάνουν fire-and-forget τον downstream sync** — χωρίς `await`, με `.catch(console.warn)`. Είναι η μοναδική διαδρομή που διαδίδει την αλλαγή σε NAT_LOADS/GL/CL/RAMP/PA | S-1 · toast ✓ ενώ η αλυσίδα μένει ασύγχρονη | P0 |
| **A37** | **Δύο εντελώς κενά `catch` γύρω από γραφές** στο `order-sync.js:156,169` — το ένα με σχόλιο που δικαιολογεί «λείπει πεδίο», ενώ καταπίνει και 403 και 500 | Επαληθεύτηκε αυτολεξεί | P0 |
| **A38** | `.catch(() => [])` σε read → `if (!gls.length) return;` → **ολόκληρος ο cascade GL→CL→NL παραλείπεται σιωπηλά** | `order-sync.js:116-117,127,145,162` | P0 |
| **A39** | **Το `Estimated Cost` και το `Workshop` χάνονται σε κάθε work order.** Ο χάρτης `maint_req` έχει μόνο 7 πεδία — επαληθεύτηκε | `maintenance.js:2312-2322` · `worker:805-817`. Το `Object.assign` δείχνει τα ζητηθέντα μέχρι το reload | P0 |
| **A40** | **Πέντε νούμερα διοίκησης είναι δομικά μηδέν**: ανεξόφλητο, εθνικό περιθώριο, κορυφαίοι πελάτες, κενά χλμ, έσοδα συνεργατών | S-9 έως S-13. Σχόλια «Previously hardcoded to 0» — από σκληρό μηδέν σε υπολογισμένο μηδέν | P0 |
| **A41** | **Το «AUDIT WRITE FAILED» καταπίνεται σε console που κανείς δεν διαβάζει** | `worker:524`. Επαληθεύτηκε | P1 |
| **A42** | **8 σημεία `catch(e){ toast('Error') }`** χωρίς μήνυμα, χωρίς log. Μέσα τους: «Σε μεταφορά», «Αναβολή», «Παραδόθηκε» του Daily Ops | S-18 | P1 |
| **A43** | Το πλαφόν `MAX_APP_ERROR_POSTS = 20` ανά φόρτωση — ένα σφάλμα σε render loop γεμίζει το πλαφόν και μετά σιωπά | `utils.js:510-511` | P2 |

## Σύνδεση με ανοιχτή εκκρεμότητα

Το **A41** δίνει υποψήφια εξήγηση για τις δύο γραμμές του σταυρωτού ελέγχου που
δεν έκλειναν στο `00_INVENTORY.md`: `order_stops` (−1) και `pl_movements`
(34 creates έναντι 28 γραμμών). Αν το audit write απέτυχε σιωπηλά, οι αριθμοί
δεν κλείνουν χωρίς να φταίνε τα δεδομένα.

**Αντίθετα**, για τα `orders` ο έλεγχος κλείνει ακριβώς (102 − 7 = 95 = 95),
άρα η απώλεια ίχνους **δεν είναι εκτεταμένη**.

## Το πρότυπο που αξίζει να γίνει κανόνας

`modules/pallet_ledger.js:287-300`: guard ώστε να μη ξεκλειδώνει το κουμπί ·
`await` σε **και τα δύο** αιτήματα **πριν** το toast · `catch` που δείχνει το
πραγματικό `e.message` στον χρήστη · `finally` που ξεκλειδώνει.
Είναι το ακριβώς αντίθετο του S-5 σε κάθε διάσταση.

Και `ceo_dashboard.js:169-180`: **ονομάζει στην οθόνη ποια πηγή δεν φόρτωσε**
αντί να αποδώσει μηδενικά. Αυτό ακριβώς λείπει από τα S-17.

---

# ΠΡΟΣΘΗΚΗ — ΕΡΓΑΣΙΑ 5 (νεκρός κώδικας), 22/08

24 ευρήματα. Επαληθεύτηκαν στη βάση τα D-1, D-2, D-3· στον κώδικα τα D-5, D-6, D-8, D-9, D-11.

## Νέα Α — ενεργή ζημιά

| # | Τι | Απόδειξη | Σοβ. |
|---|---|---|---|
| **A44** | **Η Εντολή Οδηγού τυπώνει λάθος μικτό βάρος.** Το `print.html` ζητά `Gross Vehicle Weight kg` και `Weight kg` — ghosts. Η στήλη λέγεται `Tare Weight kg` | **Τα απόβαρα ΥΠΑΡΧΟΥΝ**: 21/36 φορτηγά, **37/40 ρυμούλκες**. Ένας συνδυασμός ~8,5t + ~6,8t = **~15 τόνοι λείπουν** από χαρτί που ο οδηγός δείχνει σε ζυγιστήριο ή έλεγχο | **P0** |
| **A45** | **Ο service worker παράγει unhandled rejection σε κάθε εγγραφή.** Ο κανόνας 1 (`isAirtableAPI`) δεν πυροδοτείται ποτέ μετά το C2 cutover, άρα κάθε POST/PATCH/DELETE πέφτει στον κανόνα 3, που **δεν έχει** τον φρουρό `method !== 'GET'` και καλεί `cache.put` χωρίς `.catch` | `sw.js:94-95` (ο φρουρός) έναντι `sw.js:145-158` (χωρίς). Επαληθεύτηκε | **P1** |
| **A46** | **Η προειδοποίηση «παλιά δεδομένα» είναι νεκρή.** Το `X-Cache-Timestamp` και το `SW_CACHE_AGE` παράγονται μόνο μέσα στον κανόνα 1 | `sw.js:102,120` → `app.html:232` δεν λαμβάνει ποτέ | P1 |
| **A47** | **Δεύτερη, ανεξάρτητη αιτία θανάτου του optimistic locking.** Το `atTrackVersion` καλείται **μόνο** από το `atTrackVersions`, που δεν καλείται από πουθενά | `api.js:856-864`. Επαληθεύτηκε: μηδέν κλήσεις σε όλο το repo | P0 |
| **A48** | **Ο AI uploader δελτίων παλετών δεν έχει γράψει ποτέ τίποτα.** Γράφει στο deprecated alias `PALLET_LEDGER` | `pallet_ledger_suppliers` = **0** · `pallet_ledger_partners` = **0** · **καμία** από τις 29 κινήσεις δεν έχει `sheet_url` | **P0** |
| **A49** | **4 μπάρες KPI είναι πάντα 0%** και το `crisis_count` βάφεται **πάντα πράσινο** — `kpi.target ? … : 0` με `target: 0` | `performance.js:592-598` · KPI στις γρ. 42, 47, 56, 57 | P1 |
| **A50** | **Νεκρό αντίγραφο του Worker με το όνομα του live** — `worker/archive/raw-multipart/petras-tms-backend-staging.js` (101 KB) + 3 ακόμη αρχεία. Το `worker/README.md` προειδοποιεί για split-brain· αυτό είναι το δόλωμα | Επιβεβαιώθηκε ότι υπάρχουν | P1 |
| **A51** | Το `app.prod.html` δείχνει σε `dist/style.min.css` και `dist/app.min.js` — **ο φάκελος `dist/` δεν υπάρχει** | Επιβεβαιώθηκε | P1 |
| **A52** | `_intlChangeStatus()` (16 γρ.) και `stopsValidate()` — **μηδέν κλήσεις**. Το δεύτερο υπονοεί έλεγχο στάσεων που δεν συμβαίνει πουθενά | `orders_intl.js:1732` · `stops-helpers.js:129` | P1 |
| **A53** | **22 από 1.005 συναρτήσεις ορίζονται και δεν καλούνται.** Αξιοσημείωτο: το `atStopAutoRefresh` καλείται στο logout για κάτι που δεν ξεκινά ποτέ | `api.js:833,846` · `router.js:414` | P2 |
| **A54** | **8 από 27 scripts λείπουν από το `APP_SHELL`** του service worker — μαζί το `icons.js`. Σε κρύα offline εκκίνηση: καθόλου εικονίδια | `sw.js:18-47` | P1 |

## Απαντήσεις στα ερωτήματα της ΕΡΓΑΣΙΑΣ 5

**D-1 — ΝΑΙ, επείγον.** Τα απόβαρα είναι συμπληρωμένα σε **37/40 ρυμούλκες**
και 21/36 φορτηγά. Δεν είναι έργο συλλογής δεδομένων — είναι λάθος όνομα σε
έγγραφο που φεύγει από την εταιρεία.

**D-2 — δεν υπάρχει πλημμύρα ορφανών.** 21 αναθέσεις: 20 με `order_id`,
**0 με `national_load_id`**, **1 χωρίς κανέναν γονέα** (παλαιό, όχι από αυτή τη
διαδρομή). Και οι 21 έχουν `partner_rate`. Ο μηχανισμός ισχύει, η εθνική
διαδρομή δεν χρησιμοποιείται. **Λανθάνον** — περιμένει.

**D-3 — δεν είναι θέμα ποιο αντίγραφο ζει.** Και τα δύο ημερολόγια παλετών
έχουν **μηδέν εγγραφές**, και **καμία** από τις 29 κινήσεις του `pl_movements`
δεν έχει συνημμένο δελτίο. Ο uploader **δεν έχει παραγάγει τίποτα**, πουθενά.

## Το `print.html` — 778 γραμμές που δεν είχε δει κανείς

Τρίτο ανεξάρτητο αντίγραφο του API client, με δικό του `PROXY_URL`, δικό του
`AT_BASE` και **7 σκληροκωδικοποιημένα table ids**. Από 49 ζεύγη
πίνακας+πεδίο: **42 έγκυρα, 7 ghosts**. Το σοβαρότερο είναι το A44.

---

# ΠΡΟΣΘΗΚΗ — ΕΡΓΑΣΙΑ 6 (ασφάλεια), 22/08

18 ευρήματα, **8 P0**. Επαληθεύτηκαν ζωντανά τα X-1 και X-3.

## 🔴 ΤΟ ΠΡΩΤΟ ΠΡΑΓΜΑ — δεν περιμένει τη ValueDriven

| # | Τι | Απόδειξη | Σοβ. |
|---|---|---|---|
| **A55** | **Οι κωδικοί 12 λογαριασμών είναι δημόσια ανακτήσιμοι από το internet, ως unsalted SHA-256** | Επαληθεύτηκε ζωντανά: `index.html` → **HTTP 200, 12 hashes** · `config.js` → **HTTP 200, 12 hashes**. Πέντε λογαριασμοί μοιράζονται **ένα** hash, μεταξύ τους ο `demo_owner` | **P0** |

```
curl https://dimitrispetras21-del.github.io/PETRASGROUP-TMS/config.js
```

Δίνει σε οποιονδήποτε: **usernames + ρόλους + hashes**. Unsalted SHA-256
ανθρώπινου κωδικού σπάει σε λεπτά με GPU ή σε δευτερόλεπτα με rainbow table.
Ο backend δέχεται τον ίδιο plaintext (`verify_login` με bcrypt).

Η διαδρομή `saltedHash` **είναι νεκρή** — υπάρχει ο έλεγχος (`index.html:477`)
αλλά **κανένας χρήστης δεν έχει το πεδίο**. Και τα 12 είναι unsalted.

**Ενέργειες, με σειρά:** αλλαγή κωδικών και των 7 προσώπων · αφαίρεση του
πίνακα `USERS` και από τα δύο αρχεία (η direct-mode διαδρομή είναι νεκρή αφού
`USE_PROXY = true` και στα δύο) · απενεργοποίηση των `demo_*` και `stg_*`.

## Νέα Α

| # | Τι | Απόδειξη | Σοβ. |
|---|---|---|---|
| **A56** | **Καμία προστασία brute-force στο `/auth/login`** — ούτε rate limit, ούτε lockout, ούτε καθυστέρηση | `worker:93-129` | P0 |
| **A57** | **Το Cache Storage κρατά αυθεντικοποιημένες απαντήσεις και δεν καθαρίζεται στο logout.** Παραγγελίες με `Price`, `Partner Rate`, `Advance Paid` επιβιώνουν της αποσύνδεσης και του κλεισίματος του browser | `sw.js:148-158` · `router.js:412-417` · `clearApiCaches` αγγίζει μόνο localStorage `at_*` | P0 |
| **A58** | **Το `/app-errors` POST δεν απαιτεί authentication.** Μόνος φρουρός ένα header `Origin` που ορίζει ο πελάτης | `worker:301-330` — απεριόριστη εγγραφή στο μόνο μόνιμο διαγνωστικό κανάλι | P0 |
| **A59** | **Ο έλεγχος CORS παρακάμπτεται όταν λείπει το header `Origin`** — `if (origin && !allowlist.includes(origin))` | `worker:2991-2993`. Κάθε non-browser client τον αγνοεί | P0 |
| **A60** | **Το AI proxy χωρίς όριο κατανάλωσης.** Το ημερήσιο πλαφόν ζει σε **localStorage** και το σβήνει ο χρήστης | `ai-chat.js:107-118` · `worker:588-597`. Opus × 8.000 tokens, απεριόριστα | P0 |
| **A61** | **Το logout δεν αποσυνδέει.** JWT 8 ωρών σε localStorage, **καμία server-side ακύρωση** | `worker:92,124-128` · `router.js:412-417` | P0 |
| **A62** | **Stored XSS: 30 παρεμβολές δεδομένων βάσης σε `innerHTML` χωρίς escape** στη Συντήρηση. 27 σημεία `innerHTML`, μόλις 8 `escapeHtml` | `maintenance.js:921,891,905,2155,2266,2282,824`. Καθαρότερο: `<textarea>${f['Notes']}</textarea>` | P1 |
| **A63** | Ωμά δεδομένα σε HTML σε 3 ακόμη modules — `Supplier/Client` της ράμπας, `Reference`, `Company Name`, `License Plate` | `daily_ramp.js:129,496,604` · `orders_intl.js:1592,1612` · `orders_natl.js:951,955` | P1 |
| **A64** | Escaping μόνο JS-string σε HTML attribute (`.replace(/'/g,"\\'")`) — **δεν καλύπτει το `"`** | `entity.js:1070` · `maintenance.js:2197` · `locations.js:209,326` | P1 |
| **A65** | **48 από 86 σημεία `filterByFormula` παρεμβάλλουν μεταβλητή· ΕΝΑ κάνει allowlist.** Ο Worker απορρίπτει `[(),]` μόνο στον κλάδο `!=` | `worker:1634` έναντι `:1655,1666,1754` | P2 |
| **A66** | Ο Worker είναι το **μοναδικό** όριο εξουσιοδότησης — service_role σε κάθε ερώτημα, κανένα RLS δεν συμμετέχει | `worker:105-113`. Μεγιστοποιεί την επίπτωση κάθε άλλου ευρήματος | P2 |

## Έκλεισε

**X-3 — το διαρρεύσαν Airtable PAT είναι ΑΝΑΚΛΗΜΕΝΟ.** Δοκιμάστηκε read-only
στο `/v0/meta/whoami` → **HTTP 401**. Παραμένει στο git history (14 commits)
ως θέμα υγιεινής, όχι ως ενεργός κίνδυνος. Το `worker/archive/` σαρώθηκε:
**μηδέν μυστικά**.

## Το πρότυπο που αξίζει να γίνει κανόνας

`core/form-helpers.js:57` — **allowlist** πριν την παρεμβολή στη φόρμουλα:
δεν μαντεύει τι είναι επικίνδυνο, ορίζει τι είναι αποδεκτό.
**Ένα από 48 σημεία το κάνει.**

---

# ΠΡΟΣΘΗΚΗ — ΕΡΓΑΣΙΑ 10 (MCP: Supabase + Cloudflare), 22/08

**Το deployed ΔΕΝ ταυτίζεται με το repo.** 2.396 γραμμές deployed έναντι 3.047
του repo· απόκλιση **και προς τις δύο κατευθύνσεις**.

## ΣΤ (αναθεωρημένο) — ΤΟ SPLIT-BRAIN ΕΙΝΑΙ Η ΡΙΖΑ

**Η παραγωγή είναι ΜΠΡΟΣΤΑ:** ο dispatcher **έχει** `DELETE` στα `order_stops`
(διορθώθηκε 13/08 μεταξύ 05:54 και 07:51 — πρώτη επιτυχής διαγραφή `07:51:12`)·
το `"VS CD Date": "vs_cd_date"` **είναι χαρτογραφημένο**· ο χάρτης CLIENTS έχει
επιπλέον πεδία.

**Το repo είναι ΜΠΡΟΣΤΑ:** ολόκληρα τα `/costs/*` και `/pallets/*` (604 γρ.),
ο `local_moves`, τα appointments των national_loads — **κανένα δεν έχει γίνει
deploy**.

> ⚠️ **ΠΑΓΙΔΑ:** ένα `wrangler deploy` από τον φάκελο **θα επαναφέρει τον
> dispatcher σε σπασμένη κατάσταση** και θα ξανασπάσει το `VS CD Date`.
> **Καμία διόρθωση πριν επιστρέψει ο deployed κώδικας στο repo.**

Μηδέν Workers Builds — deploys χειροκίνητα, **χωρίς ίχνος commit**.
Observability ανύπαρκτο: `query_worker_observability` → `[]` για όλο τον
λογαριασμό. Οι 31 `console.*` δεν καταγράφονται πουθενά.

## Ευρήματα που ΑΚΥΡΩΝΟΝΤΑΙ ή αλλάζουν

| Ήταν | Τώρα |
|---|---|
| **A8** dispatcher 403 σε διαγραφή stop | **ΑΚΥΡΟ ως ενεργό** — διορθωμένο στην παραγωγή 13/08 |
| **R8-1** `VS CD Date` σιωπηλή απόρριψη | **Ο ΜΗΧΑΝΙΣΜΟΣ ΑΚΥΡΟΣ** — χαρτογραφημένο στην παραγωγή |
| **A9** 148 × 422 «λόγω VS CD Date» | **Η ΑΙΤΙΑ ΞΑΝΑΝΟΙΓΕΙ.** ~70 από τα 148 συνέβησαν **μετά** το deploy της 14/08. **Άγνωστη αιτία** |
| **D-23 / Pallet Ledger** «αδρανή» | **ΕΝΕΡΓΗ ΑΠΟΤΥΧΙΑ** — οι routes δεν έγιναν deploy, όλα 404 |
| Cascade deletes από dispatcher: 8 | **7** — δική μου υπερμέτρηση |

## Νέα P0 — η βάση

| # | Τι |
|---|---|
| **A67** | **`upsert_user()` `SECURITY DEFINER` με `EXECUTE` σε `PUBLIC`/`anon`** — αλλάζει κωδικό και ρόλο **οποιουδήποτε** χρήστη ή δημιουργεί νέο `owner`. **Δεν υπάρχει στο repo** |
| **A68** | **`delete_order_cascade()` `SECURITY DEFINER` με `EXECUTE` σε `anon`** — καταστροφή παραγγελίας χωρίς authentication |
| **A69** | **11 όψεις `SECURITY DEFINER` με SELECT στον `anon`** — παρακάμπτουν RLS. Μεταξύ τους `partner_assignments_computed` (`gross_profit`, `margin_percent`) |
| **A70** | `anon` + `authenticated` με **πλήρη DML σε 34/34 πίνακες**, μαζί `users` και `audit_log`. Σώζει **μόνο** το RLS (0 policies = deny-all) |
| **A71** | Το `delete_order_cascade` **δεν αγγίζει τα `partner_assignments`** — η ανάθεση με το `partner_rate` επιβιώνει |

**ΟΡΙΟΘΕΤΗΣΗ — επαληθεύτηκε ανεξάρτητα:** τα A67–A69 απαιτούν το Supabase
**anon key**. Ελέγχθηκε το ζωντανό front end (`config.js`, `index.html`,
`app.html`, `print.html`) **και** τα 4 αρχεία του sister repo `petras-assign`
(169 KB): **καμία εμφάνιση anon key, JWT ή `supabase.co` URL**. Δεν είναι
σήμερα εκμεταλλεύσιμα από εξωτερικό — αλλά ένα anon key είναι σχεδιασμένο να
είναι δημόσιο και η ασφάλεια δεν πρέπει να στηρίζεται στη μυστικότητά του.

## Οι περιορισμοί που λείπουν — η απάντηση στο αρχικό ερώτημα

| Εύρημα | Περιορισμός που θα το είχε αποτρέψει | Υπάρχει; |
|---|---|---|
| 3 παραγγελίες με `status` NULL | `NOT NULL` + `CHECK` | **ΟΧΙ** — μηδέν CHECK σε όλο το `orders` |
| Λάθος τιμές σε `status`/`direction`/`source_type` | `CHECK` ή enum | **ΟΧΙ** σε 7 πίνακες· **ΝΑΙ** μόνο σε `pl_movements` και `users` |
| Ορφανή `partner_assignments` | FK + CHECK «ένας γονέας» | **ΟΧΙ** — **1 ορφανή σήμερα** |
| Διπλή ανάθεση ανά παραγγελία | `UNIQUE (order_id)` | **ΟΧΙ** |
| Δύο national_loads για μία VS | `UNIQUE (source_order_id)` | **ΟΧΙ** |
| 30 εγγραφές ράμπας χωρίς γονέα | FK στο `ramp.order_id` | **ΟΧΙ** |
| national_load χωρίς γονέα | CHECK «ακριβώς ένας» | **ΜΙΣΟ** — απαγορεύει δύο, **επιτρέπει κανέναν** |

**Πρότυπο:** το `pl_movements` είναι ο μόνος πίνακας με πλήρεις περιορισμούς.
**Μηδέν custom triggers** — καμία κρυφή λογική.

---

# ΠΡΟΣΘΗΚΗ — ΕΡΓΑΣΙΑ 11 (επανέλεγχος στον deployed), 22/08

Ο deployed χάρτης ξαναχτίστηκε (299 labels, 21 πίνακες). Από **260** φαντάσματα
των ΕΡΓΑΣΙΩΝ 1/7/8/9 **άλλαξε ένα** — το `VS CD Date`. Όλα τα υπόλοιπα ισχύουν.
Τα 18 ευρήματα δικαιωμάτων πλην ενός, τα 5 endpoints και οι 3 μηχανισμοί-ρίζες:
**πανομοιότυπα**.

## Η ΑΙΤΙΑ ΤΩΝ 422 ΒΡΕΘΗΚΕ — και δεν ήταν το VS CD Date

`modules/orders_intl.js:936-939`, στον κλάδο **VS OFF** του `_syncVeroiaSwitch`:

```js
filterByFormula: `FIND("${orderId}",ARRAYJOIN({Order},","))>0`   // πάνω στο RAMP
```

Το RAMP **δεν έχει `links` block** → το `preResolveLinkTerms` επιστρέφει αμέσως
→ ο `translateTerm` δεν ξέρει `FIND` → `UnsupportedFilter` → **422**.

Τρέχει σε **κάθε αποθήκευση διεθνούς παραγγελίας χωρίς Veroia Switch** — 70 από
τις 89 ενεργές.

**Ανεξάρτητη επαλήθευση από τη βάση:** **129 από 150** (86%) των 422 συμβαίνουν
μέσα σε 60″ από `create`/`update` στα `orders`. Η ημερήσια κατανομή ακολουθεί
τον όγκο επεξεργασιών, όχι τα ανοίγματα σελίδας.

**Το A9 διορθώνεται:** η Daily Ops **δεν** χάνει το σκέλος VS — το φίλτρο της
περνά τον deployed translator. Το πραγματικό κόστος είναι ότι **η καθαριότητα
RAMP στο σβήσιμο του Veroia Switch δεν εκτελείται ποτέ**.

## A10 — αλλάζει χαρακτήρα: ΔΕΝ είναι αχρησιμοποίητο σχέδιο

Ο κώδικας **γράφει** τον σύνδεσμο: `daily_ramp.js:269-270`
→ `rec['Order'] = [intlPid]` / `rec['National Order'] = [nlPid]`.

Ο Worker τον **πετάει σιωπηλά**, γιατί το RAMP δεν έχει `links`. HTTP 200.

Άρα οι 30 ορφανές γραμμές ράμπας δεν είναι παράλειψη υλοποίησης — είναι το
**Γ1 σε δράση**. Η διόρθωση είναι να προστεθεί `links` στο RAMP, **όχι** να
αφαιρεθεί το ερώτημα. Και όταν προστεθεί, ζωντανεύει και το `daily_ramp.js:678-705`.

## Νέο split-brain, τρίτη κατεύθυνση

Τα **4 πεδία WORKSHOPS** του deployed (`Country`, `Aliases`, `VAT Number → tax_id`,
`Legal Name`) **δεν υπάρχουν στο repo** και δεν έχουν front-end καταναλωτή.
Ένα `wrangler deploy` από τον φάκελο **τα σβήνει**.

> **Τρία πράγματα που δουλεύουν σήμερα και θα σβηστούν από ένα deploy:**
> `order_stops` DELETE του dispatcher · `VS CD Date` · τα 4 πεδία WORKSHOPS.

## Διορθώσεις χαρακτηρισμού

- `Workshop` στο `maint_req`: **ΛΑΘΟΣ ΣΤΟΧΟΣ**, όχι φάντασμα — υπάρχει ως link
  στο `MAINT_HISTORY`. Το `Estimated Cost` είναι όντως φάντασμα.
- `D-2`: **+1 σημείο 422** — το `orders_natl.js:1540` βάζει το `{Nat Load}` σε
  `filterByFormula`, άρα εκεί δεν σιωπά, σκάει.
- Το CLIENTS είναι πανομοιότυπο· η απόδοση των έξτρα πεδίων στην ΕΡΓΑΣΙΑ 10
  ήταν λάθος — ανήκουν στο WORKSHOPS.
