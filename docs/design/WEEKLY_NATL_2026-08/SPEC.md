# SPEC — Weekly National v3 + φόρμα National Orders

`docs/design/WEEKLY_NATL_2026-08/SPEC.md` · 2026-08-10 · **κλειδωμένες αποφάσεις, ανοιχτή υλοποίηση**

Πηγή αλήθειας για τη λειτουργία: `WEEKLY PLAN (9).xlsx`, φύλλο `W31(26)` — γραμμές
38-71 (NATIONAL) και 77-90 (ΤΟΠΙΚΑ ΔΡΟΜΟΛΟΓΙΑ). Τα δύο prototypes του φακέλου
δείχνουν το αποτέλεσμα με πραγματικά δεδομένα εκείνης της εβδομάδας:

| Αρχείο | Τι δείχνει |
|---|---|
| `proto.html` | Weekly National — δύο ενότητες, ημέρες, groupage, συνδέσεις |
| `orders_natl_form_proto.html` | Φόρμα National Orders — απλή + groupage δύο επιπέδων |

---

## 1. Τι λύνει

Το εβδομαδιαίο σχέδιο ζει σε Excel γιατί το TMS δεν καλύπτει τρία πράγματα:

1. **Τοπικές παραδόσεις** — δεν υπάρχουν καθόλου. Είναι σημαντικό μέρος των
   ημερήσιων κινήσεων και συχνά μη τιμολογούμενες: κομμάτια εθνικών ή διεθνών
   δρομολογίων που αναλαμβάνει τοπικός οδηγός.
2. **Ποιος καλύπτει ποιον** — στο Excel γράφεται ως ελεύθερο κείμενο
   (`ΦΟΡΤΙΟ GTL ΜΠΑΜΠΑΛΟΥ`). Το σύστημα δεν ξέρει τη σχέση, άρα δεν μπορεί να
   προειδοποιήσει όταν ένα κομμάτι μένει ακάλυπτο.
3. **Σύνθεση φορτίου** — `24 ΠΑΛ ΣΚΛΑΒΕΝΙΤΗΣ - 3 ΠΑΛ ΑΒ` είναι κείμενο. Οι
   παλέτες ανά πελάτη δεν είναι μετρήσιμες, άρα ούτε το γέμισμα του φορτηγού.

---

## 2. Αποφάσεις (owner, 2026-08-10)

| # | Απόφαση | Συνέπεια |
|---|---|---|
| Δ1 | Δύο ενότητες σε **μία** σελίδα: ΕΘΝΙΚΑ πάνω, ΤΟΠΙΚΑ κάτω | Ίδιο πλάτος, ίδιες ημέρες, ένα scroll |
| Δ2 | Άξονας = **ημέρα**, οι κενές μέρες παραμένουν **ορατές** | Η κενή μέρα είναι πληροφορία, όχι απουσία |
| Δ3 | Η **τιμολόγηση δεν ανήκει** εδώ | Κανένα πεδίο ΤΠΥ· το Invoicing καλύπτει το θέμα |
| Δ4 | Η **κατάσταση οδηγών** (ανάπαυση/άδεια) μένει **εκτός** | Άλλη σελίδα, άλλη φάση |
| Δ5 | **Ώρα** μόνο όπου υπάρχει ραντεβού, ως μικρό σήμα | Ο ημερήσιος σχεδιασμός δεν είναι ωρολόγιο πρόγραμμα |
| Δ6 | **Ρητή αμφίδρομη σύνδεση** τοπικού ↔ γονικού δρομολογίου | Chip και στις δύο πλευρές· μετρητής ακάλυπτων |
| Δ7 | Τοπική γραμμή = **οδηγός × ημέρα**, με τις κινήσεις του μέσα | Ο φόρτος του οδηγού διαβάζεται μονομιάς |
| Δ8 | Δύο δρόμοι εισόδου τοπικού, **ένα** μοντέλο | Η *κίνηση* είναι η οντότητα· ο γονιός προαιρετικό πεδίο |
| Δ9 | Groupage φορτίο = **μία γραμμή, συμπτυγμένη**, ανοίγει με κλικ | Σταθερό ύψος γραμμής σε όλη την εβδομάδα |
| Δ10 | Μένουν: drag-drop ταιριάσματος, εκτύπωση, CSV. **Φεύγουν**: πάνελ «Επιστροφές οδηγών» και ο φύλακας διπλής κράτησης | Ο σχεδιασμός είναι ημερήσιος και μικρού όγκου — ο φύλακας δεν αξίζει το κόστος |
| Δ11 | Το **Pick Ups μένει** όπου είναι· το εβδομαδιαίο δείχνει **μετρητή ουράς** | Καμία επέμβαση στο `petras-assign` |
| Δ12 | Στη φόρμα: groupage **δύο επιπέδων** — πελάτης → τα σημεία του | Μία παραγγελία **ανά πελάτη**, όχι ανά σημείο |
| Δ13 | Η **αξία μπαίνει ανά πελάτη**, όχι ανά σημείο | Ο Σκλαβενίτης χρεώνεται 960 € για τα 3 του σημεία |
| Δ14 | Η επίπτωση στο **Weekly International** σχεδιάζεται **τελευταία** | Ένα chip ανά σκέλος· τίποτα άλλο |
| Δ15 | Η φόρμα ακολουθεί **το σχήμα των διεθνών**: επαναλαμβανόμενες στάσεις με τοποθεσία, παλέτες και **ημερομηνία ανά στάση** | Ίδια λογική σε όλο το TMS· λύνει και το «ημερομηνία ανά γραμμή» |
| Δ16 | Το πεδίο **`Type`** (Ανεξάρτητη / Veroia Switch) **φεύγει** από τη φόρμα | Οι VS παραγγελίες δημιουργούνται αυτόματα από τα ORDERS, ποτέ χειροκίνητα — το πεδίο ήταν πάντα `Independent` σε ό,τι γράφεται εδώ |
| Δ17 | Η επιλογή **`ORDER TYPE`** είναι **ρητή, δύο κάρτες** — όχι checkbox | Ένα checkbox διαβάζεται ως ιδιότητα· εδώ είναι δύο δρόμοι με διαφορετικό αποτέλεσμα |
| Δ18 | Οι τοπικές κινήσεις ζουν σε **νέο πίνακα `LOCAL_MOVES`** — όχι στα NAT_LOADS | Το `Direction` έχει 2 τιμές και φιλτράρεται σε **20 σημεία / 4 αρχεία**· τρίτη τιμή = 20 έλεγχοι και σιωπηλά κενά |
| Δ19 | Το «παραδόθηκε» **υπολογίζεται** από την ημερομηνία, δεν γράφεται | Το `Status` δεν ισχυρίζεται ανεπιβεβαίωτο γεγονός· τίποτα δεν τρέχει προγραμματισμένα |

### Δ18 — ο πίνακας `LOCAL_MOVES`

| Στήλη | Τι κρατά |
|---|---|
| `move_date` + `sequence` | η μέρα του οδηγού και η σειρά της κίνησης (Δ7) |
| `driver_id` · `truck_id` · `trailer_id` · `partner_id` | ποιος την εκτελεί |
| `from_location_id` · `to_location_id` → `locations` | από πού, πού |
| `description` | «2 κιβώτια Άλμη», «μοίρασμα IFCO» |
| `pallets` · `time_from` · `time_to` | παλέτες και ραντεβού όπου υπάρχουν |
| `parent_nat_load_id` → `national_loads` | όταν εξυπηρετεί εθνικό (nullable) |
| `parent_order_id` → `orders` | όταν εξυπηρετεί διεθνές (nullable) |
| `status` · `notes` | |

Και οι δύο γονείς κενοί = **αυτοτελής** τοπική κίνηση (`ΚΑΡΑΜΟΛΕΓΚΟΣ → ΝΕΑ
ΜΟΥΔΑΝΙΑ`). Ίδιο μοτίβο δύο nullable FKs με τα NAT_LOADS — καμία νέα γραμματική.

Το `Θεσσαλονίκη → Αλεξάνδρεια` κάθεται εδώ φυσικά, χωρίς κατεύθυνση βορρά-νότου.

**Σημείωση για αργότερα:** αν το Trip Costs χρειαστεί να ξεχωρίζει τις μη
τιμολογούμενες κινήσεις, εδώ είναι η φυσική θέση μιας στήλης `billable`. Δεν
μπαίνει τώρα — η Δ3 κρατά την τιμολόγηση εκτός εβδομαδιαίου.

---

## 3. Μέρος Α — Weekly National

### 3.1 Δομή σελίδας

Τέσσερα στρώματα, από πάνω:

1. **Λωρίδα εβδομάδας + tally** — αμετάβλητα (`.wk3-mast`). Νέοι μετρητές:
   `χωρίς ανάθεση`, `ακάλυπτα κομμάτια`, `στην ουρά Pick Ups ↗`.
2. **Ενότητα ΕΘΝΙΚΑ** — τρεις στήλες `ΚΑΘΟΔΟΣ | ΑΝΑΘΕΣΗ | ΑΝΟΔΟΣ`, σπασμένες
   σε ημέρες με `.wk3-dayh` (υπάρχει ήδη στο intl v3).
3. **Ενότητα ΤΟΠΙΚΑ** — ίδιο πλάτος, ίδιες ημέρες, γραμμή τύπου οδηγός × ημέρα.
4. **Ενέργειες** — drag-drop, εκτύπωση, CSV· καλύπτουν **και τις δύο** ενότητες.

**Ταξινόμηση:** μέσα στην ημέρα κατά ώρα φόρτωσης όπου υπάρχει, αλλιώς σειρά
καταχώρησης. Μια γραμμή ανήκει στη μέρα της **πρώτης της φόρτωσης** — έτσι η
γραμμή του Κασάπη (Σάββατο, μόνο άνοδος 02/08) πέφτει σωστά και δεν χάνεται.

### 3.2 Εθνική γραμμή

Αμετάβλητη οπτικά. Δύο προσθήκες:

- **Σήμα ώρας** δίπλα στην ημερομηνία όπου υπάρχει ραντεβού (`10:00`, `23:00`).
- **Ρητό `—`** στη στήλη που λείπει. Στο Excel γράφεται `Χ` και σημαίνει «δεν
  υπάρχει σκέλος» — διαφορετικό από «δεν συμπληρώθηκε ακόμη». Η διαφορά είναι
  πληροφορία και πρέπει να φαίνεται.

### 3.3 Groupage φορτίο

Σήμα `▸ N · Xp` δίπλα στον προορισμό. Κλικ → ανάλυση με παλέτες ανά πελάτη και
**γέμισμα φορτηγού** `31/33` (πορτοκαλί ≥30, κόκκινο >33). Οι σημειώσεις ανά
στάση (`4 Σιάμπης χρεών · 3 ΑΣΝ όχι`) εμφανίζονται μέσα στην ανάλυση.

### 3.4 Τοπική γραμμή

Οδηγός + όχημα στην κορυφή, οι κινήσεις αριθμημένες από κάτω. Κάθε κίνηση:
`από → προς`, περιγραφή, ώρα αν υπάρχει, chip σύνδεσης αν εξυπηρετεί γονικό.

### 3.5 Η σύνδεση

| Πού | Τι δείχνει |
|---|---|
| Στην τοπική κίνηση | `εξυπηρετεί ▸ Μπάμπαλος · διεθνές` → κλικ πάει στη γραμμή του |
| Στο γονικό σκέλος | `καλύπτεται τοπικά · Γαλλής Δευ` → κλικ γυρνά πίσω |
| Στο γονικό, όσο εκκρεμεί | `χρειάζεται τοπικό` σε κόκκινο· μετράει στο tally |

Το `χρειάζεται τοπικό` **δηλώνεται ρητά** από τον χρήστη (δεξί κλικ → «Ανάθεση
σε τοπικό οδηγό»), δεν συμπεραίνεται. Αν δεν επιλεγεί οδηγός εκείνη τη στιγμή,
η ανάγκη καταγράφεται και μένει ορατή μέχρι να καλυφθεί.

---

## 4. Μέρος Β — Φόρμα National Orders

### 4.1 Η βάση: το σχήμα των διεθνών

Η φόρμα των διεθνών έχει ήδη τη σωστή δομή ([orders_intl.js:607](../../../modules/orders_intl.js)):
ενότητες `Loading Stops` και `Delivery Stops`, επαναλαμβανόμενες μέχρι 10, όπου
**κάθε στάση** έχει τοποθεσία, παλέτες και ημερομηνία. Οι εθνικές παραγγελίες
υιοθετούν το ίδιο σχήμα αυτούσιο. Δεν εφευρίσκουμε δεύτερη γραμματική.

### 4.2 Ο διακόπτης δεν είναι για τα σημεία — είναι για τους πελάτες

| Τρόπος | Στάσεις | Πελάτες | Αποτέλεσμα |
|---|---|---|---|
| **Ένας πελάτης** | πολλές | ένας | **1** παραγγελία |
| **Πολλοί πελάτες — groupage** | πολλές | πολλοί | **1 παραγγελία ανά πελάτη** |

**Ένας πελάτης:** πελάτης + αξία, μετά `ΣΗΜΕΙΑ ΦΟΡΤΩΣΗΣ` και `ΣΗΜΕΙΑ ΠΑΡΑΔΟΣΗΣ`
— ακριβώς όπως τα διεθνή, με προσθήκη στήλης «σημείωση».

**Πολλοί πελάτες:** το «ένα» σκέλος (κοινό, επαναλαμβανόμενο κι αυτό), και από
κάτω κουτιά πελατών. Κάθε κουτί: πελάτης, αξία, και μέσα οι στάσεις του με
τοποθεσία, παλέτες, ημερομηνία, σημείωση.

### 4.3 Δύο έλεγχοι που τρέχουν όσο γράφεις

- **Γέμισμα φορτηγού** `31 / 33`, πορτοκαλί ≥30, κόκκινο >33.
- **Ισοζύγιο παλετών**: `φόρτωση = παράδοση ✓`, ή κόκκινο
  `φόρτωση 31 ≠ παράδοση 28`. Πιάνει το λάθος τη στιγμή που γίνεται.

Το όριο των 10 σημείων ανά πλευρά επιβάλλεται από τη φόρμα με μήνυμα — ποτέ
σιωπηλή απώλεια (Α6).

### 4.4 Ακολουθεί την κατεύθυνση

| Direction | «Ένα» πεδίο | Επαναλαμβανόμενα |
|---|---|---|
| `North→South` (ΚΑΘΟΔΟΣ) | Σημείο φόρτωσης | **Παραδόσεις** |
| `South→North` (ΑΝΟΔΟΣ) | Σημείο παράδοσης | **Φορτώσεις** |

### 4.5 Πριν την καταχώρηση

Πλαίσιο προεπισκόπησης: «**3** παραγγελίες θα δημιουργηθούν», με τη λίστα και
τα σημεία κάθε μιας. Το κουμπί λέει `Καταχώρηση 3 παραγγελιών`. Καμία έκπληξη
μετά το κλικ.

---

## 5. Μέρος Γ — Ευρήματα αρχιτεκτονικής

Ελέγχθηκαν: `modules/orders_natl.js`, `modules/weekly_natl.js`,
`docs/worker/petras-tms-backend-staging-2026-08-05.js`, `ARCHITECTURE.md`,
`docs/SCHEMA.md`.

### ✅ Τι κουμπώνει

- **Τοποθεσίες:** τα NAT_LOADS **έχουν** `Pickup/Delivery Location 1..10` ως
  links στον Worker (γρ. 1381-1412). Δεν φαίνονται στο `fields` μπλοκ γιατί
  είναι FKs — δουλεύουν κανονικά.
- **ORDER_STOPS:** υπάρχει `Pallets` ανά στάση και `reverseLinks` προς
  `national_loads`. Η ανάλυση ανά σημείο έχει πού να ζήσει.
- **Πολλαπλά NO:** `Client` στα NAT_ORDERS είναι link ένας-προς-έναν, που
  ταιριάζει απόλυτα με «μία παραγγελία ανά πελάτη».

### 🔴 Α1 — Οι παλέτες ανά στάση ισοπεδώνονται

`orders_natl.js:961` και `:965` γράφουν `pallets: _pals` — **το σύνολο της
παραγγελίας σε κάθε στάση**. Ο Σκλαβενίτης με 20p σε 3 σημεία θα γράψει
20+20+20 = 60p στα ORDER_STOPS. Το ίδιο και στις γρ. 646-647 για τα NO stops.

**Χωρίς αυτή τη διόρθωση, η ανάλυση της §3.3 δείχνει λάθος νούμερα.**
Απαιτείται: η φόρμα να περνά παλέτες **ανά στάση** στο `stopsSave`.

**Δεν είναι συστημικό — είναι μόνο στα εθνικά.** Τα διεθνή το κάνουν ήδη σωστά:
`orders_intl.js:1435` και `:1442` γράφουν `pallets: parseFloat(pal) || 0` ανά
στάση, και αθροίζουν για το σύνολο (`:1513-1514`). Τα εθνικά έμειναν πίσω από το
πρότυπο του σπιτιού. Η διόρθωση είναι ευθυγράμμιση, όχι νέα σχεδίαση.

### 🔴 Α2 — Ένα NO → ένα NAT_LOAD, άρα τρεις γραμμές αντί για μία

`_syncNationalLoad` βρίσκει/φτιάχνει NL με `{Source Record}="${noId}"`
(`orders_natl.js:873`, `:913`). Τρεις παραγγελίες → **τρία NAT_LOADS** → τρεις
γραμμές στο εβδομαδιαίο. Αντιφάσκει με τη Δ9 («μία συμπτυγμένη γραμμή»).

Και δεν λύνεται με ένα NL που δείχνει σε τρία NO: ο Worker ορίζει
**πολυμορφικό γονέα με CHECK** — `source_order_id` *ή* `source_cons_load_id`,
ποτέ και τα δύο, και **ένα** το καθένα (γρ. 1334-1341, 1383-1386).

**Τρεις δρόμοι:**

| | Πώς | Κόστος | Ρίσκο |
|---|---|---|---|
| **Γ1** *(σύσταση)* | Η φόρμα φτιάχνει **CONSOLIDATED LOAD** για τη φόρτωση· το CL γεννά **ένα** NL (`source_cons_load_id`) | Μεσαίο — υπάρχουσα διαδρομή, ήδη δοκιμασμένη από το Pick Ups | Χαμηλό |
| Γ2 | Ομαδοποίηση **στην προβολή** με κοινό `Groupage ID` | Χαμηλό στο backend, ψηλότερο στο `weekly_natl.js` | Μεσαίο — η ομάδα δεν υπάρχει στη βάση |
| Γ3 | Νέος πίνακας ζεύξης NL ↔ NO | Υψηλό — αλλαγή schema + RLS | Υψηλό |

**ΑΠΟΦΑΣΗ (owner, 2026-08-10): Γ1.**

---

## 5.1 Γ1 — επαλήθευση αλυσίδας πεδίο-προς-πεδίο

Ελέγχθηκε κάθε πίνακας στον χάρτη του Worker
(`docs/worker/petras-tms-backend-staging-2026-08-05.js`).

### 🔴 Το εύρημα που αλλάζει την υλοποίηση

**Τα CONSOLIDATED LOADS δεν έχουν link προς NATIONAL ORDERS.** Τα links τους
είναι μόνο: `Client`, `Truck`, `Trailer`, `Driver`, `Partner`,
`Loading Location 1..10`, `Delivery Location 1..10`. Κανένα `reverseLinks`,
κανένα alias προς παραγγελίες.

Ο **μόνος** δρόμος από ένα CL πίσω στις παραγγελίες του είναι τα
**GROUPAGE LINES**, που κρατούν και τα δύο άκρα:

```
"Linked National Order":   { column: "national_order_id", table: "national_orders" }
"Linked Consolidated Load":{ column: "cons_load_id",      table: "consolidated_loads" }
```

**Συνέπεια:** η φόρμα **πρέπει** να γράψει και GROUPAGE LINES. CL χωρίς GL είναι
ορφανό — κανείς δεν μπορεί να πει ποιες παραγγελίες περιέχει, η τιμολόγηση δεν
έχει ίχνος, και ο κανόνας «τα GL δεν διαγράφονται ποτέ» δεν έχει τι να
προστατεύσει.

Αυτό **δεν** είναι κακό νέο: σημαίνει ότι το Γ1 χρησιμοποιεί την πλήρη υπάρχουσα
αλυσίδα, την ίδια που χτίζει το Pick Ups με drag-drop. Η φόρμα την αυτοματοποιεί.

### Η αλυσίδα που πρέπει να γράφεται

```
NAT_ORDERS  ×3   (ένα ανά πελάτη)
     │  Client, Direction, Goods, Price, Pallets,
     │  Temperature °C, Pallet Exchange, Loading/Delivery DateTime,
     │  Pickup/Delivery Location 1..10, Notes, Reference
     ↓
GROUPAGE_LINES  ×6   (ένα ανά στάση)
     │  Pallets, Loading Date, Delivery Date, Direction, Status,
     │  Goods, Temperature C, Notes
     │  Loading Location (ΜΙΑ), Delivery Location (ΜΙΑ)
     │  Linked National Order  → η παραγγελία
     │  Linked Consolidated Load → το φορτηγό
     ↓
CONSOLIDATED_LOADS  ×1
     │  Name, Date, Direction, Status, Total Pallets, Goods,
     │  Temperature C, Loading/Delivery DateTime, Notes,
     │  Groupage ID, Is Groupage, Pallets 1..10
     │  Loading/Delivery Location 1..10
     │  Client, Truck, Trailer, Driver, Partner
     ↓
NAT_LOADS  ×1
        source_cons_load_id (Source Type = 'Groupage')
        Pickup/Delivery Location 1..10

ORDER_STOPS  (παράλληλα)
        Parent Nat Order → national_orders
        Parent Nat Load  → national_loads
        Location, Client at Stop, Pallets, DateTime, Stop Type, Stop Number
```

### ✅ Επαληθεύτηκαν

| Έλεγχος | Αποτέλεσμα |
|---|---|
| `Pallets 1..10` στα CL αντιστοιχούν σε `Delivery Location 1..10` | ✅ υπάρχουν και τα δύο |
| CL έχει links ανάθεσης (Truck/Trailer/Driver/Partner) | ✅ |
| CL έχει `Is Groupage` + `Groupage ID` **εγγράψιμα** | ✅ scalar πεδία |
| NAT_LOADS δέχονται `source_cons_load_id` ως γονέα | ✅ πολυμορφικό, CHECK |
| GL έχουν **μία** Loading + **μία** Delivery Location | ✅ → ένα GL ανά στάση |
| ORDER_STOPS έχουν `Pallets` **ανά στάση** | ✅ |
| ORDER_STOPS έχουν `Client at Stop` | ✅ **λύνει το Α4** |

### 🟠 Τρία σημεία που θέλουν προσοχή

**Ο1 — `Groupage ID` είναι derived στα GL, γράψιμο στα CL.** Ο Worker το δηλώνει
ρητά: στα GROUPAGE LINES τα `Groupage ID`, `Week Number`, `Total Pallets` είναι
formula/rollup και **λείπουν επίτηδες** από τον χάρτη. Στα CONSOLIDATED LOADS το
`Groupage ID` είναι κανονικό scalar (`groupage_id`). Γράψε το **μόνο** στο CL.

**Ο2 — ημιτελής καταχώρηση = ορφανό CL.** Αν η δημιουργία σπάσει στη μέση (π.χ.
γράφτηκε το CL, έπεσαν τα GL), μένει φορτίο χωρίς παραγγελίες. Χρειάζεται:
σειρά γραφής **NAT_ORDERS → GL → CL → NL**, και επέκταση του `cleanupOrphans()`
με έλεγχο «CL χωρίς κανένα GL».

**Ο3 — το Α4 λύθηκε.** Το `Client` στα NAT_LOADS μένει απλό κείμενο για εμφάνιση
(`Σκλαβενίτης +2`), αλλά **κανένα δεδομένο δεν χάνεται**: ο πραγματικός πελάτης
κάθε στάσης ζει στο `ORDER_STOPS.Client at Stop`. Δεν χρειάζεται νέο πεδίο.

### 🔴 Α3 — Το εβδομαδιαίο κόβει στα 5 σημεία

`weekly_natl.js:110-111` ζητά μόνο `Pickup/Delivery Location 1..5`, ενώ ο
renderer κάνει loop **1..10** (`:735`, `:747`) και το schema έχει 10. Φόρτωση
με 6+ σημεία εμφανίζεται κομμένη, **σιωπηλά**.

Υπαρκτό bug, ανεξάρτητο από το νέο σχέδιο. Διόρθωση: μία γραμμή.

### 🟠 Α4 — `Client` στα NAT_LOADS είναι κείμενο

Ο Worker το σημειώνει ρητά: *plain text on this base, NOT a link* (γρ. 1352).
Με τρεις πελάτες σε ένα φορτίο χρειάζεται σύμβαση — π.χ. `Σκλαβενίτης +2`, με
την πλήρη λίστα στα ORDER_STOPS. Δεν είναι blocker, είναι επιλογή εμφάνισης.

### 🔴 Α7 — Οι εθνικές παραγγελίες δεν φτάνουν ποτέ σε `Delivered`

Το Invoicing τραβά NAT_ORDERS με
`OR({Status}="Delivered",{Status}="Invoiced",{Invoiced}=1)` (`invoicing.js:155`).

Αναζήτηση σε όλο το repo για εγγραφή αυτού του πεδίου:

| Πού γράφεται `Status` σε NAT_ORDERS | Τιμή |
|---|---|
| `weekly_natl.js:1120` | `'Assigned'` |
| οπουδήποτε αλλού | **καμία** |

Το `'Delivered'` γράφεται μόνο σε `TABLES.ORDERS` (`daily_ops.js:573`, `:626`) —
δηλαδή μόνο στα διεθνή. **Οι εθνικές παραγγελίες μένουν σε `Assigned` και δεν
εμφανίζονται ποτέ στη λίστα τιμολόγησης.**

Δεν το προκαλεί το Γ1 — προϋπάρχει. Το Γ1 όμως το **πολλαπλασιάζει**: αντί για
μία αόρατη παραγγελία ανά φορτίο, τρεις.

**Όριο του ελέγχου:** εξετάστηκε μόνο αυτό το repo. Αν trigger στη Supabase ή το
`petras-assign` γυρίζει το status, δεν φαίνεται από εδώ.

**ΛΥΣΗ (owner, 2026-08-10): υπολογίζεται, δεν γράφεται.** Το φίλτρο του Invoicing
επεκτείνεται ώστε να περιλαμβάνει εθνικές παραγγελίες με **περασμένη ημερομηνία
παράδοσης** που δεν έχουν τιμολογηθεί. Καμία εγγραφή στη βάση, τίποτα
προγραμματισμένο να τρέχει, και το `Status` δεν ισχυρίζεται ποτέ κάτι που κανείς
δεν επιβεβαίωσε — αν μια παράδοση μετατεθεί, δεν έχει γραφτεί ψέμα.

### 🟠 Α5 — Σύγκρουση ονόματος «Groupage»

Το υπάρχον checkbox `National Groupage` (`orders_natl.js:445`) ενεργοποιεί τη
διαδρομή GL → CL → NL μέσω Pick Ups. Το νέο groupage της φόρμας είναι
**διαφορετικό πράγμα με το ίδιο όνομα**. Πρέπει να ξεχωρίσουν ονομαστικά,
αλλιώς κανείς δεν θα ξέρει τι κάνει το καθένα σε έξι μήνες.

### 🟠 Α6 — Όριο 10 σημείων ανά πλευρά

Το schema έχει `Location 1..10`. Τρεις πελάτες με πολλά σημεία ο καθένας
μπορούν να το ξεπεράσουν. Η φόρμα πρέπει να μετρά και να σταματά στα 10, με
σαφές μήνυμα — όχι σιωπηλή απώλεια.

---

## 6. Τι ΔΕΝ αλλάζει

- Το `petras-assign` (National Pick Ups) — **καμία** επέμβαση.
- Το Weekly International — μόνο ένα chip, και **στο τέλος** (Δ14).
- Ο χρωματικός κώδικας: navy ιδιόκτητο, πράσινο συνεργάτης, κόκκινο ακάλυπτο.
- Η λωρίδα εβδομάδας, τα `.wk3-*`, η εκτύπωση, το CSV.
- Ο κανόνας «GL records δεν διαγράφονται ποτέ».

---

## 7. Ανοιχτά — χρειάζονται απόφαση πριν το πλάνο

1. Στο edit μιας από τις τρεις παραγγελίες: βλέπεις τη μία ή όλη την ομάδα;
2. Το όριο 33 παλετών **μπλοκάρει** την καταχώρηση ή απλώς προειδοποιεί;
   (Το ίδιο και για το ισοζύγιο φόρτωσης ≠ παράδοσης.)
3. Χρειάζεται ακόμη ο αυτόματος μερισμός παλετών (`orders_natl.js:779`) τη
   στιγμή που γράφονται ρητά;

**Έκλεισαν:**
- «Ημερομηνία ανά γραμμή ή κοινή;» → **ανά στάση** (Δ15), πρότυπο των διεθνών.
- «Α2: Γ1/Γ2/Γ3;» → **Γ1**, με την πλήρη αλυσίδα GL (§5.1).
- «Α4: σύμβαση για το Client;» → δεν χρειάζεται· `ORDER_STOPS.Client at Stop`
  κρατά την αλήθεια, το `NAT_LOADS.Client` είναι μόνο ετικέτα.

---

## 8. Σειρά υλοποίησης

| Φάση | Τι | Εξαρτάται από |
|---|---|---|
| 0 | Διόρθωση Α3 (fetch 1..10) — αυτόνομο, μία γραμμή | — |
| 1 | Διόρθωση Α1 (παλέτες ανά στάση) + `cleanupOrphans()` για CL χωρίς GL (Ο2) | — |
| 2 | Φόρμα National Orders — αλυσίδα Γ1: NAT_ORDERS → GL → CL → NL | Φάση 1 |
| 3 | Weekly National — ημέρες, groupage σύμπτυξη, ενότητα τοπικών | Φάση 2 |
| 4 | Συνδέσεις τοπικού ↔ γονικού (Δ6, Δ8) | Φάση 3 |
| 5 | Chip στο Weekly International (Δ14) | Φάση 4 |

Η Φάση 0 μπορεί να φύγει σήμερα — δεν περιμένει τίποτα.
