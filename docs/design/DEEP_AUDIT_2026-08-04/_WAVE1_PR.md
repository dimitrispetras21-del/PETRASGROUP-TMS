Κύμα 1 του deep audit της 4/8/2026: **15 από τα 230 ευρήματα**, όλα από την κατηγορία «λάθος νούμερο, ψέμα στην οθόνη, ή νεκρή λειτουργία». Καμία αλλαγή σχεδιασμού, καμία νέα δυνατότητα.

Πλήρης τεκμηρίωση: `docs/design/DEEP_AUDIT_2026-08-04/` (34 αρχεία σελίδων, `_ALL_FINDINGS.md`, `_CROSS_CUTTING.md`, `_ROADMAP.md`).

---

## ⚠️ Τι θα αλλάξει οπτικά για την ομάδα

**Διαβάστε αυτό πριν το merge — αλλιώς θα αναφερθεί ως bug.**

1. **Οι αριθμοί εβδομάδας μετακινούνται κατά μία μονάδα** σε Dashboard, International Orders και My Performance. Στις 4/8 έδειχναν 31 ενώ τα Weekly planners έδειχναν 32. Σωστό είναι το 32.
2. **Το Expiry Alerts λέει τώρα «45 οχήματα με ληγμένο έγγραφο»** αντί για «45 documents expired». Ο αριθμός δεν άλλαξε — άλλαξε το τι δηλώνει ότι μετράει.
3. **Νέα κάρτα «FRC Expired» στο Maintenance Dashboard.** Εμφανίζει 4 ληγμένα πιστοποιητικά ψύξης που μέχρι τώρα δεν φαίνονταν πουθενά.
4. **Ο δείκτης του Error Log γράφει «Κεντρική καταγραφή: ΑΝΕΝΕΡΓΗ».** Δεν χάλασε κάτι — απλώς έλεγε ψέματα πριν, γιατί το Sentry DSN είναι κενό.
5. **Το βοηθητικό γκρι κείμενο είναι πιο σκούρο** παντού (`--text-dim`), για να περνά AA.

---

## Τι διορθώθηκε

| Εύρημα | Πρόβλημα | Λύση |
|---|---|---|
| **D-1 / OI-1 / PF-1** | Δύο τύποι εβδομάδας διαφωνούσαν· το φίλτρο των Orders τραβούσε σιωπηλά λάθος εβδομάδα | Μία `isoWeekNumber()`. **Μετρημένη επιλογή:** ISO ταιριάζει με το αποθηκευμένο `Week Number` σε **111/124** παραγγελίες, Sunday-start 99/124, ο παλιός τύπος 76/124. 6 tests |
| **SH-1** | Το sidebar δεν κυλούσε μόνο του και όριζε ύψος 1.750px σε 780px περιεχόμενο | `body: min-height → height:100vh`, που ενεργοποίησε το **ήδη υπάρχον** `overflow-y:auto` σε sidebar και content. Δώρο: sticky topbar |
| **MD-1** | Banner 57, κάρτες 24+18+11=53 | Τα ρυμουλκούμενα έχουν **FRC** αντί ΚΕΚ και το FRC δεν είχε κάρτα |
| **ME-1 / ME-2** | «documents» ενώ μετρούσε οχήματα· «30%» δίπλα σε «Valid 13» | Σωστές ετικέτες + αριθμητής «19/64» |
| **IN-2** | Η στήλη αριθμού παραγγελίας έδειχνε record id («Ta1Azv») | «(χωρίς αριθμό)» — παραδεκτό κενό αντί για ψεύτικο νούμερο |
| **AD-1** | «Sentry ON» με κενό DSN | Απαιτεί SDK **και** DSN |
| **LG-1 / LG-3 / LG-5** | Σφάλμα login έσβηνε σε 3″· κουμπί χωρίς κλείδωμα· Enter από παντού | Μήνυμα μέχρι την επόμενη πληκτρολόγηση· `disabled` εν πτήσει· Enter μόνο από τα πεδία |
| **WI-1 / WN-2** | Δύο KPI στο «loading…» για πάντα σε κενή εβδομάδα (0 requests σε 8″) | Το async block βγήκε από το `if(total>0)` + `_ccFallback()` |
| **CE-2** | Ο CEO timer έγραφε σε DOM άλλης σελίδας | Έλεγχος σελίδας, όπως ήδη κάνουν dashboard & ramp |
| **D-2** | Το κόκκινο banner δεν κλικαρόταν | Δύο `<button>` με χωριστό προορισμό |
| **LO-4** | Το Veroia Cross-Dock διαγραφόταν σαν κάθε άλλο σημείο | Guard + λουκέτο στη γραμμή |
| **WS-2 / ON-5** | «Try adjusting filters» σε βάση χωρίς φίλτρα και χωρίς εγγραφές | Ξεχωριστά μηνύματα + CTA. Μία αλλαγή, 6 σελίδες |
| **SH-4 / SH-5 / SH-12** | FAB χωρίς όνομα· toast πάνω στο FAB· 3 αγγλικά μηνύματα κελύφους | `aria-label`· toast στα 88px· ελληνικά (7 σελίδες) |
| **`--text-dim`** | 2.35:1, κάτω από AA | #64748B → 4.76:1. Επαληθεύτηκε: 0 χρήσεις σε σκούρα επιφάνεια |

---

## Τι επαληθεύτηκε και τι όχι

**Επαληθεύτηκε live** (τοπικός server, hard reload): sidebar/content scroll με μετρήσεις· mobile drawer· banner-κουμπί με screenshot· login (κενή υποβολή POST 1→0, μήνυμα ζωντανό στα 4″, αδέσποτο Enter 1→0, κουμπί disabled)· `currentWeekNumber` 31→32 και ταύτιση με `metrics._weekOf`· 6 week tests· `_ccFallback`· και οι δύο κλάδοι του empty state.

**ΔΕΝ επαληθεύτηκε end-to-end:** ο τοπικός server δεν περνά CORS στον Worker, οπότε οι σελίδες πέφτουν στο δικό τους «Failed to fetch» πριν αποδοθούν τα Command Center widgets, η κάρτα FRC και οι μετρητές του Expiry. Χρειάζεται ένα πέρασμα σε staging ή μετά το merge.

**Checklist μετά το deploy:**
- [ ] Dashboard, Orders και Performance δείχνουν την ίδια εβδομάδα με τα Weekly planners
- [ ] Maintenance Dashboard: 24+18+11+FRC = ο αριθμός του banner
- [ ] Weekly International σε κενή εβδομάδα: καμία κάρτα δεν μένει σε «loading…»
- [ ] Expiry Alerts: «19/64 χωρίς ληγμένο έγγραφο» κάτω από το ποσοστό
- [ ] Locations: το Veroia Cross-Dock δείχνει λουκέτο, όχι κάδο

---

## Τι ΔΕΝ μπήκε

- **Σύνδεση Pallet Upload** (PP-1) — 620 γραμμές που δεν έχουν τρέξει ποτέ μέσα στο shell· θέλει δοκιμή σε πραγματική παραγγελία με δελτίο πριν φανεί στο μενού.
- **National Pick Ups** (PU-1) — το `HTTP 401` δεν λύνεται από το TMS· εξαρτάται από την απόφαση για το petras-assign.
- **Τα 94 P2** και το πέρασμα design-system — Κύμα 2.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
