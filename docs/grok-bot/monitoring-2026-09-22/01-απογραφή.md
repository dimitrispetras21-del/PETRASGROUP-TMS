# 01 — Απογραφή: τι υπάρχει ΣΗΜΕΡΑ για αυτόματη παρακολούθηση (22/9/2026, βράδυ)

Πακέτο AUTOMATED MONITORING, βήμα 1. Session μόνο ανάγνωσης: repo (`main` = `8e5a76b`), ζωντανή βάση (SELECT με LIMIT, ως `postgres` μέσω MCP), έγγραφα `docs/grok-bot/00–13`. **Cloudflare: κανένα MCP δεν απάντησε** (`cloudflare-bindings` και `cloudflare-observability`: «requires re-authorization, token expired») — ό,τι λέγεται για CF προέρχεται από `wrangler.toml`, `08`, `DECISION_LOG:2022-2027` και δεν επαληθεύτηκε ζωντανά σε αυτό το session.

Τέσσερα επίπεδα, πάντα ξεχωριστά: **repo main** · **branch** · **deployed** (Worker 8cb4f167 / Pages) · **ζωντανή βάση**. Κάθε δυνατότητα: **επαληθευμένο** (μετρήθηκε σήμερα) · **υλοποιημένο αλλά ανεπαλήθευτο** (υπάρχει στον κώδικα, κανείς δεν το μέτρησε) · **σχεδιασμένο** (DRAFT/πρόταση).

---

## 1. Τι τρέχει ΠΡΑΓΜΑΤΙΚΑ μόνο του σήμερα

Μόνο μηχανισμοί **μέσα στη βάση** και ο **CI ratchet**. Τίποτα προγραμματισμένο (χρονικά) δεν υπάρχει πουθενά.

| Μηχανισμός | Επίπεδο | Απόδειξη | Κατάσταση |
|---|---|---|---|
| **Triggers βάσης: 25 σε 8 πίνακες** — `orders` 12 (`rt_sync_from_order`, `rt_create_from_order`, `rt_link_split`, `order_dates_follow`, `order_soft_delete_unlink`, `orders_invoice_mark_guard`, `national_load_follow_order`, `order_leg_*` ×5), `ct_round_trips` 2 (`rt_sync_to_orders`, `dl_sync_from_rt`), `ct_rt_legs` 2 (`rt_sync_legs`, `rt_reopen_on_leg`), `dl_entries` 2 (`dl_cash_lock`, `dl_cash_sync_entry`), `national_loads` 2, `national_orders` 2, `ct_cost_lines` 1 (`dl_cash_sync_line`) | ζωντανή βάση | `pg_trigger` SELECT 22/9 | **επαληθευμένο** (ύπαρξη· όχι ορθότητα) |
| **Triggers που γράφουν `audit_log`** με `actor='trigger:*'`, `role='system'`: `rt_sync_audit()` (SECURITY DEFINER, 013), legs (018/020), unlink (023), dates (028) | ζωντανή βάση | 14 ημέρες: `ct_round_trips` 177, `order_stops` 26, `national_loads` 19, `orders` 18, `dl_entries` 9, `ct_rt_legs` 3 γραμμές από trigger· `system` 527 | **επαληθευμένο** |
| **CHECK / FK constraints** — π.χ. `orders` 2 CHECK/27 FK, `ct_round_trips` 7/4, `pl_movements` 8/7, `dl_entries` 7/2, `ct_cost_lines` 4/4, `ct_cost_docs` 4/0, `ramp` 0/5, `national_orders` 0/26 | ζωντανή βάση | `pg_constraint` 22/9 | **επαληθευμένο** (ύπαρξη) |
| **CI `code-guards.yml`** → `tests/check-fail-open.sh` (ratchet: όχι νέο `.catch(() => [])`), σε push/PR στο `main` | repo main / GitHub Actions | αρχείο υπάρχει· **τελευταίο run ΔΕΝ ελέγχθηκε** (`gh` δεν υπάρχει στο PATH) | υλοποιημένο αλλά ανεπαλήθευτο |
| **Service worker** `sw.js` (`SW_VERSION 1790103900` στο main): app shell network-first, backend `no-store`, `skipWaiting`+`clients.claim`, μήνυμα `SW_ACTIVATED` | repo main / συσκευές | κώδικας `sw.js:10,71-98` | υλοποιημένο· συμπεριφορά ανά συσκευή ανεπαλήθευτη |
| **Offline queue** `core/api.js:85-169`: PATCH/POST σε `localStorage.tms_offline_queue` (max 50), flush στο `online` event και στο init (`:757`), έλεγχος σύγκρουσης πριν το PATCH | repo main / browser | κώδικας μόνο | υλοποιημένο αλλά ανεπαλήθευτο |
| **Retry** `_atRetry` (`api.js:252-340`): 403/4xx δεν ξαναδοκιμάζονται, εξάντληση → `logError('API retry exhausted')` | repo main | 8 γραμμές `app_errors` 22/9 «API retry exhausted: signal is aborted» — άρα η διαδρομή **γράφει** | **επαληθευμένο** (ότι φτάνει στη βάση) |
| **Undo buffer** `api.js:479-526` (`_undoSet`, μόνο από cache) | repo main | κώδικας | υλοποιημένο αλλά ανεπαλήθευτο |

**Δεν υπάρχει**: `pg_cron`, `pg_net` (`pg_extension` = `pg_stat_statements, pgcrypto, plpgsql, supabase_vault, uuid-ossp`)· schema `cron`/`monitoring`/`net` (`information_schema.schemata` = 0 γραμμές)· `scheduled()`/`[triggers] crons` στον Worker (grep κενό)· Sentry (DSN κενό, `config.js:36`)· Logpush/tail consumers.

## 2. Έλεγχοι που υπάρχουν αλλά τρέχουν ΧΕΙΡΟΚΙΝΗΤΑ

| Έλεγχος | Πώς τρέχει | Ποιος | Τελευταίο ίχνος εκτέλεσης | Κατάσταση |
|---|---|---|---|---|
| **40 SQL του `02-έλεγχοι.md`** (Α–Η: διαθεσιμότητα, σφάλματα, audit, Δ/Ε/ΣΤ/Ζ δεσμοί, Η μετά από deploy) | copy-paste SELECT σε SQL editor / MCP | Claude session | **μία φορά, 22/9** («τιμή 22/9»)· Ζ2, Ζ13, Ζ15, Δ4, ΣΤ3 ποτέ | επαληθευμένο ως εργαλείο, **καμία επανάληψη** |
| `docs/SYNC_MAP.md` E1–E14 (SQL δεσμών) | χειροκίνητα | Claude/owner | ημερομηνία ανά ενότητα στο αρχείο | υλοποιημένο |
| **Όψεις ελέγχου** `dl_v_rt_gap`, `pl_v_order_gate`, `ct_v_rt_pallet_gate`, `ct_v_rt_pnl`, `dl_v_balance` | `SELECT count(*)` όποτε κάποιος θυμηθεί | — | 22/9 (`01 §5`) | επαληθευμένο· κανείς δεν τις διαβάζει αυτόματα |
| **`scripts/check-change.sh`** (Μ0 πεδίο, Μ1 markers, Μ2 `node --check`, Μ3 bump `?v=`↔`SW_VERSION`, Μ4 πεδία↔χάρτης, Μ5 διαδρομές↔`*_PERMS`, Μ8 migrations, Μ9 writer, Μ10/Μ11 λήξη 14 ημ.· exit 0/1/2, ΓΚΡΙ = δεν ξέρω) | `scripts/check-change.sh [BASE] [HEAD]` | κανείς ακόμη | **μόνο στο branch `origin/draft/check-change`** (`5219647` + README `6623517`, 133 γραμμές)· **δεν υπάρχει στο main**, όχι hook, όχι CI | **σχεδιασμένο** (DRAFT· 12 §Γ-4) |
| **Critics** `tests/critics/*` (20 αρχεία: `run.js` fan-out 12 units, `contract/semantics/kanban.spec.js`, proof rigs expenses/invoicing/payroll/rota, `ratchet.js`, HAR replay) | `PW_BASE_URL=http://127.0.0.1:8788/ npm run critics` (χωρίς `PW_BASE_URL` αρνείται — `run.js:17-25`) | Claude session ανά παράδοση | νούμερα σε docs (expenses 894/894, payroll 215/215)· `tests/` άγγιξε `c16f097` 22/9 | υλοποιημένο· **τρέχει μόνο με άνθρωπο** |
| **e2e** `tests/e2e/*` (Playwright κατά του **ζωντανού** Pages/Worker) | `npm run e2e` | — | άγνωστο | υλοποιημένο, ανεπαλήθευτο |
| **Unit tests** `worker/test/*.test.mjs` (4: costs-line-rules, invoice-mark-guard, ledger-month, ledger-rules), `tests/*.test.js` (invoicing-price, orders-list ×2, payroll-format), `tests/test-runner.html` (browser) | `node --test` / άνοιγμα HTML· `npm test` **μόνο echo** | — | `c16f097` 22/9 (invoicing-price) | υλοποιημένο· **δεν τρέχουν σε CI** |
| **DKV parser** `tests/dkv/run*.js` (127+ cases) | `node tests/dkv/run.js` | — | 17–19/9 (memory) | υλοποιημένο |
| **Φρουρός των τριών** (3 strings στο deployed bundle, `CLAUDE.md`) | grep στο κατεβασμένο bundle πριν/μετά deploy | owner | 22/9 21:08 «12/1/1 πριν και μετά» (`08`) | επαληθευμένο για το deploy 8cb4f167 |
| `scripts/pre-deploy.sh` (bump + warn console.log/κλειδιά) | χειροκίνητα | owner | — | υλοποιημένο |
| Agent `elegktis` (`.claude/agents/elegktis.md`, read-only, SELECT) | μόνο όταν τον καλεί Claude session | Claude | 22/9 reviews (11 αρχεία `docs/grok-bot/reviews/2026-09-22-*`) | επαληθευμένο ως διαδικασία |
| Fact-Forcing Gate / ECC hooks | plugin τοπικού Claude Code· **όχι στο repo** (`.claude/settings.local.json`: 0 hooks) | μόνο ο υπολογιστής του owner | — | εκτός repo |

## 3. Μόνο πρόταση / DRAFT (κατάσταση κατά `12-εγκρίσεις.md`)

| # (12) | Τι | Πού | Κατάσταση 22/9 βράδυ |
|---|---|---|---|
| Γ-2 | Ρόλος βάσης `tms_reader` / `tms_monitor` (SELECT ρητή λίστα, `users` κανένα, `audit_log` χωρίς `*_data`) | `starter/03 §Β` DRAFT SQL | **σχεδιασμένο** — `pg_roles` σήμερα: μόνο Supabase defaults (`postgres`, `service_role`, `anon`, `authenticated`, `supabase_read_only_user` κ.λπ.)· **κανένας δικός μας ρόλος** |
| Γ-3 | `pg_cron` + schema `monitoring` + `monitoring.health_checks` + `health_checks_run()` | `02 §Εκτέλεση/Αποθήκευση` | **σχεδιασμένο** — extension όχι, schema όχι, function όχι (`pg_proc` 0) |
| Γ-4 | `check-change.sh` merge + (αργότερα) pre-push hook ή CI βήμα | branch `draft/check-change` | **σχεδιασμένο**, DRAFT σε branch |
| Γ-1-εκτ. | P1 επιλογή (i) `invocation_logs = false` | `08`, `wrangler.toml:43-53` | **ΕΚΤΕΛΕΣΤΗΚΕ 22/9 21:08** (deploy 8cb4f167 από main 2dfa287) — βλ. §4 |
| Γ-1 (ii) | Worker γράφει δική του γραμμή αιτήματος (JSON χωρίς headers) → ξαναζωντανεύουν Α4/Α5/Β4/Β5 | `08` | **σχεδιασμένο** (DRAFT) |
| Γ-7 | Ρόλος 2 (πακέτα διάγνωσης) | `skill-daily-readonly-audit.md §3` | μόνο μετά Γ-2/Γ-3 |
| Γ-9 | Grok connectors βάσης/CF | `03` | κλειστά μέχρι Γ-1/2/3· GitHub: βλ. §6 |
| Γ-10 | Νεκρός Worker `tms-api-proxy` | `01 §1` | απόφαση owner εκκρεμεί |
| 03 §Δ | Ρόλος TMS `auditor` (`"*": ["GET"]`, 3 σημεία + deploy) | `03` | **σχεδιασμένο** |
| Ν1 | Πιλότος Γ (ροή ανά 2 ημέρες, read-only) | `11` | ξεκίνησε: 2 ροές Grok 22/9 (`reviews/…-ροή-*`), DRAFT 046 από τη 2η |
| — | Migrations DRAFT: 030 fuel_lines, 032, 035, 036, **034 ΔΕΝ ΕΓΚΡΙΘΗΚΕ**, 046 (νέο, 8e5a76b) | `worker/migrations/` | σχεδιασμένο |
| Η1–Η5 (02) | Έλεγχοι «μετά από deploy» (SW ενεργό, Η3 φρουρός, Η4 migrations↔`pg_trigger`) | `02 §Η` | σχεδιασμένο — κανένα script |

## 4. Τι καταγράφεται και πού

### Frontend (browser)
- `logError(error, ctx)` (`core/utils.js:519-553`) — ο **μοναδικός** αγωγός: (α) `localStorage.tms_errors` (δακτύλιος, dedup)· (β) `console.error`· (γ) Sentry **ανενεργό** (DSN κενό)· (δ) `POST /app-errors` (`_postAppError`, `:574-608`): **max 20/φόρτωση**, **παραλείπεται σε localhost**, αποτυχία του post **σιωπηλή**, `sw_version` **δεν στέλνεται ποτέ**. Πηγές: `window.onerror`, `unhandledrejection` (`app.html:163-171`), `safeFetch` (`utils.js:693-709`), `api.js` (33 κλήσεις: retry, 403, offline queue, cache).
- Τι **δεν** φτάνει κεντρικά: `.catch(() => [])` που δεν έχουν μετατραπεί σε `safeFetch` (ratchet BASELINE στο `check-fail-open.sh`)· toasts (`ui.js:253 showError`, `:331 toast`) — μόνο οθόνη· `console.warn('[api] undo buffer capture failed')` (`api.js:506`) — μόνο console.
- **Σελίδα «Καταγραφή Σφαλμάτων»** (`error_log`, μόνο owner, `router.js:421-427`, `renderErrorLog` `utils.js:956`): διαβάζει **`localStorage.tms_errors` του ίδιου browser** — **ΟΧΙ** τον πίνακα `app_errors`. Το `GET /app-errors` (owner/management, `index.js:356,4760`) **δεν έχει κανέναν καταναλωτή στο front end** (grep: μόνο το POST). Άρα τα σφάλματα των άλλων χρηστών διαβάζονται **μόνο με SQL/curl**. **επαληθευμένο**
- `audit_trail.js:108` διαβάζει `GET /audit` (owner/management) — το `audit_log` **έχει** οθόνη.

### Worker (`worker/src/index.js`, deployed 8cb4f167 = main 2dfa287· **0 αλλαγές στο `worker/` μετά**, `git diff --stat 2dfa287..HEAD -- worker/` κενό)
- **54 `console.error/warn`** (γρ. 130–4724): `AUDIT WRITE FAILED` (:624), `APP ERROR INSERT FAILED` (:397), `UNKNOWN-FIELD LOG FAILED` (:668-671), facade GET/POST/PATCH/DELETE, COSTS/IMPORT/PALLETS/PRINT. Πάνε **μόνο** σε CF Workers Logs.
- **Cloudflare Workers Logs**: `[observability] enabled = true` **+ `invocation_logs = false`** από 22/9 21:08 (`wrangler.toml:43-53`, `08`, `DECISION_LOG:2022`): τα αυτόματα per-request events (`cf-worker-event`: μέθοδος, διαδρομή, status, χρόνοι, **headers**) **σταμάτησαν** (327/ώρα → 0, μέτρηση συντονιστή 21:12). Ό,τι μένει: **μόνο** `cf-worker-log` από `console.*` — **και αυτό είναι ανεπαλήθευτο**: το `08` γράφει «εκκρεμεί απόδειξη ότι ένα `cf-worker-log` φτάνει», και στις 7 ημέρες πριν το P1 **δεν** είχε βρεθεί κανένα (`01 §2β`). Retention 7 ημέρες· τα παλιά events με headers λήγουν ως 29/9. Συνέπεια: έλεγχοι Α4/Α5/Β4/Β5 του `02` = **ΓΚΡΙ** μόνιμα μέχρι την επιλογή (ii). Σε αυτό το session: **δεν επαληθεύτηκε** (MCP unauthorized).
- `audit(env, entry)` (`:611-627`) / `auditMany` (`:295`): μία γραμμή `audit_log` σε **επιτυχές** create/update/delete/batch του facade (:2430, :2802, :2851, :2881) + `/costs/*` (ct_settings, ct_rt_legs, ct_round_trips, ct_cost_lines, dl_entries, dl_import_batches) + `/pallets/*` (pl_movements create/update/delete/**confirm/reverse**, `pallet-sheets` upload). Αποτυχία εγγραφής audit → μόνο `console.error`, το αίτημα **επιτυγχάνει**. **Δεν καταγράφονται**: αποτυχίες (403/422/500), GET, και οι πίνακες `ct_cost_docs`, `ct_import_rules`, `ct_plate_aliases`, `local_moves`, `maint_plan`, `users` (`01 §3`).
- `logUnknownFields` (`:641-674`) → RPC `log_unknown_field` → `facade_unknown_fields` (read/write/filter, ανά πίνακα/label/ρόλο).
- `/health` (`:4748`): `{status:"ok", ts}` χωρίς auth, **χωρίς έκδοση**.

### Βάση (ζωντανή, SELECT 22/9)
- `audit_log`: **6.519** γραμμές, τελευταία 22/9 18:28 UTC. Ανά ημέρα (14 ημ.): 9/9 282 · 10/9 33 · 11/9 175 · 12/9 18 · 13/9 123 · 14/9 282 · 15/9 180 · 16/9 280 · 17/9 149 · 18/9 250 · **19/9 3 · 20/9 5** (Σ/Κ) · 21/9 288 · 22/9 188. Ανά ρόλο: dispatcher 1.234 · system 527 · owner 264 · accountant 227 · **management 4** (τελευταία 14/9). Ανά πίνακα (κορυφή): `order_stops` 663, `orders` 517, `ct_cost_lines` 367, `ct_round_trips` 76 + 177 trigger, `national_loads` 107.
- `app_errors`: **1.533**, τελευταία **22/9 12:48 UTC**. 14 ημ.: 9/9 6 · 11/9 6 · 13/9 1 · 14/9 8 · 15/9 8 · 17/9 3 · **18–21/9: 0** · **22/9: 11** (8 «API retry exhausted: signal is aborted without reason» + 3 «safeFetch: dashboard alerts: national loads: signal is aborted» — **1 actor, ίδιο λεπτό**: timeout/abort ενός browser, όχι σφάλμα βάσης). Η «σιωπή 5 ημερών» του `01` **έσπασε**: τα posts **φτάνουν**. `sw_version` NULL σε όλες.
- `facade_unknown_fields`: **281** γραμμές, τελευταία **17/9 04:56**, 2 τις τελευταίες 7 ημέρες. Άρα Β3 (νέα σιωπηλή απόρριψη) = 🟢 σήμερα.
- `rt_sync_audit`: **function** (SECURITY DEFINER), όχι πίνακας· γράφει `audit_log` με `actor='trigger:rt_sync'`.
- `supabase_migrations.schema_migrations`: σταματημένος 23/8 (`01 §3`) — η αλήθεια = κεφαλίδες `worker/migrations/*.sql` («ΕΚΤΕΛΕΣΤΗΚΕ» 037–043, 045· DRAFT 030/032/035/036· 034 ΔΕΝ ΕΓΚΡΙΘΗΚΕ) + `pg_trigger`.

### Service worker / offline
- Ο SW **δεν** έχει ουρά εγγραφών — μόνο cache app shell. Η ουρά offline ζει στο `core/api.js` (localStorage), flush στο `online` event· υπερχείλιση (50) → toast + `logError`. Καμία κεντρική ένδειξη «πόσες εγγραφές περιμένουν σε ποιο browser».

## 5. Πού καταλήγουν τα σφάλματα και ΠΟΙΟΣ ειδοποιείται σήμερα

**Κανείς, αυτόματα.** Ειλικρινά:

| Σφάλμα | Καταλήγει | Το βλέπει |
|---|---|---|
| JS στον browser χρήστη | `localStorage` του χρήστη + `app_errors` (αν < 20/φόρτωση, όχι localhost) | Ο owner **μόνο αν** ανοίξει τη σελίδα «Καταγραφή Σφαλμάτων» — και τότε βλέπει **μόνο τα δικά του** (localStorage)· τα `app_errors` των άλλων μόνο με SQL |
| Worker exception / `AUDIT WRITE FAILED` / `APP ERROR INSERT FAILED` | `console.error` → CF Workers Logs (αν φτάνουν — ανεπαλήθευτο) | Κανείς· CF dashboard μόνο αν κάποιος μπει· retention 7 ημ. |
| 403/422/500 προς χρήστη | toast στην οθόνη + (4xx/exhausted) `app_errors` | Ο χρήστης τη στιγμή εκείνη |
| Παραβίαση CHECK/FK/trigger guard (π.χ. 043 `orders_invoice_mark_guard`) | 422/500 στον Worker → toast | Ο χρήστης |
| Σιωπηλή απόρριψη πεδίου | `facade_unknown_fields` | Κανείς (καμία οθόνη, κανένα query) |
| Pages 404 / Worker down / βάση down | πουθενά | Ο πρώτος χρήστης στις 05:30 |
| CI ratchet κόκκινο | GitHub Actions (email GitHub στον owner, αν ενεργό — ΔΕΝ ΕΛΕΓΧΘΗΚΕ) | owner |

Το `skill-daily-readonly-audit.md §5` ορίζει P1–P4 + «ΜΗΧΑΝΙΣΜΟΣ» και πότε ειδοποιείται κάποιος — **κανένα κανάλι δεν έχει υλοποιηθεί** (ούτε email, ούτε chat, ούτε πίνακας `health_checks`). Δεν υπάρχει «τελευταίος επιτυχημένος έλεγχος» πουθενά.

## 6. Τι μπορεί να διαβάσει το Grok Bot σήμερα

| Πηγή | Πρόσβαση | Όριο | Κατάσταση |
|---|---|---|---|
| **GitHub** repo (δημόσιο) | token **με push** (ο owner το αποδέχθηκε, παρά τη σύσταση read-only του `03 §Α`) | Κανένα τεχνικό όριο εγγραφής· ο κανόνας «branch → ελεγκτής → main» είναι διαδικαστικός. Pages = `main` ⇒ ένα push στο `main` είναι deploy | επαληθευμένο ως πολιτική (owner)· scopes του token **δεν** ελέγχθηκαν εδώ |
| **Supabase** | ως `postgres` (superuser-like: `rolbypassrls=true`, `rolcreaterole=true`) με κανόνα «SELECT-only» **στο prompt** | Το όριο είναι **μαλακό** (`03`: «η οδηγία δεν είναι όριο δικαιωμάτων»)· `tms_reader` δεν υπάρχει· ο `postgres` βλέπει `users` (hashes) και `audit_log.*_data` | επαληθευμένο (`pg_roles`) |
| **Cloudflare** | μόνο docs (`search_cloudflare_documentation`) | Καμία ανάγνωση Worker/logs (P1 ως 29/9· token Workers Scripts Read δεν φτιάχτηκε) | κατά `03 §Γ` |
| **TMS login** | κανένα | — | κατά `03 §Δ` |
| **Προγραμματισμένη εκτέλεση** (cron/webhook/automation μέσα στο Grok Bot) | **ΑΓΝΩΣΤΟ** — δεν βρέθηκε τεκμηρίωση στα `docs/grok-bot`, δεν υποθέτουμε | Χωρίς αυτό, «ημερήσιος έλεγχος» = κάποιος πατά «τρέξε» κάθε πρωί | **ΑΓΝΩΣΤΟ — να ρωτηθεί ο owner/να δοκιμαστεί** |

Τι **μπορεί** άρα να κάνει σήμερα χωρίς νέα υποδομή: τα 30+ SQL των Β–Ζ του `02` (ως `postgres`, μαλακό όριο) και τα Η1/Η4 (repo)· **όχι** Α4/Α5/Β4/Β5 (CF), **όχι** αποθήκευση αποτελεσμάτων (δεν υπάρχει `monitoring.health_checks`), **όχι** ειδοποίηση.

## 7. Τι ΔΕΝ μπορώ να επαληθεύσω από αυτό το session

1. **Deployed Worker vs main**: το `08`/`DECISION_LOG` λένε 8cb4f167 = main 2dfa287 (22/9 21:03 +03:00), και το `worker/` δεν άλλαξε στα 5 επόμενα commits (ως 8e5a76b) — άρα **πηγή = deployed** σε επίπεδο repo, αλλά **δεν** κατέβασα το bundle (CF MCP κλειστό). Byte-ισότητα ούτως ή άλλως αδύνατη (wrangler re-bundle, `CLAUDE.md`).
2. **`invocation_logs=false` ζωντανά** και **αν φτάνει έστω ένα `cf-worker-log`**: μόνο από τη μέτρηση του συντονιστή 21:12· εδώ 0 πρόσβαση.
3. **Live schema drift**: `pg_trigger` (25) ↔ αρχεία migrations ταιριάζουν ονομαστικά (045 `rt_reopen_on_leg` υπάρχει), αλλά **ορισμοί** (`pg_get_functiondef`) δεν συγκρίθηκαν σήμερα· `schema_migrations` νεκρός.
4. **SW σε συσκευές**: ποιος χρήστης τρέχει ποιο `app.html`/module· `sw_version` δεν ταξιδεύει (100% NULL)· αδύνατο κεντρικά.
5. **Τελευταίο run του CI** (`gh` απουσιάζει) και αν το GitHub στέλνει email αποτυχίας.
6. **Τι ζητούν οι connectors του Grok** (τύπος κλειδιού Supabase/CF, τοπικό ή server) — ερώτηση προς owner (`03 §Ε`).
7. **Front end deployed vs main**: τα 5 commits μετά το 2dfa287 άλλαξαν `invoicing.js`/`sw.js` (bump 1790103900)· αν το Pages τα σερβίρει ήδη **δεν** το μέτρησα (χωρίς `curl` στο Pages σε αυτό το session).

---

**Σε μία πρόταση:** σήμερα «τρέχουν μόνα τους» μόνο 25 triggers, οι constraints και ένας CI ratchet· όλα τα υπόλοιπα (40 SQL, critics, tests, φρουρός, check-change) θέλουν άνθρωπο, τα σφάλματα καταλήγουν σε τρεις αποθήκες που **κανείς δεν διαβάζει αυτόματα** (`app_errors` χωρίς οθόνη, CF logs μισοτυφλά μετά το P1, `facade_unknown_fields` χωρίς οθόνη), και δεν υπάρχει ούτε ρόλος ανάγνωσης ούτε χρονοδιακόπτης ούτε κανάλι ειδοποίησης.
