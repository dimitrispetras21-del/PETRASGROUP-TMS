# Αξιολόγηση 6fda476 — 2026-09-22 (τέταρτη αξιολόγηση Β)
Commit: 6fda476 · branch `fix/rota-matched-import` · βάση main 24c58bd (merge-base ✓) · 4 commits (μόνο `modules/weekly_intl.js` + 2 rigs + shots + docs)
Εγχειρίδια: knowledge `7e4697980ebb` · Ελεγκτής: Claude Sonnet subagent, χωρίς το μήνυμα του συγγραφέα · Tokens: **153.472** subagent + ~8k ≈ **161k** (πάνω από το όριο 150k του 11 — diff 28 KB + πολλά σημεία κλήσης)

ΑΠΟΤΕΛΕΣΜΑ: **ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ**

## Μηχανικά (`check-change.sh 24c58bd 6fda476`)
Μ0–Μ3, Μ5, Μ8, Μ9 ΠΡΑΣΙΝΑ (SW 1790070183) · Μ4 ΓΚΡΙ 8 labels → **0 νέα write labels**: «Rotation ID»/«Group ID»/«Delivery DateTime»/«Loading DateTime»/«Matched Import ID» **υπάρχουν** στον χάρτη ORDERS (index.js:1206-1290), «Delivery Summary» derived/σκόπιμα εκτός και μόνο ανάγνωση, «Export»/«Import» τιμές Direction/strings rig · Μ10 ΓΚΡΙ (γνωστό) · Μ7: 2 rigs (rot-cands-sim με assertions, rota-matched-import-rig Playwright live/HAR) — **δεν εκτελέστηκαν**.

## Αιτιολογία (≤10)
1. (α) `_wiPanelRotaGo` (wt:3673-3684) καλεί `_wiRotAdd(parseInt(p.rowId), thisOid)`: η **αμετάβλητη** `_wiRotAdd` (base:4150-4174) γράφει `Rotation ID` **στην τρέχουσα γραμμή** (`atSafePatch(TABLES.ORDERS, thisOid, {'Rotation ID': pOid})`) με τιμή το order id του επιλεγμένου γονέα, και καλεί `rtOnOrderSaved(pOid)` **στον γονέα** — σωστή αντιστροφή, ίδια σημασιολογία παραμέτρων.
2. (α΄) Αν ο γονέας είναι ταιριασμένη εισαγωγή: `pOid = pr.orderId` (base:4150-4152) = order id της **ΕΙΣΑΓΩΓΗΣ**, όχι της εξαγωγής — ίδια σύμβαση με την εμπρόσθια κατεύθυνση, **όχι νέο ρίσκο**, αλλά αξίζει να το ξέρει ο owner (Rotation ID δείχνει σε εισαγωγή).
3. (β) «Ανάθεση…» από ταιριασμένη κάρτα → `_wiPanelAssign(matchedExportRowId,false)` (wt:4570-4577) = ταυτόσημη κλήση με το μενού της γραμμής εξαγωγής (base:3710)· το πάνελ έχει υπο-ενότητα «ΤΑΙΡΙΑΣΜΕΝΗ ΕΙΣΑΓΩΓΗ». Τίτλος γενικός «Ανάθεση» — όχι ρητό «του ζεύγους» (παρατήρηση UX, όχι ελάττωμα).
4. (γ) `_wiRotCands` έχει **μοναδικό καλούντα** το `_wiPanelRota` (:3632) + το rig — το −1 ημέρα/χωρίς slice δεν αγγίζει άλλη χρήση.
5. (δ) Νέο global μόνο `window._wiMatchedImpCtx` (:5305), απαραίτητο για inline `oncontextmenu`· `_wiRotParents` δεν εξάγεται — σωστά.
6. (ε) Escape/outside-click: νέο πάνελ → `_wiPanelKeydown/_wiPanelOutside` (base:3575-3581) καλούν **μόνο** `_wiPanelClose()`. Υπάρχον `_wiPanelAssign/_wiOpenPopover`: `_wiPopoverOutside` (:3179-3183) μόνο `_wiClosePopover()`, **κανένας keydown Escape handler** για το popover βρέθηκε. **Ο ισχυρισμός «Escape στο assign panel αποθηκεύει» ΔΕΝ επιβεβαιώθηκε στον κώδικα** — αν παρατηρήθηκε ζωντανά, χρειάζεται ξεχωριστή διερεύνηση (πιθανόν άλλη διαδρομή: blur/change handler σε select;). Δεν καταγράφεται ως [1].
7. Κλειδωμένες αποφάσεις: το −1 ημέρα επικαλείται σωστά το «lag παράδοσης» (owner 14/9)· ιστορικές γραμμές χωρίς Rotation ID ανεπηρέαστες· 0 Worker/migrations.
8. **Η νέα ενότητα οδηγεί ενεργά τον dispatcher στο σενάριο «σκέλος ΜΕΤΑ από Delivered γονέα»** — εκεί η αμετάβλητη `_wiRotAdd` καλεί `rtOnOrderSaved(pOid).catch(console.warn)` και σε κλειστό RT ο συγχρονισμός «μόνο φωνάζει», ενώ το toast «✓» βγαίνει ό,τι κι αν γίνει (base:4162-4165). Προϋπάρχον fail-open, **αυξημένη έκθεση**.
9. Το σενάριο-αφορμή (367→364) **δεν δοκιμάστηκε ζωντανά** από κανέναν — δηλωμένο εκκρεμές στο `2026-09-22-pantelis-rota-cands.md:85-86`.
10. Rigs: `rot-cands-sim.js` με assertions/exit(1)· `rota-matched-import-rig.js` θέλει ζωντανό URL — και τα δύο πρέπει να τρέξουν πριν το merge.

## Οι 6 ερωτήσεις Β
(α) Νέο δεξί κλικ σε ταιριασμένη εισαγωγή (4 ενέργειες), κατώφλι ρότας −1 ημέρα χωρίς όριο 6, νέα ενότητα «προσάρτηση ως σκέλος μετά από…» (αντίστροφη `_wiRotAdd`). (β) Μόνο `weekly_intl` (planning full = owner/dispatcher)· πίνακας `orders` πεδίο `Rotation ID` (ήδη γραφόμενο από dispatcher)· RT μέσω `rtOnOrderSaved`. (γ) Rigs 2 (μη εκτελεσμένα)· κανένας έλεγχος 02 για ρότα· ΣΤ1 (`after_data->>'split'`) θα πιάσει έμμεσα κομμένο κύκλο **μετά** το γεγονός. (δ) Κανένας παλιός έλεγχος γίνεται ανεπαρκής· το `rot-cands-sim` ενημερώθηκε. (ε) DECISION_LOG:1949-1970 και rota-cands doc συμφωνούν με τον κώδικα. (στ) Ζωντανή δοκιμή 367→364 με SELECT `rotation_id` + `ct_rt_legs` πριν το merge· σενάριο rig «γονέας = ταιριασμένη εισαγωγή» (ποιο pOid γράφεται)· έλεγχος τι βλέπει ο χρήστης όταν το RT είναι κλειστό (toast ✓ vs warn).

## Ευρήματα
1. **[5 μη δοκιμασμένη διαδρομή] P3 ΕΠΙΒΕΒΑΙΩΜΕΝΟ** — «σκέλος μετά από Delivered γονέα» χωρίς ζωντανή επιβεβαίωση· σε κλειστό RT ο dispatcher βλέπει «✓» ενώ το RT δεν συνδέθηκε. ΕΠΟΜΕΝΟ: ζωντανή δοκιμή πριν merge, SELECT μετά.
2. **[6 προϋπάρχον] P4** — `rtOnOrderSaved` fail-open με toast επιτυχίας στο `_wiRotAdd` (base:4162-4165), εκτός diff.
3. **[4 ασαφής] P4** — Rotation ID όταν ο γονέας είναι ταιριασμένη εισαγωγή δείχνει στην **εισαγωγή** (σύμβαση προϋπάρχουσα)· να επιβεβαιωθεί ότι είναι η επιθυμητή.
4. **[6 UX] P4** — τίτλος πάνελ «Ανάθεση» χωρίς «ζεύγους/εξαγωγής».
5. **ΔΕΝ ΕΠΙΒΕΒΑΙΩΘΗΚΕ** ο ισχυρισμός «Escape στο assign panel αποθηκεύει» — κώδικας δείχνει μόνο close· θέλει ξεχωριστή ζωντανή αναπαραγωγή (ποια ακριβώς ενέργεια έγραψε;).

## ΔΕΝ ΕΛΕΓΧΘΗΚΕ
Εκτέλεση rigs· screenshots· πλήρες `core/rt-feed.js`· ζωντανή συμπεριφορά Escape.
