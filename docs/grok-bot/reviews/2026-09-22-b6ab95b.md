# Αξιολόγηση b6ab95b — 2026-09-22 (πέμπτη αξιολόγηση Β)
Commit: b6ab95b · branch `fix/rota-cands-myday` · βάση main 55f101c (merge-base ✓) · 8 αρχεία (`core/rt-feed.js`, `modules/weekly_intl.js`, `sw.js`, `app.html`, 2 rigs, 1 doc, 1 png)
Εγχειρίδια: knowledge `7e4697980ebb` · Ελεγκτής: Claude Sonnet subagent, χωρίς το μήνυμα του συγγραφέα · Tokens: **144.151** subagent + ~6k ≈ **150k** (diff 14,7 KB — το κόστος πήγε στους 12 καλούντες της `rtOnOrderSaved` και στο Worker/033 context)

ΑΠΟΤΕΛΕΣΜΑ: **ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ**

## Μηχανικά (`check-change.sh 55f101c b6ab95b`)
Μ0–Μ3, Μ5, Μ8, Μ9, Μ11 ΠΡΑΣΙΝΑ (SW_VERSION 1790074357 = `?v=` app.html) · Μ4 **0 νέα labels** (244 κλειδιά) · Μ10 ΓΚΡΙ (γνωστό) · Μ2 επιβεβαιωμένο ανεξάρτητα (`node --check` σε rt-feed, weekly_intl, 2 rigs) · Μ7: `rt-close-sim.js` (νέο, assertions/exit 1) + `rota-matched-import-rig.js` (Playwright, ζωντανό) — **δεν εκτελέστηκαν**.

## Αιτιολογία (≤10)
1. **Παλιό** (base:420-422): `shouldClose` = Status του export (solo) ή του **ταιριασμένου import μόνο** (ζεύγος). Σενάριο περιστατικού (ζεύγος Delivered + σκέλος ρότας Pending) → **κλείνει**. **Νέο** (wt:439): `if (shouldClose && _rtOpenLegs(legsInfo, gathered).length) shouldClose = false` → **δεν κλείνει**. Ο ισχυρισμός [1] επιβεβαιώνεται.
2. `_rtOpenLegs` (wt:204-211) καθαρή συνάρτηση: Delivered/Cancelled = κλειστό, ό,τι άλλο **και άγνωστη εγγραφή** = ανοιχτό (αρχή 1, «δεν ξέρω» ≠ «παραδόθηκε»).
3. Η πύλη μπαίνει **μέσα** στη `rtOnOrderSaved`, άρα καλύπτει και τους **12** καλούντες (orders_intl, order-sync, 8+ σημεία weekly_intl: match/popover/split/rejoin/rota/import-group/merge/seg) — ενιαία διόρθωση, όχι μπάλωμα της ρότας.
4. **Σκέλη από άλλο ζεύγος**: `legsInfo = rtLegsForOrder(rec, gathered)` (wt:302) και `gathered = _rtGatherOrders(rec)` (wt:291) — γράφημα Group/Matched Import/Rotation ID από την τρέχουσα παραγγελία, **όχι** τα πραγματικά `ct_rt_legs` του RT. Σκέλος του RT εκτός αυτού του γράφου δεν εμποδίζει το κλείσιμο. Ο σχεδιασμός (base:96-100, 033:132-151) λέει ότι το γράφημα είναι συμμετρικό, άρα στην πράξη ταυτίζονται — αλλά **καμία ενεργή διασταύρωση** με το RT (εύρημα 1).
5. **Διπλή κλήση**: `_rtClosed(rt)` ελέγχεται πριν το PATCH (wt:324, 440) με `rt` που διαβάστηκε στην αρχή της ίδιας κλήσης. Σειριακά = idempotent· σχεδόν ταυτόχρονα = δύο PATCH closed (αβλαβές στα δεδομένα, ×2 στο audit). Εύλογη εξήγηση του ×2 του περιστατικού, **δεν επαληθεύτηκε** με τα raw audit rows· το commit δεν το αγγίζει.
6. **Ξανάνοιγμα**: η 033 ξανανοίγει μόνο μέσω trigger σε `insert/update of status, truck_id, …, rotation_id` του `orders` (033:229-232). Το front attach (`POST /costs/rt`, index.js:3104-3131, rt-rules.mjs:104-131) γράφει `ct_rt_legs` χωρίς να αγγίζει το `orders` row → **δεν ξανανοίγει**. **Εκτός scope** αυτού του commit — προϋπάρχον κενό (εύρημα 2).
7. **Rig** `rt-close-sim.js:6-8`: `readFileSync` + regex + `new Function` πάνω στο `core/rt-feed.js` → δοκιμάζει τη **γνήσια** `_rtOpenLegs`, όχι αντίγραφο ✓. 4 σενάρια: Pending κρατά ανοιχτό / όλα Delivered κλείνει / Cancelled δεν μπλοκάρει / άγνωστη = ανοιχτή. **Λείπει**: σκέλος εκτός γράφου (σημ. 4). Παρατήρηση: το regex σπάει σιωπηλά αν αλλάξει η υπογραφή (πετά `fn not found` — καλά, φωνάζει).
8. **weekly_intl** (wt:3629-3659): μόνο CSS `max-height: min(320px,45vh)` + λεζάντα «N υποψήφια» + «(N)» στους γονείς. `_wiRotCands`/`_wiRotParents` **ανέγγιχτες**· λεζάντα και λίστα διαβάζουν την ίδια `cands.length`/`parents.length` ✓. Ο ισχυρισμός [2] επιβεβαιώνεται.
9. **Κλειδωμένες αποφάσεις**: «κλεισμένο RT δεν αγγίζεται αυτόματα» (base:324-328) δεν παραβιάζεται — το commit αλλάζει μόνο την απόφαση **να κλείσει**. Το ιστορικό RT-1171 **δεν** διορθώνεται από τον κώδικα ✓ (σωστά: δεδομένα = owner/SQL, δηλωμένο στο doc :75-77).
10. Όριο `_rtGatherOrders` ≤20 εγγραφές: σε πολύ μεγάλη ομάδα σκέλος μπορεί να λείψει από `gathered` → και από την πύλη· μερικώς καλυμμένο από το προϋπάρχον `_rtWarn` (>20 legs).

## Οι 6 ερωτήσεις Β
(α) Κλείσιμο RT μόνο όταν **κανένα** σκέλος του γράφου δεν είναι ανοιχτό (ήταν: μόνο το ζεύγος-άγκυρα)· πάνελ ρότας ψηλότερο με μέτρηση. (β) Όλες οι σελίδες που καλούν `rtOnOrderSaved` (orders_intl, weekly_intl, order-sync)· πίνακας `ct_round_trips.status` (PATCH closed λιγότερο συχνά)· ρόλοι owner/dispatcher. (γ) Νέο rig καλύπτει την πύλη· **κανένας έλεγχος 02** για «closed RT με ανοιχτό σκέλος» — ο ΣΤ4/Ζ (RT σκέλη) δεν το πιάνει· πρόταση νέου SQL: `ct_round_trips.status='closed'` με σκέλος σε `orders.status NOT IN ('Delivered','Cancelled')`. (δ) Κανένας παλιός έλεγχος γίνεται ανεπαρκής· η παραδοχή «RT κλείνει με το ζεύγος» στο skill-tms-knowledge πρέπει να ενημερωθεί (τύπος 6). (ε) DECISION_LOG/rota-cands doc συμφωνούν με τον κώδικα· η 033 §reopen περιγράφει μόνο το trigger, όχι το attach path. (στ) Rig σενάριο «σκέλος εκτός γράφου»· ζωντανή δοκιμή 367→364 με SELECT `ct_round_trips.status` + `ct_rt_legs` μετά· έλεγχος timestamps των 2 PATCH του περιστατικού· απόφαση owner για reopen-on-attach.

## Ευρήματα
1. **[3 κενό κάλυψης] P3** — η πύλη κρίνει το γράφημα `_rtGatherOrders`, όχι τα `ct_rt_legs` του RT (wt:204-211, 439). Ψευδο-αρνητικό θεωρητικό (σκέλος του RT εκτός γράφου). ΒΕΒΑΙΟΤΗΤΑ μεσαία. ΕΠΟΜΕΝΟ: rig σενάριο ή ρητή δήλωση ότι η αρχιτεκτονική εγγυάται ισοδυναμία.
2. **[2 πιθανό, προϋπάρχον] P3** — attach Pending σκέλους σε **ήδη closed** RT μέσω `POST /costs/rt` δεν ξανανοίγει (μόνο ο trigger 033 σε update `orders`). Θα αναπαράγει το σύμπτωμα RT-1171 μελλοντικά. ΒΕΒΑΙΟΤΗΤΑ υψηλή. ΕΠΟΜΕΝΟ: ερώτηση owner αν θέλει reopen-on-attach στον Worker (εκτός αυτού του commit).
3. **[2 πιθανό] P4** — χωρίς προστασία race σε ταυτόχρονες `rtOnOrderSaved` → ×2 PATCH closed (audit μόνο). ΒΕΒΑΙΟΤΗΤΑ χαμηλή-μεσαία. ΕΠΟΜΕΝΟ: timestamps των 2 audit rows του περιστατικού.
4. **[5 μη δοκιμασμένη διαδρομή] P4** — ζωντανή επιβεβαίωση στο Pages δηλωμένη εκκρεμής από τον συγγραφέα (doc :85-86)· rigs δεν εκτελέστηκαν από τον ελεγκτή.
5. **[6 τεκμηρίωση] P4** — `skill-tms-knowledge.md` λέει «RT κλείνει με Delivered του ζεύγους»· από αυτό το commit ισχύει «… και κανένα σκέλος ανοιχτό».
6. **[3 κενό κάλυψης] P4** — κανένας σταθερός έλεγχος 02 για «closed RT με ανοιχτό σκέλος» (θα είχε πιάσει το RT-1171 πριν το περιστατικό).

Κανένα P1/P2 → δεν μπλοκάρεται.

## ΔΕΝ ΕΛΕΓΧΘΗΚΕ
Εκτέλεση rigs (μόνο `node --check`)· ζωντανό Pages· raw audit rows του ×2 PATCH· αν υπάρχουν κι άλλα ιστορικά RT «closed + ανοιχτό σκέλος» (θέλει SELECT — δεν έγινε σε αυτή την αξιολόγηση)· συμπεριφορά σε ομάδα >20 σκελών.
