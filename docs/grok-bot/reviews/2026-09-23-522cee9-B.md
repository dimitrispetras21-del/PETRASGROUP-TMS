# Αξιολόγηση 522cee9 (ρόλος 1 — Β: κώδικας / αλυσίδα / ισχυρισμοί) — 2026-09-22 23:14 EEST
Commit: 522cee9b95716b4aa62f2245ca457e78d6a923c6 · «tms-auditor: τεχνικός ελεγκτής TMS — γράφος, 69 έλεγχοι, μηχανή…»
Base: 168f6a7 · Branch: origin/feat/tms-auditor
Εγχειρίδια: knowledge 7e4697980ebb · CLAUDE 72a4c258b5db · Ελεγκτής: Claude Opus 5 (1M), Claude Code
Diff: 143 αρχεία, 2.550 KB (34.603+/35−) — κώδικας εκτέλεσης μόνο 5 αρχεία (worker/src/index.js +83/−7, core/api.js +89/−26, core/utils.js +4, app.html, sw.js)· τα υπόλοιπα = DRAFT SQL, JSON γράφου, tests, docs.
Πηγές: GitHub@522cee9 (worktree detached), τοπικά tests (node --test). Καμία βάση, κανένα Cloudflare, κανένα login.

ΑΠΟΤΕΛΕΣΜΑ: ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ
(0 ευρήματα P1/P2 ΓΕΓΟΝΟΣ. Ο φρουρός των τριών είναι ανέπαφος: `order_stops: [...,"DELETE"]` για dispatcher `worker/src/index.js:553`· `"VS CD Date": "cross_dock_date"` :1317· WORKSHOPS `Country`/`Aliases`/`"VAT Number"→tax_id`/`"Legal Name"→legal_name` :960-963.)

## ΙΣΧΥΡΙΣΜΟΙ ΣΥΓΓΡΑΦΕΑ — τι μέτρησα ο ίδιος
Εντολές (worktree `…/scratchpad/rv-b`, `npm install` μόνο για `@electric-sql/pglite`):
- `node --test tms-auditor/test/*.test.mjs` → `ℹ tests 24 · pass 24 · fail 0` **ΕΠΑΛΗΘΕΥΤΗΚΕ (24/24)**
- `node --test worker/test/*.test.mjs` → `ℹ tests 31 · pass 31 · fail 0` **ΕΠΑΛΗΘΕΥΤΗΚΕ (31/31, 26 υπάρχοντα + 5 νέα)**
- `node --test tests/level-a-api.test.mjs` → `ℹ tests 5 · pass 5 · fail 0` **ΕΠΑΛΗΘΕΥΤΗΚΕ (5/5)**
- `node --test tests/*.test.js` (υπάρχοντα, αμετάβλητα) → `ℹ tests 32 · pass 32 · fail 0` **ΕΠΑΛΗΘΕΥΤΗΚΕ**
- `bash tests/check-fail-open.sh` → `✓ PASS: 0 fail-open data fetches, at baseline.` **ΕΠΑΛΗΘΕΥΤΗΚΕ**
- Γράφος: `tms-graph.json` → `merged nodes 758 edges 1974 · orphan ends: 0` **ΕΠΑΛΗΘΕΥΤΗΚΕ**
- Κατάλογος: `loadChecks()` → `checks 69 enabled 66 errors 0` **ΕΠΑΛΗΘΕΥΤΗΚΕ**
- «Η γραμμή δεν περιέχει Authorization/cookies/body/query/ονόματα»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** — `reqLineOf` (`worker/src/index.js:4766-4784`) γράφει μόνο `req/method/url.pathname/status/ms/role/table/action/rec/app`· `path` = `pathname` χωρίς `search`· `app` φιλτραρισμένο με `^[0-9a-z._-]{1,20}$`.
- «role μόνο από επαληθευμένο JWT»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** — `getCaller` = `jwtVerify(token, env.JWT_SECRET)` (:152-157)· αποτυχία → `catch (_) { role = null }` (:4818), δεν αλλάζει την απόκριση.
- «Ασφαλές στη σειρά deploy»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** — το front στέλνει `x-tms-req` μόνο αφού δει `x-tms-worker-req` (`core/api.js:378,386,393`)· `_tmsReqOk` δεν γράφεται πουθενά μόνιμα (βλ. όμως εύρημα 4).
- «409 passthrough του 046 ανέπαφο»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** — `new Response(res.body, res)` (:4813) διατηρεί status· τα 9 σημεία 409 (:3289, 3547, 3704, 3902 κ.λπ.) δεν αγγίχτηκαν.
- «049 fallback»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** — `insertWithReqId` (:617-628) ξαναγράφει ΧΩΡΙΣ τη στήλη και κάνει `console.error("REQ_ID COLUMN MISSING — run migration 049")`· το regex `/req_id|'role'/` καλύπτει και τις δύο νέες στήλες του 049.
- «routines χωρίς connector Supabase, μόνο Gmail»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** — `tms-auditor/routines/COMMON.md` §Σύνδεση (ρητή απαγόρευση connector + μόνο `psql` ως `tms_monitor_writer`).
- «διάγνωση ανεξάρτητη παρόχου»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** — `investigate/diagnose.mjs#PROVIDERS` (claude-routine / mock / anthropic-api=stub που επιστρέφει `NOT CONFIGURED`).
- «69 έλεγχοι, 66 ενεργοί, 0 ορφανά άκρα, 758/1.974»: **ΕΠΑΛΗΘΕΥΤΗΚΕ** (πάνω).
- «Καμία νέα εξαίρεση δεν μετατρέπει 200 σε 500»: **ΔΕΝ ΕΠΑΛΗΘΕΥΤΗΚΕ ΠΛΗΡΩΣ** — η γραμμή `const out = new Response(res.body, res)` (:4813) είναι ΕΚΤΟΣ try/catch. Το μόνο 204 του Worker είναι το OPTIONS (:4825) που επιστρέφει νωρίτερα (:4812), άρα το γνωστό «204 με σώμα» δεν ισχύει· δεν βρήκα άλλη διαδρομή που να σκάει. Η τελική απόδειξη θέλει παραγωγή.

## ΕΥΡΗΜΑΤΑ

1. **ΤΙ:** Το Επίπεδο Α δεν καλύπτει τις διαδρομές εκτός facade — `/costs/*`, `/pallets/gate|legs`, `/print/pdf`, `/audit`, `/app-errors` — ούτε τα preflight OPTIONS.
   **ΠΟΥ:** `core/api.js:371-395` (`_apiHeaders` παίρνει `req` μόνο από `_atRetry`)· χειροποίητα `fetch` σε `modules/costs.js`, `core/pallet-feed.js`, `modules/audit_trail.js`, `modules/payroll_print.js`, `core/utils.js:593`· `worker/src/index.js:4812` (`if (request.method === "OPTIONS") return res;` → καμία γραμμή `kind:"req"`).
   **ΚΑΝΟΝΑΣ:** Ισχυρισμός `docs/tms-auditor/05-…md:39` «μία γραμμή ανά αίτημα» και `00-…md:69` «Καταγραφή κλικ → αίτημα → βάση».
   **ΕΠΙΠΤΩΣΗ:** owner/accountant — ένα περιστατικό στα Έξοδα, στις Παλέτες ή στη Μισθοδοσία θα έχει `req_id = NULL` και δεν θα συνδέεται με το κλικ· μια αποτυχία CORS/preflight (που κόβει ΟΛΑ τα αιτήματα) δεν αφήνει κανένα ίχνος στο Επίπεδο Α.
   **ΣΟΒΑΡΟΤΗΤΑ:** P3 · **ΒΕΒΑΙΟΤΗΤΑ:** ΕΠΙΒΕΒΑΙΩΜΕΝΟ (διάβασα τον κώδικα)
   **ΕΠΟΜΕΝΟ:** ο συγγραφέας να γράψει ρητά στο 05 ποιες διαδρομές ΔΕΝ καλύπτει η πρώτη φάση (η παράλειψη του OPTIONS είναι σωστή επιλογή όγκου — αλλά πρέπει να λέγεται, αλλιώς το «7k γραμμές/ημέρα» και το «ανά αίτημα» διαβάζονται ως πλήρης κάλυψη).

2. **ΤΙ:** Το δηλωμένο μέτρο κόστους της διάγνωσης (απόσπασμα κώδικα στο πακέτο ⇒ ένας κύκλος μοντέλου) δεν υλοποιήθηκε — το πακέτο περιέχει μόνο **δείκτες**.
   **ΠΟΥ:** `docs/tms-auditor/07-…md` §2 («ο δείκτης κώδικα μπαίνει στο πακέτο ως **απόσπασμα** (≤ 60 γραμμές ανά αρχείο, ≤ 3 αρχεία) … Στόχος ≤ 40k») έναντι `tms-auditor/investigate/package.mjs:26-40` (`codePointers` → `{src, edge, status}`, καμία ανάγνωση αρχείου) και `investigate/prompt.md` §«Τι μπορείς να κάνεις» που ΕΠΙΤΡΕΠΕΙ «ανάγνωση αρχείων του repo στους code_pointers».
   **ΚΑΝΟΝΑΣ:** Ο ίδιος ο ισχυρισμός κόστους του 07 §2· αρχή 3 (δύο πηγές αλήθειας).
   **ΕΠΙΠΤΩΣΗ:** owner — η μοναδική μετρημένη διάγνωση κόστισε 130.641 tokens ακριβώς λόγω των 4 αναγνώσεων αρχείων· με τον κώδικα ως έχει η επόμενη θα ξανακάνει το ίδιο, ενώ το `LIMITS.max_tokens_in = 60000` δεν επιβάλλεται πουθενά στον κώδικα (μόνο `max_package_bytes` ελέγχεται, `diagnose.mjs:57-58`). Η πρόβλεψη «≈1,23–1,77M/ημέρα» στηρίζεται σε στόχο που δεν υπάρχει.
   **ΣΟΒΑΡΟΤΗΤΑ:** P3 · **ΒΕΒΑΙΟΤΗΤΑ:** ΕΠΙΒΕΒΑΙΩΜΕΝΟ
   **ΕΠΟΜΕΝΟ:** ή υλοποιείται το απόσπασμα, ή το 07 §2 λέει «στόχος χωρίς μηχανισμό — μετράται στο E-28» (σήμερα λέει μόνο «*Ο στόχος δεν έχει μετρηθεί*», που αφήνει να εννοηθεί ότι το μέτρο υπάρχει).

3. **ΤΙ:** Το σχόλιο «Written AFTER routing, never on the user's path» είναι λάθος: η γραμμή καταγραφής και μια **δεύτερη** επαλήθευση JWT γίνονται πριν επιστραφεί η απόκριση.
   **ΠΟΥ:** `worker/src/index.js:4802-4821` — `const out = new Response(...)` → `await getCaller(request, env)` → `logReqLine(...)` → `return out`. Το `ctx.waitUntil` δεν χρησιμοποιείται.
   **ΚΑΝΟΝΑΣ:** Ο κανόνας σχολίων του CLAUDE.md («σχόλιο που περιγράφει κάτι που άλλαξε ενημερώνεται ή φεύγει»)· ισχυρισμός του συγγραφέα.
   **ΕΠΙΠΤΩΣΗ:** κάθε ρόλος — μία επιπλέον HMAC επαλήθευση ανά αίτημα προστίθεται στον χρόνο απόκρισης κάθε κλήσης (συμπεριλαμβανομένων των αιτημάτων που ήδη έκαναν `getCaller` στο routing). Μικρή σε απόλυτο μέγεθος, αλλά δεν είναι «εκτός διαδρομής» όπως λέει το σχόλιο, και ο owner δεν μπορεί να το γνωρίζει από την τεκμηρίωση.
   **ΣΟΒΑΡΟΤΗΤΑ:** P3 · **ΒΕΒΑΙΟΤΗΤΑ:** ΕΠΙΒΕΒΑΙΩΜΕΝΟ
   **ΕΠΟΜΕΝΟ:** ο συγγραφέας διορθώνει το σχόλιο ή μεταφέρει την καταγραφή σε `ctx.waitUntil`· ο owner να μετρήσει p50/p99 πριν/μετά στο smoke του E-30.

4. **ΤΙ:** Ο service worker αποθηκεύει τις GET απαντήσεις του facade **μαζί με τις κεφαλίδες τους**· μια τέτοια απάντηση από cache μπορεί να ανάψει τη σημαία `_tmsReqOk`, παρακάμπτοντας εν μέρει την ρητή εγγύηση «ποτέ δεν αποθηκεύεται, ώστε ένα rollback να μη σπάσει την εφαρμογή».
   **ΠΟΥ:** `sw.js:103-137` (`cache.put` της πλήρους απόκρισης, σερβίρεται στο `.catch` offline) → `core/api.js:253` (`_noteWorkerCaps(res)` δεν ξεχωρίζει απόκριση δικτύου από απόκριση cache).
   **ΚΑΝΟΝΑΣ:** Το ίδιο το σχόλιο `core/api.js:371-376`· αρχή 7 (μία αλλαγή, αναστρέψιμη).
   **ΕΠΙΠΤΩΣΗ:** μόνο στο παράθυρο ενός rollback του Worker και μόνο αν ο χρήστης έχει ήδη δει cached απάντηση: ο browser θα στείλει `x-tms-req`, ο παλιός Worker δεν το επιτρέπει στο CORS, και **κάθε** αίτημα της σελίδας θα κοπεί στο preflight (dispatcher σε βάρδια = μαύρη οθόνη). Δεν μπόρεσα να το αναπαραγάγω χωρίς browser/rollback.
   **ΣΟΒΑΡΟΤΗΤΑ:** P3 · **ΒΕΒΑΙΟΤΗΤΑ:** ΥΠΟΘΕΣΗ
   **ΕΠΟΜΕΝΟ:** απόφαση owner: το πιο φθηνό αντίμετρο είναι να **μην** αφαιρεθεί ποτέ η γραμμή `Access-Control-Allow-Headers: …, x-tms-req, x-tms-app` σε τυχόν επαναφορά bundle (να γραφτεί στο E-30 ως όρος του rollback).

5. **ΤΙ:** Τρεις **ενεργοί** έλεγχοι δεν έτρεξαν ποτέ στην παραγωγή, ενώ η κεντρική σελίδα γράφει ότι ο κατάλογος «έτρεξε μία φορά στην παραγωγή χωρίς σφάλμα».
   **ΠΟΥ:** `tms-auditor/baseline/2026-09-22-prod.json` (65 ids) έναντι `loadChecks()` (66 ενεργοί) — λείπουν **B-52, B-53, B-54**· `docs/tms-auditor/00-…md:63` και `05-…md` §1 («64/64»), ενώ το baseline έχει 65 τιμές.
   **ΚΑΝΟΝΑΣ:** Αρχή 2 (η απόδειξη είναι ο πίνακας) και ο κανόνας ειλικρίνειας των τεσσάρων βαθμίδων (`00-…md:56-57`).
   **ΕΠΙΠΤΩΣΗ:** owner — αν ένας από τους τρεις έχει λάθος SQL για το πραγματικό σχήμα, την πρώτη νύχτα λειτουργίας θα παραχθεί περιστατικό ΜΗΧΑΝΙΣΜΟΥ αντί για δεδομένο, και το «ο ελεγκτής ξεκίνησε καθαρά» θα είναι ψευδές. Τα υπόλοιπα labels («υλοποιημένη τοπικά», 0 με επαληθευμένη παράδοση) είναι **σωστά** — κανένα «επαληθευμένο» δεν βρέθηκε για κάτι που έτρεξε μόνο τοπικά.
   **ΣΟΒΑΡΟΤΗΤΑ:** P4 · **ΒΕΒΑΙΟΤΗΤΑ:** ΕΠΙΒΕΒΑΙΩΜΕΝΟ
   **ΕΠΟΜΕΝΟ:** ο συγγραφέας να προσθέσει B-52/B-53/B-54 στο επόμενο μονό SELECT μέτρησης ή να τους αφήσει `enabled: no` μέχρι να μετρηθούν· να διορθωθεί το «64» σε 65/66.

## ΑΣΦΑΛΕΙΑ ΤΗΣ ΣΥΓΧΩΝΕΥΣΗΣ ΤΩΡΑ (πριν το deploy του Worker)
Φορτώνεται νέο `core/api.js?v=1790107518`, `core/utils.js?v=1790107518`, `SW_VERSION=1790107518` (συμφωνούν — κανόνας bump τηρήθηκε). Με τον **παλιό** Worker: καμία κεφαλίδα `x-tms-req` δεν φεύγει (πύλη `_tmsReqOk`), άρα κανένα preflight δεν αλλάζει· το επιπλέον πεδίο `req` στο σώμα του `/app-errors` αγνοείται (ο παλιός `handleAppErrorPost` χτίζει τη `row` ρητά). **Μία πραγματική αλλαγή συμπεριφοράς:** κάθε άδειασμα της ουράς offline γράφει πλέον **μη-σφάλμα** στο `app_errors` (`core/api.js:162-167`, μήνυμα `queue: offline flush …`). Οι έλεγχοι B-34/B-34b το εξαιρούν ρητά (`message NOT LIKE 'queue: offline flush%'`, ταιριάζει με το `${ctx}: ${msg}` του `core/utils.js:597`), **αλλά** οι έλεγχοι δεν είναι ενεργοί — και καμία οθόνη δεν διαβάζει σήμερα το `app_errors`, οπότε η επίπτωση περιορίζεται σε όγκο γραμμών. Δεν βρήκα τίποτα που να σπάει για τον χρήστη στο επόμενο Pages load.

## ΔΕΝ ΕΛΕΓΧΘΗΚΕ
- Η έκδοση του Worker **στην παραγωγή** μού είναι άγνωστη· κανένα από τα 047/047b/048/049/050 δεν είναι εκτελεσμένο (όλα DRAFT) — δεν το επιβεβαίωσα στη βάση (καμία σύνδεση σε αυτή την αξιολόγηση).
- Συμπεριφορά σε πραγματικό browser: preflight με παλιό/νέο Worker, service worker με πραγματικό cache, ανακατεύθυνση 401. Θέλει rig/λογαριασμό.
- Το SQL των 69 ελέγχων ένα προς ένα ως προς **επιχειρησιακή ορθότητα** (έτρεξαν χωρίς σφάλμα σε PGlite και 65/66 στην παραγωγή· δεν έκρινα αν κάθε κατώφλι «κόκκινου» είναι σωστό).
- `docs/tms-auditor/graph/part-*.json` (19k γραμμές): επαλήθευσα μόνο σύνολα και 0 ορφανά άκρα, όχι την ορθότητα των ακμών.
- Δεν διάβασα καμία άλλη αξιολόγηση αυτού του branch (όρος της εντολής).

## ΕΚΤΕΛΕΣΤΗΚΑΝ
`git fetch origin` · `git diff 168f6a7..origin/feat/tms-auditor --stat` · `git worktree add --detach <scratchpad>/rv-b origin/feat/tms-auditor` · `npm install --no-audit --no-fund` (μόνο `@electric-sql/pglite`, στο worktree) · `node --test tms-auditor/test/*.test.mjs` · `node --test worker/test/*.test.mjs` · `node --test tests/*.test.js` · `node --test tests/level-a-api.test.mjs` · `bash tests/check-fail-open.sh` · `node --check core/api.js core/utils.js sw.js` (καθαρά) · αναγνώσεις repo. **Καμία βάση, κανένα Cloudflare, κανένα δίκτυο πέρα από npm, καμία αλλαγή σε αρχείο του repo εκτός από αυτή την αναφορά.**

## ΠΡΟΤΑΣΗ ΕΓΧΕΙΡΙΔΙΟΥ
Το `07-change-review-runbook.md` §5 λέει «δεν τρέχει tests». Από την 7η αξιολόγηση ο ελεγκτής **τρέχει** καθαρά unit tests, και εδώ χρειάστηκε και `npm install` μιας dev dependency σε προσωρινό worktree. Ο κανόνας πρέπει να γραφτεί ρητά (τι επιτρέπεται να εγκατασταθεί, πού, και ότι το αποτέλεσμα μπαίνει αυτούσιο).
