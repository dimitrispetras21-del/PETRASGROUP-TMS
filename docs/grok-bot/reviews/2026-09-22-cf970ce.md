# Αξιολόγηση cf970ce — 2026-09-22 21:24
Commit: cf970ce (branch fix/invoicing-price-zero, βάση main 5dca17e — όλο το push: ddfc2e5 + cf970ce) · Τιμολόγηση price>0: και η παράκαμψη owner (_invOverrideInvoice) κρίνει με _invHasPrice — Grok ελεγκτής ddfc2e5 P3
Εγχειρίδια: knowledge 7e4697980ebb · CLAUDE 72a4c258b5db · Ελεγκτής: Claude Fable 5.1 (ρόλος 1, read-only· SELECT στη Supabase με ρητή άδεια συντονιστή — παρέκκλιση από 07 §5)
Diff: 3 αρχεία (modules/invoicing.js, app.html, sw.js), 4,7 KB · Χρόνος: 4 λεπτά (21:20→21:24) · Tokens: εκτίμηση ~25k (bytes/3 των αναγνωσμένων)

ΑΠΟΤΕΛΕΣΜΑ: ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ

ΜΗΧΑΝΙΚΑ: Μ1 pass (0 markers) · Μ2 pass (node --check invoicing.js, sw.js) · Μ3 pass (app.html:154 ?v=1790103900 = sw.js:10 SW_VERSION) · Μ4 n.a. (κανένα νέο label· η εγγραφή μένει Invoiced/Invoice Number/Invoice Date, στον χάρτη και των δύο πινάκων) · Μ5 n.a. (καμία νέα διαδρομή εγγραφής) · Μ6 n.a. (κανένα νέο fetch· το CI script υπάρχει, δεν έτρεξε) · Μ7 υπάρχει tests/critics/invoicing-proof.js — ΔΕΝ έτρεξε (07 §5) και ΔΕΝ καλύπτει Price 0 (βλ. εύρημα 1) · Μ8 n.a.

Ισχυρισμοί συγγραφέα (δική μου ανάγνωση πρώτα):
(α) ΝΑΙ — οθόνη `v !== null && v > 0` (invoicing.js:111-114) ≡ Worker `!(Number(price) > 0)` (worker/src/index.js:2759) ≡ 043 `coalesce(price,0) <= 0`. NULL, 0, NaN, αρνητικό: και οι τρεις αρνούνται.
(β) ΝΑΙ — grep `_invPrice(…) === null`: 0 εμφανίσεις. Οι 4 πύλες (KPI :510, φόρμα :752, φρουρός :853, παράκαμψη :894) σε `_invHasPrice`.
(γ) ΝΑΙ, συνεπές — `readyTotal`/`outstandingTotal`/`invTotal` αθροίζουν `_invPrice(r)||0`: το 0 προσθέτει 0, ό,τι και πριν. Η λεζάντα «N χωρίς τιμή» πλέον μετρά ΚΑΙ τα 0 — σωστό, το άθροισμα όντως τα αγνοεί. Μόνη ασυμμετρία: αρνητική τιμή αθροίζεται (−2 €) ενώ λογίζεται «χωρίς τιμή» (εύρημα 2).
(δ) ΝΑΙ — 1790103900 και στα δύο.
(ε) ΝΑΙ — τα εθνικά μπαίνουν στο ίδιο `INV.data` (:334) με `_type='natl'`· `_renderInvDetail`, `_invMarkInvoiced`, `_invOverrideInvoice`, `_renderInvKPI` είναι κοινές· `_invWriteInvoice` επιλέγει πίνακα (:830)· Worker `invoiceMarkError` δέχεται και `national_orders` (:2751)· 043 trigger και στους δύο.
(στ) SELECT 22/9 21:22 (δεδομένα παραγωγής, χωρίς ονόματα):
  orders, deleted_at null, status='Delivered', invoiced is distinct from true — σύνολο 183: price=0 → 14 (και τα 14 χωρίς parent_order_id), price NULL → 17 (13 χωρίς γονέα), price>0 → 151, price<0 → 1.
  national_orders, ίδιος κανόνας οθόνης (status='Delivered' ή (status is distinct from 'Cancelled' και delivery_datetime < current_date)), not invoiced — σύνολο 11: price=0 → 0, NULL → 7, >0 → 4.
  ⇒ Ιστορικά: 14 διεθνείς που ως χθες έδειχναν φόρμα ΤΠΥ και έπεφταν σε 422 τώρα δείχνουν «Χωρίς τιμή — ζήτησέ την από τον dispatcher». Ο μετρητής «χωρίς τιμή» ανεβαίνει από 13 σε 27 (+1 αρνητική = 28). Καμία εθνική με 0.
  SQL 1: select count(*) filter (where price = 0) as price_zero, count(*) filter (where price is null) as price_null, count(*) filter (where price > 0) as price_pos, count(*) filter (where price < 0) as price_neg, count(*) as total, count(*) filter (where price = 0 and parent_order_id is null) as zero_parent_or_simple, count(*) filter (where price is null and parent_order_id is null) as null_parent_or_simple from orders where deleted_at is null and status = 'Delivered' and invoiced is distinct from true limit 1;
  SQL 2: select count(*) filter (where price = 0) as price_zero, count(*) filter (where price is null) as price_null, count(*) filter (where price > 0) as price_pos, count(*) filter (where price < 0) as price_neg, count(*) as total from national_orders where deleted_at is null and invoiced is distinct from true and (status = 'Delivered' or (status is distinct from 'Cancelled' and delivery_datetime < current_date)) limit 1;
Callers εκτός invoicing.js: `_invPrice`/`_invHasPrice` — 0 σε modules/, core/, app.html, index.html, print.html, config.js. Κλειδωμένες αποφάσεις (knowledge §5): τηρούνται — ΤΠΥ υποχρεωτικός, τιμή πρώτα, καμία έκδοση, `Net Price` ανέγγιχτο.

ΕΥΡΗΜΑΤΑ:
1. ΤΙ: Ο νέος κανόνας (Price 0 = χωρίς τιμή) δεν καλύπτεται από κανένα test — το rig έχει μόνο recO4 χωρίς Price (NULL)· fixture με `Price: 0` δεν υπάρχει (grep).
   ΠΟΥ: tests/critics/invoicing-proof.js:31,63 · worker/test/invoice-mark-guard.test.mjs:31 (μόνο ο Worker ελέγχει price 0)
   ΚΑΝΟΝΑΣ: Μ7 · ερώτηση 1Β «περιπτώσεις που δεν δοκιμάστηκαν» · αρχή 6 (πιάσε το λάθος στο build)
   ΕΠΙΠΤΩΣΗ: Λογίστρια, Τιμολόγηση — αν κάποιος ξαναγυρίσει την πύλη σε `=== null`, το rig μένει πράσινο και τα 14 ξαναπέφτουν σε 422.
   ΣΟΒΑΡΟΤΗΤΑ: P3
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ
   ΕΠΟΜΕΝΟ: Ο συγγραφέας προσθέτει fixture με Price 0 (μήνυμα «Χωρίς τιμή», όχι #invNumInput/#invMarkBtn, μετρητής «2 χωρίς τιμή», γραμμή 0,00 €) και τρέχει το rig — να αναφερθεί ο αριθμός assertions.
2. ΤΙ: Μία παραδομένη μη τιμολογημένη διεθνής έχει price = −2: η οθόνη τη δείχνει «Χωρίς τιμή» (σωστά, ίδιο με Worker/043) αλλά το −2 € μπαίνει στα readyTotal/outstandingTotal και στο CSV.
   ΠΟΥ: modules/invoicing.js:505,508 (`_invPrice(r)||0` αθροίζει αρνητικό) · orders legacy_id recLjutEq9SOLqxDU (παράδοση 16/9)
   ΚΑΝΟΝΑΣ: ερώτηση 1Β «ιστορικά δεδομένα» · αρχή 2 (η απόδειξη είναι ο πίνακας)
   ΕΠΙΠΤΩΣΗ: Λογίστρια/owner, KPI «Έτοιμες»: τζίρος μειωμένος κατά 2 € ενώ η γραμμή λογίζεται «χωρίς τιμή». Δεδομένα, όχι η αλλαγή· δεν μπλοκάρει.
   ΣΟΒΑΡΟΤΗΤΑ: P4
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ (SELECT 21:22 + κώδικας)
   ΕΠΟΜΕΝΟ: Dispatcher διορθώνει την τιμή της παραγγελίας από την καρτέλα (όχι από τη βάση — owner 7/9). Ο συγγραφέας αποφασίζει αν τα σύνολα πρέπει να αγνοούν `!_invHasPrice` — εκτός πεδίου αυτής της αλλαγής.

ΔΕΝ ΕΛΕΓΧΘΗΚΕ: εκτέλεση tests/critics/invoicing-proof.js και worker/test (07 §5) · ζωντανή οθόνη ως accountant (η αλλαγή δεν είναι στο main· ALLOWED_ORIGIN) · ότι ο ζωντανός Worker (8cb4f167) περιέχει το invoiceMarkError όπως στο repo (043 κεφαλίδα λέει ναι 22/9 πρωί).
ΠΡΟΤΑΣΗ ΕΓΧΕΙΡΙΔΙΟΥ: knowledge §5 γραμμή «Χωρίς τιμή → καμία φόρμα» να γράφει ρητά «τιμή = price > 0 (0, NULL, αρνητικό = χωρίς τιμή), ίδιος κανόνας σε οθόνη/Worker/043» — το κενό ορισμού είναι που άφησε το 0 να περάσει 21/9→22/9. Και 07 §5: να καταγραφεί η εξαίρεση «SELECT μόνο, με ρητή άδεια συντονιστή».
