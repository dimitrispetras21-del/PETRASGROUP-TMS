# Αξιολόγηση 522cee9 — 2026-09-23 (ρόλος Α: βάση + όρια ελεγκτή)
Commit: 522cee9b95716b4aa62f2245ca457e78d6a923c6 · "tms-auditor: τεχνικός ελεγκτής TMS — γράφος, 69 έλεγχοι, μηχανή περιστατικών, διάγνωση Claude, ro…"
Branch: feat/tms-auditor · Base: main 168f6a7 · Εγχειρίδια: CLAUDE.md `72a4c258b5db` (ταιριάζει με 07 §2) · 01-PROMPT-GROK.md `2efbbee2a19f` · Ελεγκτής: Claude Sonnet 5 (Claude Code agent, read-only)

Diff πλήρες: 143 αρχεία, 34.603 προσθήκες. Πεδίο μου (βάση + όρια): 105 αρχεία / 4.447 γραμμές — 5 migrations (047/047b/048/049/050), 69 checks/*.sql (10 διαβάστηκαν πλήρως: B-01,B-11,B-18,B-29a/b,B-44,B-46,B-48,B-49,B-53,B-54,P-01), checks/load.mjs+build-seed.mjs, 5 test files (roles/catalog/chain/engine + fixtures), lib/db.mjs, worker/src/index.js (Level A), core/api.js (Level A front). ΔΕΝ διάβασα γραμμή-γραμμή: graph/*.json (34k γραμμών δεδομένων, όχι λογική), investigate/*, routines/*.md, worker/test/req-line.test.mjs, tests/level-a-api.test.mjs. Χρόνος: ~35λ.

ΑΠΟΤΕΛΕΣΜΑ: ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ

ΜΗΧΑΝΙΚΑ: n.a. (ειδικευμένη αξιολόγηση βάσης· τα Μ1–Μ8 του 04-πιλότος.md αφορούν front-end review, δεν μου δόθηκαν εδώ)

ΙΣΧΥΡΙΣΜΟΙ:
- «Τα 047/047b/048/049/050 ΔΕΝ εκτελέστηκαν» → ΕΠΑΛΗΘΕΥΤΗΚΕ: όλα κάτω από `worker/migrations/drafts/`, κάθε αρχείο ξεκινά με «DRAFT · NOT EXECUTED»· καμία εκτέλεση migration αυτόματα σε merge (δεν βρέθηκε CI runner στο diff).
- «049 additive, nullable, καμία rewrite» → ΕΠΑΛΗΘΕΥΤΗΚΕ: μόνο `ADD COLUMN IF NOT EXISTS` + partial indexes, χωρίς `NOT NULL`/default.
- «Τα pg_cron/pg_net ζουν μόνο στο 048» → ΕΠΑΛΗΘΕΥΤΗΚΕ: `CREATE EXTENSION`/`cron.schedule` μέσα σε `@prod-only-begin/end`, εκτελούνται μόνο αν ο owner τρέξει το 048.
- «Front rollback-safe (x-tms-req μόνο μετά x-tms-worker-req)» → ΕΠΑΛΗΘΕΥΤΗΚΕ σε core/api.js: `_tmsReqOk` ξεκινά false, μη-persisted, ενεργοποιείται μόνο από response header υπάρχοντος (αναβαθμισμένου) Worker.

ΕΥΡΗΜΑΤΑ:
1. [P3] ΤΙ: Το `sql_text` του `monitoring.checks` εκτελείται δυναμικά (`EXECUTE r.sql_text`) χωρίς ΚΑΝΕΝΑ constraint στη ΒΑΣΗ που να το περιορίζει σε SELECT· ο μοναδικός φραγμός «SELECT-only» είναι το `lintSql()` στο repo (`tms-auditor/checks/load.mjs`), ελεγμένο μόνο με `node --test`.
   ΠΟΥ: worker/migrations/drafts/047_monitoring_schema.sql:18-41 (πίνακας `checks`, καμία CHECK στη στήλη sql_text) έναντι tms-auditor/checks/load.mjs (FORBIDDEN regex + allowlist συναρτήσεων)
   ΚΑΝΟΝΑΣ: αρχή 4 «ο κανόνας μπαίνει όσο πιο χαμηλά αντέχει» — ένα σχόλιο/script στο repo πιάνει μία διαδρομή, ένα CHECK στη βάση θα έπιανε κάθε μελλοντική εγγραφή.
   ΕΠΙΠΤΩΣΗ: αν κάποτε μπει γραμμή στο monitoring.checks χωρίς να περάσει από build-seed.mjs (χειροκίνητο SQL, μελλοντικό εργαλείο), το run_checks() θα εκτελέσει ό,τι λέει η στήλη — σήμερα μετριάζεται ΜΟΝΟ από το ότι κανείς άλλος εκτός postgres δεν έχει INSERT στο monitoring.checks (καμία τέτοια GRANT στο 050).
   ΣΟΒΑΡΟΤΗΤΑ: P3
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ (διάβασα τον κώδικα)
   ΕΠΟΜΕΝΟ: ο συγγραφέας αποφασίζει αν αξίζει CHECK constraint στη βάση (π.χ. `sql_text ~* '^\s*select'`) ή αν το privilege-only κλείδωμα θεωρείται αρκετό.

2. [P3] ΤΙ: Σειρά migrations 047/048 → 050: το schema `monitoring` και οι 3 SECURITY DEFINER συναρτήσεις (`record_notification`, `beat`, `record_diagnosis`) δημιουργούνται ΠΡΙΝ υπάρξει το ρητό `REVOKE ALL … FROM PUBLIC` — που έρχεται μόνο στο 050, ξεχωριστό αργότερο draft.
   ΠΟΥ: worker/migrations/drafts/047_monitoring_schema.sql:369-394 (CREATE FUNCTION … SECURITY DEFINER, καμία REVOKE εδώ) έναντι worker/migrations/drafts/050_monitor_roles.sql:44-49 (οι REVOKE)
   ΚΑΝΟΝΑΣ: αρχή 5 «ό,τι γεννιέται, γεννιέται κλειστό» — το ίδιο CLAUDE.md δίνει ως παράδειγμα ακριβώς αυτό το φαινόμενο (Supabase → anon σε κάθε νέο πίνακα).
   ΕΠΙΠΤΩΣΗ: αν ο owner εκτελέσει 047+048 και καθυστερήσει το 050, οποιοσδήποτε ρόλος με USAGE σε public (προεπιλογή Postgres σε νέο schema/function) θα μπορούσε να καλέσει τις 3 SECURITY DEFINER συναρτήσεις, που τρέχουν ΩΣ Ο ΙΔΙΟΚΤΗΤΗΣ. ΔΕΝ επιβεβαίωσα αν κάποιος μη-postgres ρόλος (anon/authenticated) έχει πραγματικά δρόμο μέχρι το schema `monitoring` μέσω PostgREST — σταμάτησα τα ερωτήματα στη βάση πριν το ελέγξω (βλ. ΔΕΝ ΕΛΕΓΧΘΗΚΕ).
   ΣΟΒΑΡΟΤΗΤΑ: P3 (η ίδια η ρύθμιση είναι ΓΕΓΟΝΟΣ· η εκμεταλλευσιμότητα είναι ΥΠΟΘΕΣΗ, άρα δεν ανεβαίνει σε P2)
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ ότι η σειρά δεν κλείνει πρώτα· ΥΠΟΘΕΣΗ για το αν εκτίθεται σήμερα
   ΕΠΟΜΕΝΟ: ο owner τρέχει 047+048+050 ΜΑΖΙ, ή προσθέτει `REVOKE`/`ALTER DEFAULT PRIVILEGES` μέσα στο ίδιο το 047/048 αντί να το αφήνει σε ξεχωριστό αργότερο draft.

3. [P3] ΤΙ: Ο ρόλος `tms_reader` — αυτός που το 050 φτιάχνει ρητά ώστε «read-only να είναι προνόμιο, όχι πρόταση σε prompt» (01 §6) — δεν μπορεί να ξανατρέξει ή να επαληθεύσει ανεξάρτητα τους ελέγχους P-01/B-53 (χρειάζονται `audit_log.actor`/`before_data`/`after_data`, ρητά ΕΚΤΟΣ της GRANT SELECT λίστας) ούτε να καλέσει `run_checks()`/`build_package()` (καμία GRANT EXECUTE σε αυτόν στο 050).
   ΠΟΥ: worker/migrations/drafts/050_monitor_roles.sql:31-34 (στήλες audit_log) και :36-46 (μόνο 3 functions grant σε tms_monitor_writer· run_checks/self_check/κτλ ρητά REVOKE FROM PUBLIC χωρίς GRANT πουθενά σε ρόλο)
   ΚΑΝΟΝΑΣ: το ίδιο το 050 το ομολογεί σε σχόλιο («τρέχουν ως postgres από cron, ποτέ ως ρόλος») — συνεπές, αλλά σημαίνει ότι ο νέος ρόλος δεν λύνει πλήρως το πρόβλημα του 01 §6 για όσους ελέγχους το χρειάζονται.
   ΕΠΙΠΤΩΣΗ: μελλοντικός ανεξάρτητος ελεγκτής που συνδέεται ως tms_reader (όχι ως postgres) δεν μπορεί να ξανατρέξει build_package/run_checks για επαλήθευση — πρέπει να εμπιστευτεί τις γραμμές monitoring.results/incidents όπως τις άφησε το cron.
   ΣΟΒΑΡΟΤΗΤΑ: P3
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ
   ΕΠΟΜΕΝΟ: αποδεκτό ως σχεδιαστική επιλογή (η ίδια η migration το εξηγεί) — ο owner το επιβεβαιώνει ρητά ή σκέφτεται GRANT EXECUTE στο build_package για tms_reader.

4. [P3] ΤΙ: Τα pg_cron ωράρια είναι σε UTC με σχόλιο ότι «τα shift-bound jobs πρέπει να ξαναμπούν στις 25/10» (αλλαγή ώρας Ελλάδας) — αλλά ΚΑΝΕΝΑΣ έλεγχος του καταλόγου δεν παρακολουθεί αν το cron.job έχει ακόμη τη σωστή ώρα μετά την αλλαγή.
   ΠΟΥ: worker/migrations/drafts/048_monitoring_notify.sql (μπλοκ `-- @prod-only-begin` πριν το τέλος, σχόλιο «Athens = UTC+3 until 25/10, then UTC+2»)
   ΚΑΝΟΝΑΣ: αρχή 1 «ό,τι δεν γίνεται, πρέπει να ακούγεται» — μια μετατόπιση ωραρίου που δεν ελέγχεται από κανέναν μηχανισμό είναι ακριβώς αυτό.
   ΕΠΙΠΤΩΣΗ: μετά τις 25/10, τα fast/half checks θα τρέχουν ~1 ώρα νωρίτερα σε τοπική ώρα (π.χ. 04:00–13:45 αντί 05:00–14:45) χωρίς καμία ειδοποίηση — κανένας δεν θα το μάθει παρά μόνο αν κάποιος θυμηθεί το σχόλιο.
   ΣΟΒΑΡΟΤΗΤΑ: P3
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ (το κείμενο του σχολίου το λέει το ίδιο)
   ΕΠΟΜΕΝΟ: ημερολογιακή υπενθύμιση εκτός κώδικα, ή ένα MECH-check που συγκρίνει `cron.job.schedule` με την τρέχουσα μετατόπιση Αθήνας.

5. [P4] ΤΙ: Οι 3 checks που ο συγγραφέας λέει ότι διορθώθηκαν (B-44/B-49: `orders`/`pl_movements` χωρίς `updated_at`· B-48: `national_loads.matched_load` είναι text legacy_id, όχι FK) είναι όντως τεκμηριωμένα σωστά στα .sql αρχεία — B-49 μάλιστα είναι `enabled: no` με αιτιολογία >20 χαρακτήρων (περνά το lint).
   ΠΟΥ: tms-auditor/checks/B-44.sql, B-48.sql, B-49.sql
   ΚΑΝΟΝΑΣ: συνέπεια ισχυρισμού συγγραφέα ↔ κώδικα
   ΕΠΙΠΤΩΣΗ: καμία — θετικό εύρημα, καταγράφεται για πληρότητα
   ΣΟΒΑΡΟΤΗΤΑ: P4
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ
   ΕΠΟΜΕΝΟ: κανένα

ΔΕΝ ΕΛΕΓΧΘΗΚΕ:
- Ο έλεγχος ταυτότητας Supabase έδειξε `current_user=supabase_read_only_user, rolbypassrls=true` (transaction_read_only=on) — κατά γράμμα του 01-PROMPT-GROK.md §4 («αν rolbypassrls=true → δεν συνεχίζεις»), ΣΤΑΜΑΤΗΣΑ κάθε άλλο SQL στη ζωντανή βάση. Άρα ΔΕΝ επαλήθευσα: τον ισχυρισμό «~30 SECURITY DEFINER functions με EXECUTE σε PUBLIC» μέσω pg_proc/proacl, ούτε έτρεξα τα 10 δείγματα checks με LIMIT στο ζωντανό schema (βασίστηκα στο catalog.test.mjs που δηλώνει 0 σφάλματα σε PGlite fixture-schema, και στο commit message που αναφέρει προηγούμενο live run 22/9 19:33 UTC = 64/64 χωρίς σφάλμα, ΠΡΙΝ από αυτή τη συνεδρία).
- Δεν έτρεξα τα 24 tms-auditor tests ούτε τα worker/level-a tests μόνος μου (θα χρειαζόταν εγκατάσταση `@electric-sql/pglite` σε προσωρινό checkout) — μόνο ανάγνωση πηγαίου κώδικα.
- investigate/*.mjs, routines/*.md, worker/test/req-line.test.mjs, tests/level-a-api.test.mjs: δεν διαβάστηκαν λεπτομερώς.
- Σύγκριση checks/*.sql ένα-προς-ένα με docs/grok-bot/monitoring-2026-09-22/02b (μόνο δειγματοληπτικός έλεγχος 3 διορθώσεων).
- Σύνταξη plpgsql 047/048/050 δεν ελέγχθηκε με ξεχωριστό psql/EXPLAIN — αλλά εκτελείται πραγματικά σε PGlite (ενσωματωμένη Postgres) από τα tests, που είναι ισχυρότερο από συντακτικό έλεγχο.
- Ο έλεγχος «anon/authenticated φτάνουν στο schema monitoring μέσω PostgREST;» (εύρημα 2) δεν επαληθεύτηκε.

ΕΚΤΕΛΕΣΤΗΚΑΝ:
- `select current_user, current_setting('transaction_read_only');` → `{"current_user":"supabase_read_only_user","read_only":"on"}`
- `select rolname, rolsuper, rolcreaterole, rolbypassrls from pg_roles where rolname = current_user;` → `{"rolname":"supabase_read_only_user","rolsuper":false,"rolcreaterole":false,"rolbypassrls":true}`
- `git fetch origin && git diff 168f6a7..origin/feat/tms-auditor --stat`
- `git show origin/feat/tms-auditor:<path>` για: worker/migrations/drafts/{047,047b,048,049,050}*.sql, tms-auditor/checks/{load,build-seed}.mjs, tms-auditor/checks/{B-01,B-11,B-18,B-29a,B-29b,B-44,B-46,B-48,B-49,B-53,B-54,P-01}.sql, tms-auditor/test/{roles,catalog,chain,engine}.test.mjs, tms-auditor/test/helpers.mjs, tms-auditor/lib/db.mjs, tms-auditor/package.json, worker/src/index.js (diff), core/api.js (diff)

ΠΡΟΤΑΣΗ ΕΓΧΕΙΡΙΔΙΟΥ: το 01-PROMPT-GROK.md §4 δεν λέει τι κάνει ο ελεγκτής ΜΕΤΑ το σταμάτημα λόγω rolbypassrls=true σε ρόλο που ΔΕΝ είναι postgres/service_role/anon/authenticated (εδώ: `supabase_read_only_user`, ο οποίος ΕΧΕΙ transaction_read_only=on παρά το bypassrls=true) — θα άξιζε διάκριση μεταξύ «πλήρης στάση» και «προσοχή, αλλά SELECT-only session επιτρέπεται».
