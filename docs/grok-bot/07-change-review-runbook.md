# 07 — Runbook: Ελεγκτής προτεινόμενων αλλαγών (ρόλος 1, πιλότος βήματος 5)

Έγκριση owner 22/9/2026 (βήμα 5 του `00-ΠΡΟΤΑΣΗ.md`). Διάρκεια πιλότου: 1 εβδομάδα. Εργαλείο: Claude Code session με τα εγχειρίδια του repo. **Ο ελεγκτής διαβάζει μόνο. Δεν αλλάζει, δεν κάνει push, δεν αγγίζει βάση ή Cloudflare.**

## 1. Πότε και σε τι (πεδίο εφαρμογής — συντονιστής 22/9, μετά το benchmark)

- **Μονάδα αξιολόγησης = το push (ή το branch), όχι το μεμονωμένο commit** (Ε1 του benchmark, εφαρμόστηκε): ο ελεγκτής βλέπει το `git diff origin/main...<branch>` ή το σύνολο `origin/main..HEAD`. Έτσι το bump `?v=`/`SW_VERSION` που έρχεται σε ξεχωριστό commit **δεν** δίνει ψευδές κόκκινο.
- **Πριν** από το push στο `main` (προτιμώμενο): ο συντονιστής ζητά αξιολόγηση με το SHA του τελευταίου commit + το branch, και δίνει «go» **μετά** το αποτέλεσμα. **Μετά** από push χωρίς αξιολόγηση: ίδια διαδικασία, το αποτέλεσμα μπορεί μόνο να ζητήσει revert.
- **Ο ρόλος 1 (κρίση agent) τρέχει ΜΟΝΟ σε «μεγάλες» αλλαγές:** ό,τι αγγίζει `worker/src/`, `worker/migrations/`, φόρμες παραγγελιών (`modules/orders_intl.js`, `modules/orders_natl.js`, `modules/weekly_*.js`), δικαιώματα (`PERMISSIONS`/`COSTS_PERMS`/`PL_PERMS`), ή diff > 150 γραμμές. Αιτία: το benchmark έδειξε ~109k tokens ανά αξιολόγηση και κρίση ~1/3 — δεν αξίζει για μικρά.
- **Τα μικρά** (CSS, docs, screenshots, αλλαγές μίας γραμμής) περνούν **μόνο** από τα μηχανικά Μ1–Μ8 (βήμα 4, script χωρίς μοντέλο — DRAFT σε ξεχωριστό branch).

## 2. Τι ζητά ο συντονιστής (copy-paste)

```
ΑΞΙΟΛΟΓΗΣΗ ΑΛΛΑΓΗΣ — ρόλος 1
SHA: <7+ χαρακτήρες>   Branch: <όνομα ή "main">   Repo: dimitrispetras21-del/PETRASGROUP-TMS
Εγχειρίδια: docs/grok-bot/skill-tms-knowledge.md (sha256 πρώτα 12: <…>) · CLAUDE.md (sha256 πρώτα 12: <…>)
Οδηγίες: docs/grok-bot/04-πιλότος.md §1Α (Μ1–Μ8) και §1Β (5 ερωτήσεις).
Πεδίο: git diff origin/main...<branch> (όλο το push, όχι ένα commit). Όρια: μόνο Read/Grep/Glob στο repo στο σημείο του SHA, git show/diff, και node --check. Καμία αλλαγή, κανένα push, καμία βάση/CF, κανένα test.
Απάντησε: ΕΓΚΡΙΝΕΤΑΙ / ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ / ΜΠΛΟΚΑΡΕΤΑΙ + αιτιολογία ≤10 γραμμές (αρχείο:γραμμή) + Μηχανικά Μ1–Μ8 + ΔΕΝ ΕΛΕΓΧΘΗΚΕ.
Γράψε το αποτέλεσμα στο docs/grok-bot/reviews/<YYYY-MM-DD>-<sha7>.md (νέο αρχείο, χωρίς commit).
```

Τα hashes των εγχειριδίων: `shasum -a 256 docs/grok-bot/skill-tms-knowledge.md CLAUDE.md` (στις 22/9: `7e4697980ebb` / `72a4c258b5db`). **Αν αλλάξει το hash, οι παλιές αξιολογήσεις δεν συγκρίνονται με τις νέες.**

## 3. Πώς τρέχει ο ελεγκτής (μέσα στο session)

1. `git fetch` και `git show <SHA> --stat`, μετά `git show <SHA>` (το diff). Αν ο SHA είναι σε branch που δεν υπάρχει τοπικά: `git fetch origin <branch>`.
2. Για τον κώδικα «όπως είναι στο SHA» χωρίς να αγγίξει το working tree: `git worktree add --detach <scratchpad>/review-<sha7> <SHA>` — και **αφαίρεση** μετά (`git worktree remove`).
3. Διαβάζει τα 2 εγχειρίδια, τρέχει Μ1–Μ8 με grep/ανάγνωση **και `node --check`** (επιτρέπεται ρητά — Ε6 του benchmark, εφαρμόστηκε: είναι ντετερμινιστικό και χωρίς παρενέργειες· **τίποτα άλλο** δεν εκτελείται, ούτε tests ούτε rigs), απαντά στις 5 ερωτήσεις του §1Β.
4. Γράφει το αρχείο αναφοράς (§4). **Δεν** το κάνει commit — ο συντονιστής αποφασίζει αν οι αναφορές μπαίνουν στο repo (πρόταση: ναι, μία φορά την εβδομάδα, μαζί).
5. Επιστρέφει στον συντονιστή τις ≤10 γραμμές + τη διαδρομή του αρχείου.

Χρόνος-στόχος: ≤ 15 λεπτά ανά αλλαγή. Αν το diff > 100 KB, ο ελεγκτής το λέει και αξιολογεί **μόνο** τα αρχεία front-end εγγραφών, χάρτη πεδίων, δικαιωμάτων, `sw.js`/`app.html` — και γράφει τι παρέλειψε.

## 4. Μορφή αρχείου `docs/grok-bot/reviews/<YYYY-MM-DD>-<sha7>.md` (≤ 1 οθόνη)

```
# Αξιολόγηση <sha7> — <YYYY-MM-DD HH:MM>
Commit: <sha> · <τίτλος commit, κομμένος στα 100 χαρακτήρες>
Εγχειρίδια: knowledge <hash12> · CLAUDE <hash12> · Ελεγκτής: <μοντέλο/εργαλείο>
Diff: <N> αρχεία, <K> KB · Χρόνος: <λεπτά> · Tokens: <από τον μετρητή, ή "εκτίμηση bytes/3">

ΑΠΟΤΕΛΕΣΜΑ: ΕΓΚΡΙΝΕΤΑΙ | ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ | ΜΠΛΟΚΑΡΕΤΑΙ

ΜΗΧΑΝΙΚΑ: Μ1 pass · Μ2 pass · Μ3 fail · Μ4 pass · Μ5 n.a. · Μ6 pass · Μ7 n.a. · Μ8 n.a.

ΕΥΡΗΜΑΤΑ (0–5, καθένα με τα 7 πεδία):
1. ΤΙ: <μία πρόταση>
   ΠΟΥ: <αρχείο:γραμμή>
   ΚΑΝΟΝΑΣ: <Μx ή ερώτηση 1Β-y ή κλειδωμένη απόφαση + πηγή>
   ΕΠΙΠΤΩΣΗ: <ποιος χρήστης, ποια οθόνη, τι θα δει λάθος>
   ΣΟΒΑΡΟΤΗΤΑ: P1 / P2 / P3 / P4
   ΒΕΒΑΙΟΤΗΤΑ: ΕΠΙΒΕΒΑΙΩΜΕΝΟ (διάβασα τον κώδικα) / ΠΙΘΑΝΟ / ΥΠΟΘΕΣΗ
   ΕΠΟΜΕΝΟ: <τι κάνει ο συγγραφέας της αλλαγής — όχι ο ελεγκτής>

ΔΕΝ ΕΛΕΓΧΘΗΚΕ: <λίστα ή "τίποτα">
ΠΡΟΤΑΣΗ ΕΓΧΕΙΡΙΔΙΟΥ (προαιρετικό): <κενό που βρέθηκε — δεν διορθώνεται εδώ>
```

Κανόνες: ΜΠΛΟΚΑΡΕΤΑΙ μόνο με ≥1 εύρημα P1/P2 **ΕΠΙΒΕΒΑΙΩΜΕΝΟ**. ΥΠΟΘΕΣΗ ποτέ δεν μπλοκάρει. Κανένα όνομα εργαζομένου/πελάτη στην αναφορά (το repo είναι δημόσιο).

## 5. Τι ΔΕΝ κάνει ο ελεγκτής

- Καμία επεξεργασία αρχείων του repo εκτός από το **νέο** αρχείο `reviews/…`.
- Κανένα `git push`, `commit`, `checkout` στο working tree του συντονιστή, `stash`.
- Καμία πρόσβαση σε Supabase, Cloudflare, `.env.local`, MCP με κλειδιά.
- Δεν τρέχει tests/rigs (τα αναφέρει ως «πρέπει να τρέξουν»). Επιτρέπεται μόνο `node --check`.
- Δεν «διορθώνει» — δεν προτείνει patch, μόνο λέει τι σπάει και ποιος κανόνας.
- Δεν αλλάζει τα εγχειρίδια. Αν βρει κενό, το γράφει ως «ΠΡΟΤΑΣΗ ΕΓΧΕΙΡΙΔΙΟΥ» στο τέλος της αναφοράς.

## 6. Μετρικές εβδομάδας (συμπληρώνεται από τον συντονιστή, μία γραμμή ανά αξιολόγηση)

| Ημερομηνία | sha7 | Αποτέλεσμα | Ευρήματα (P1/P2/P3/P4) | Επιβεβαιωμένο χρήσιμο; (ναι/όχι/άγνωστο) | Ψευδής συναγερμός; (ναι/όχι) | Tokens | Λεπτά |
|---|---|---|---|---|---|---|---|
| 2026-09-22 | c4a9c69 | ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ | 0/0/2/2 (τύποι: 3,3,4,6,1-πληροφ.) | άγνωστο (συντονιστής) | όχι (Μ4 «Cancelled» = ψευδώς θετικό του script, όχι του ελεγκτή) | 148k | ~17 |
| 2026-09-22 | bd115c7 | ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ | 0/0/1/2 (τύποι: 3,2,6) | άγνωστο (συντονιστής) | όχι | 127k | ~12 |
| 2026-09-22 | 0f2f7f3 | ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ | 0/0/0/3 (τύποι: 6,6,6) | άγνωστο (συντονιστής) | όχι (Μ4 fixture strings = θόρυβος script) | 133k | ~14 |
| 2026-09-22 | 6fda476 | ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ | 0/0/1/3 (τύποι: 5,6,4,6) | άγνωστο (συντονιστής) | όχι· 1 ισχυρισμός εξωτερικός (Escape αποθηκεύει) ΔΕΝ επιβεβαιώθηκε | 161k (>150k) | ~18 |

**Σύνοψη 4 αξιολογήσεων 22/9:** 4/4 ΕΓΚΡΙΝΕΤΑΙ ΜΕ ΠΑΡΑΤΗΡΗΣΕΙΣ (0 ΜΠΛΟΚΑΡΕΤΑΙ)· 15 ευρήματα: τύπος 3 (κενό κάλυψης) 4, τύπος 6 (τεκμηρίωση/εργαλείο) 7, τύπος 4 (ασαφής κανόνας) 2, τύπος 5 (μη δοκιμασμένη διαδρομή) 1, τύπος 1 πληροφοριακό 1· P1/P2: 0· ψευδείς συναγερμοί ελεγκτή: 0 (3 ψευδώς θετικά του **script** Μ4 — Ε8/Ε9)· μέσα tokens ~142k (127–161k) — το όριο 150k του 11 ξεπεράστηκε 1/4· 1 εύρημα κλείστηκε από τον συγγραφέα στο επόμενο commit (units.js files). Σε κάθε αξιολόγηση ο ελεγκτής **διόρθωσε** τουλάχιστον έναν ισχυρισμό συγγραφέα/συντονιστή («byte-ίδιο», «Name πεδίο», «Escape αποθηκεύει») — αυτή είναι η αξία του ρόλου, όχι το μπλοκάρισμα.

Στο τέλος της εβδομάδας: πλήθος αξιολογήσεων · % χρήσιμα · % ψευδή · μέσα tokens · αλλαγές που πέρασαν ΧΩΡΙΣ αξιολόγηση (από `git log` vs `reviews/`). Κριτήριο συνέχισης: `04-πιλότος.md` §6. Τιμές αναφοράς από το benchmark 22/9 (`06-benchmark-2026-09-22.md`): ~90–110k tokens ανά αξιολόγηση με Sonnet σε diff 20–70 KB.

## 7. Προτάσεις του benchmark που ΔΕΝ εφαρμόστηκαν (`06-benchmark-2026-09-22.md`)

Ε2 (πίνακας βαθμού στο εγχειρίδιο ρόλου — υπάρχει μόνο εδώ §4), Ε3 (υιοθεσία υπάρχοντος κώδικα = έλεγχος ως νέο), Ε4 (κλειδί μοναδικότητας εισαγωγών στο εγχειρίδιο γνώσης), Ε5 (Μ9 μηχανικό για writer δημιουργίας-αντί-PATCH — μπαίνει στο script του βήματος 4, όχι εδώ), Ε7 (πρόσθετα ευρήματα → ουρά διαγνώσεων). Αποφασίζει ο συντονιστής/owner.

## 8. Πρώτη πραγματική αξιολόγηση

Η πρώτη αξιολόγηση θα γίνει στο επόμενο push του session τιμολόγησης (alias `#invoicing` → όψη Παραγγελιών), **πριν** το push, με SHA + branch από τον συντονιστή. Ο φάκελος `docs/grok-bot/reviews/` δημιουργείται τότε.
