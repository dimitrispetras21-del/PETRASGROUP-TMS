# 06b — Benchmark κάλυψης: μετρά ο agent αν οι έλεγχοι επαρκούν; (σχέδιο — ΟΧΙ εκτέλεση, ζητείται go)

Συμπλήρωμα του `06-benchmark-2026-09-22.md`. Εκείνο μέτρησε «βρίσκει το ελάττωμα;». Αυτό μετρά τα τρία που ζητά ο αναθεωρημένος ρόλος (`09`): **(α)** βλέπει ότι ένας **νέος κανόνας** δεν καλύπτεται από κανέναν έλεγχο; **(β)** βλέπει **αντίφαση** εγχειριδίου ↔ κώδικα; **(γ)** βλέπει **μη-προφανή επίπτωση** σε άλλη ροή; Καμία δοκιμή δεν έχει τρέξει. Ίδιο πρωτόκολλο με το 06: detached worktree, Sonnet subagent με Read/Grep μόνο, **χωρίς** τη γνωστή απάντηση, βαθμολόγηση από άνθρωπο.

## Δοκιμή (α) — νέος κανόνας χωρίς κάλυψη

| | |
|---|---|
| Εισροή | Το diff της `042_cash_lines_to_payroll.sql` (commit `7ec8bd6` 21/9) **ή** της `039_category_restatement.sql` (ίδιο commit), μαζί με το `02-έλεγχοι.md` **χωρίς** τους ελέγχους Ζ2 (Σ CASH) και Ζ3/Ζ6 — μια περικομμένη έκδοση φτιαγμένη μόνο για τη δοκιμή. Το skill-tms-knowledge.md ως έχει. |
| Ερώτηση (ακριβώς) | «Αυτή η αλλαγή εισάγει κανόνα επιχείρησης; Ποιον; **Ποιοι από τους υπάρχοντες ελέγχους του 02 καλύπτουν** τη νέα συμπεριφορά; Αν κανένας, τι έλεγχος λείπει (πρόταση, χωρίς εφαρμογή);» |
| Σωστή απάντηση (κρυφή) | 042: «Σ γραμμών CASH ανά RT = `dl_entries.expenses`, `expenses_auto`, `needs_review` όταν προϋπήρχε χειροκίνητη τιμή, `dl_cash_lock`». Κανένας έλεγχος (χωρίς Ζ2)· λείπει ισότητα ανά RT + `needs_review` + γραμμές CASH **χωρίς `rt_id`** που μένουν έξω σιωπηλά. 039: «restatement = ξεχωριστή κατηγορία, όχι επεξεργασία της αρχικής» — λείπει έλεγχος ότι κάθε restatement δείχνει σε υπάρχουσα γραμμή/RT. |
| Βαθμολόγηση | ΠΙΑΣΕ = ονομάζει τον κανόνα **και** «κανένας υπάρχων έλεγχος» **και** προτείνει κάτι σαν τον Ζ2 · ΜΕΡΙΚΩΣ = «καλύπτεται από Ζ1» (λάθος: το `dl_v_rt_gap` βλέπει μόνο απουσία γραμμής) · ΔΕΝ ΠΙΑΣΕ = «καμία επίπτωση στους ελέγχους» |
| Μάρτυρας ψευδούς συναγερμού | diff της `041_cost_docs_lines_deleted.sql` **με** το πλήρες 02 (έχει Ζ5) — σωστό: «καλύπτεται από Ζ5». Αν πει «λείπει έλεγχος» = ψευδής. |

## Δοκιμή (β) — αντίφαση εγχειρίδιο ↔ κώδικας (υπάρχει ήδη, δεν χρειάζεται κατασκευή)

| | |
|---|---|
| Εισροή | `CLAUDE.md` (σημερινό) + `worker/src/index.js` (σημερινό) + το νούμερο «users: 18 λογαριασμοί, 7 ενεργοί» (01 §6). Ερώτηση: «Σύγκρινε το εγχειρίδιο με τον κώδικα και τα δεδομένα. Πού διαφωνούν; Ποιο είναι σωστό και από ποια πηγή;» |
| Γνωστές αντιφάσεις (κρυφές) | 1. `CLAUDE.md:168` και `:510`: ο accountant «δεν έχει δικαίωμα εγγραφής στα orders» → ο κώδικας έχει `orders: ["GET","PATCH"]` από `2d08e6f` 3/9. 2. `CLAUDE.md:390` «6 πραγματικοί λογαριασμοί» → 18 (7 ενεργοί). 3. `CLAUDE.md` πίνακας «Ράμπα 0/30» → 82 με 38 δεμένες, νέες 16/16 (6/9 `c6d6f33`). 4. `docs/SECURITY.md`/`AUDIT_ACCESS.md` (7/5/2026, Airtable) περιγράφουν μηχανισμούς που δεν υπάρχουν. |
| Βαθμολόγηση | Πόσες από τις 4 βρήκε · για καθεμία είπε **ποιο είναι σωστό και από ποια πηγή**, ή διάλεξε αυθαίρετα; (αυθαίρετη επιλογή = αποτυχία τύπου 6, ακόμη κι αν σωστή) |
| Ψευδής συναγερμός | Αν δηλώσει «αντίφαση» μια κλειδωμένη απόφαση (π.χ. «national_orders άδειο ενώ ο κώδικας το γράφει») |

## Δοκιμή (γ) — μη-προφανής επίπτωση σε άλλη ροή

| | |
|---|---|
| Εισροή | Το diff της `037_rt_merge_split_pairs.sql` (`e45d07c` 20/9: συγχώνευση δύο RT + trigger `rt_link_split`) + εγχειρίδιο γνώσης. Ερώτηση: «Ποιες **άλλες** σελίδες, πίνακες και ροές επηρεάζει, πέρα από τα round trips; Για καθεμία: τι αλλάζει και ποιος έλεγχος το βλέπει;» |
| Σωστή απάντηση (κρυφή) | `dl_entries` (δύο γραμμές μισθοδοσίας → μία; trigger `dl_sync_from_rt`) · `ct_cost_lines.rt_id` του «χαμένου» RT (μεταφέρονται ή ορφανεύουν → Ζ7) · DKV ταίριασμα (`matchRoundTrip` ανά φορτηγό/ημερομηνία — ένα ενοποιημένο RT αλλάζει το ταίριασμα) · `ct_v_rt_pnl` (έσοδα δύο παραγγελιών σε ένα RT) · καρτέλα Μισθοδοσίας · `audit_log` (`after_data->>'split'`, ΣΤ1). Κάλυψη: μόνο ΣΤ1 + έμμεσα Ζ1/Ζ7 — **κανένας** για «μεταφέρθηκαν οι γραμμές κόστους/μισθοδοσίας του συγχωνευμένου RT». |
| Βαθμολόγηση | Από 5 επιπτώσεις: πόσες ονόμασε · για καθεμία «καλύπτεται από Χ» ή «δεν καλύπτεται» |
| Ψευδής συναγερμός | Επίπτωση που δεν υπάρχει (π.χ. «παλέτες» — τα `pl_movements` δένουν σε order_stop, όχι RT) |

## Πρωτόκολλο (κοινό)

1. Ένα detached worktree ανά δοκιμή στο scratchpad, χωρίς `.env.local`, MCP, push. Για (α)/(γ) στο SHA της αλλαγής· για (β) στο σημερινό `main`.
2. Οι «σωστές απαντήσεις» **δεν** δίνονται στον agent — μόνο στον βαθμολογητή.
3. Ίδια εισροή για κάθε υποψήφιο (Claude Code / Grok / άλλο).
4. Αποτέλεσμα: πίνακας 3 × (ΠΙΑΣΕ/ΜΕΡΙΚΩΣ/ΟΧΙ, ψευδείς, tokens) + 3 γραμμές συμπέρασμα. **Καμία υπόθεση πριν τρέξει.**
5. Κόστος (παραδοχή από το 06): ~110k tokens ανά δοκιμή, 4 δοκιμές ≈ 450k, ~1 ώρα.

**Ζητείται go** πριν τρέξει. Μετά: ίδια μέρα, αποτέλεσμα ως `06c-…md`, χωρίς αλλαγή εγχειριδίων.
