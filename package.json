{
  "name": "petrasgroup-tms",
  "version": "1.0.0",
  "private": true,
  "description": "Petras Group Transport Management System",
  "scripts": {
    "build": "bash build.sh",
    "test": "echo 'Open tests/test-runner.html in a browser for unit tests, or run: npm run e2e'",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "@playwright/test": "^1.44.0"
  }
}
